apiVersion: apps/v1
kind: Deployment
metadata:
  name: loretask-controller
  namespace: lore-floor
  labels:
    app: loretask-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loretask-controller
  template:
    metadata:
      labels:
        app: loretask-controller
      annotations:
        iam.gke.io/gcp-service-account: loretask-controller@${project_id}.iam.gserviceaccount.com
    spec:
      serviceAccountName: loretask-controller
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: controller
          image: ghcr.io/re-cinq/lore-floor:latest
          imagePullPolicy: Always
          command:
            - node
            - dist/delivery/loretask-controller-main.js
          env:
            - name: NAMESPACE
              value: lore-floor
            - name: LORE_LOG_BUCKET
              value: lore-task-logs-${project_id}
            - name: LORE_INGEST_URL
              value: ${lore_api_url}
          ports:
            - name: health
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi
