# Values for the standalone satellite chart (FR6,
# specs/running-stations-in-any-k8s-cluster). The empty values below are
# REQUIRED — templates fail the render loudly when any is missing. There are
# deliberately no Postgres/database values anywhere: the satellite reaches the
# world only through loreApiUrl + eventRouterUrl.

# ---- REQUIRED -------------------------------------------------------------

# Public HTTPS URL of the central lore-api (registration, claim, heartbeat,
# reads/writes on behalf of station pods).
loreApiUrl: ""

# Public HTTPS URL of the central event-router front door (POST /api/events);
# the satellite reports terminal Agent CR phases here with its per-agent token.
eventRouterUrl: ""

# The pre-shared registration token (LORE_CLUSTER_AGENT_REGISTRATION_TOKEN).
# Only used to register; every later call uses the per-agent token the
# registration returns, persisted in the identity Secret below.
registrationToken: ""

# This cluster's unique registry name (pipeline.cluster_agents.name).
name: ""

# Capability tags this cluster offers, e.g. ["node:agent"]. Rendered
# comma-joined into LORE_CLUSTER_AGENT_TAGS. Advertise a STATION type
# (node:validate, node:gate, node:detect) only if this
# cluster can serve it: every seeded station recipe mounts LORE_INGEST_TOKEN,
# which FR5 keeps central, so on a plain satellite such a claim dies at init
# with CreateContainerConfigError and wastes the run.
tags: []

# GHCR pull credentials — the chart writes a `ghcr-pull-secret`
# dockerconfigjson Secret into both namespaces (both charts' imagePullSecrets
# already point at that name).
ghcr:
  username: ""
  token: ""

# The agent LLM credential. The run pods mount agent-secrets[<secretKey>]; on
# GKE ESO writes that Secret, on a satellite THIS chart does, and the catalog
# sync renders the same key into every recipe it applies (use
# CLAUDE_CODE_OAUTH_TOKEN on a laptop to bill a subscription instead of org
# API credit).
llm:
  secretKey: ANTHROPIC_API_KEY
  credential: ""

# The per-cluster values this cluster-agent's catalog sync renders into the
# AgentDefinition/Station CRs it applies from lore.agent_definitions — this
# cluster's catalog has no other writer. Every empty value OMITS the block it
# feeds, which is the satellite default: a recipe must never point at a URL
# its pod cannot reach.
catalog:
  # `bare` is declared, never inferred (the 2026-09-01 incident); `full`
  # refuses to boot unless eventsUrl, mcpUrl and skillsUrl are all set.
  profile: bare
  # Live per-tool-call telemetry for this cluster's run pods — cost rows and
  # the run view — authenticated with the per-agent token this cluster-agent
  # publishes into `agent-secrets` after it registers. Empty = no http sink:
  # runs still execute and report their outcome, just not live. Two shapes
  # work: the PUBLIC lore-agent-events ingress (pods post straight out), or
  # this cluster-agent's own relay Service, which install-satellite.sh picks
  # when floorUrl is set — it queues and retries and lets the run pods' public
  # egress close. Keep ai-agents.floorSink below in agreement with it.
  eventsUrl: ""
  # The central Floor's agent-files endpoint (download inputs such as a
  # planning pod's plan.md, upload watched files), reached the same way as
  # eventsUrl. Set, the cluster also offers the `agent-files` tag; empty, it
  # never claims a pass whose pod downloads a file (every planning pass), so
  # those stay central.
  filesUrl: ""
  # The central lore-mcp gateway's public /mcp and /skills endpoints. Without
  # skillsUrl every Claude-agent recipe dies at startup ("Settings file not
  # found"), so leave it empty only on a cluster that claims non-agent
  # stations alone.
  mcpUrl: ""
  skillsUrl: ""
  # The image station-mode recipes (def-*) run.
  stationImage: ghcr.io/re-cinq/lore-station:latest
  # Repoints the ingest station's Dgraph env; empty keeps the recipe's own.
  dgraphUrl: ""
  # Credential keys for model families beyond anthropic, e.g.
  # {gemini: GEMINI_API_KEY}; a model whose family is absent is refused.
  modelSecretKeys: {}

# PUBLIC base URL of the central Floor. THE SWITCH for live run telemetry on
# this satellite, and the only knob an operator needs: set it and
# install-satellite.sh points this cluster's run pods at the local relay
# (`catalog.eventsUrl`), which forwards their NDJSON onward with the
# per-agent token this process already holds.
#
# Off by default, and deliberately not derived from any other value: a chart
# value that silently makes another one REQUIRED breaks every existing install
# the moment it upgrades.
#
# Public rather than in-cluster for the obvious reason — this is not the cluster
# the Floor runs in.
floorUrl: ""

# Stream this cluster's run-pod stdout to the centre as it happens
# (LORE_POD_LOG_STREAMING). OFF by default and deliberately so: it is the one
# input that puts log VOLUME on pipeline.events, a dispatch queue built for
# handler fan-out rather than bulk data. Turn it on for one cluster, watch the
# bus, then decide.
#
# What it buys is the reason the table exists: live pod logs are read from ONE
# cluster's Kubernetes API and the Cloud Logging fallback names ONE project, so
# a run claimed by a satellite is invisible to both. This is the only way a
# cluster that reports inward can have its logs read from the centre.
podLogStreaming: false

# Optional per-task GitHub credential (#1575). Without it, this cluster-agent
# can still register/claim/heartbeat — but a claimed run needing a git push
# (agent, github_action, retrospective) fails "GitHub not configured" once
# claimed, wasting the claim. (Station types need no push, but see `tags` above: they need a
# credential this chart never ships.) Set at most one of:
#   token          — a GitHub PAT scoped to the repos this satellite may push
#                    to (least privilege; the natural choice for a personal
#                    satellite)
#   app.appId/privateKey/installationId — the full GitHub App triple, for a
#                    satellite acting as the org's own identity
# `token` wins if both are set. Handing a satellite ANY GitHub credential is
# the operator's deliberate call — unlike the LLM credential, there is no
# default here.
github:
  token: ""
  app:
    appId: ""
    privateKey: ""
    installationId: ""
  secretName: lore-cluster-agent-github

# ---- Defaults -------------------------------------------------------------

# Namespace for the cluster-agent itself.
namespace: lore-cluster-agent
# Namespace the ai-agents subchart deploys into and this agent acts on. Must
# match the subchart's own `namespace` value.
agentsNamespace: ai-agents
# Render Namespace objects for both. Set false when the namespaces already
# exist outside Helm's ownership (scripts/install-satellite-minikube.sh does).
createNamespaces: true

replicaCount: 1

image:
  repository: ghcr.io/re-cinq/lore-cluster-agent
  tag: latest
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-pull-secret

resources:
  limits:
    cpu: "300m"
    # 4Gi, not the 256Mi the central chart uses. A satellite watches the Agent
    # CRs and streams pod logs for a cluster it does not otherwise serve, and on
    # a laptop minikube that OOM-killed it roughly seven seconds after the claim
    # loop started — repeatedly, on 2026-08-31, until the limit was raised.
    # Central has never hit it, which is why the two charts differ rather than
    # one default moving for both.
    #
    # A limit does not reserve: `requests` stays at 128Mi, so this costs nothing
    # until the process actually needs it. What it buys is a satellite that does
    # not crash-loop the first time a busy cluster makes it read more than the
    # ceiling allowed.
    memory: 4Gi
  requests:
    cpu: "50m"
    memory: 128Mi

env:
  PORT: "8080"

# Where the satellite persists its registered identity ({id, token}). The file
# is surfaced by mounting the named Secret; the pod WRITES it through the
# Kubernetes API (see templates/rbac.yaml — create/update on exactly this one
# Secret), so a restart re-registers with the persisted per-agent token
# instead of minting a new identity.
identity:
  secretName: lore-cluster-agent-identity
  mountPath: /var/lib/lore-identity
  fileName: identity.json

# Subchart values (the ai-agents subsystem installed beside the agent).
ai-agents:
  # The run-pod NetworkPolicy hole for the telemetry sink. Centrally it points
  # at the Floor; here it must point at the local cluster-agent, or the policy
  # drops exactly the traffic the sink needs. Must agree with catalog.eventsUrl.
  floorSink:
    namespace: lore-cluster-agent
    port: 8080
    app: lore-cluster-agent
