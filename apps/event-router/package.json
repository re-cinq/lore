{
  "name": "@re-cinq/lore-event-router",
  "version": "0.1.0",
  "description": "Lore event router \u2014 the one writer of pipeline.events (GitHub webhooks + the Agent CR watch)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "start:watch": "node --watch-path=./dist --watch-path=../../libs/shared/dist dist/index.js"
  },
  "dependencies": {
    "@hapi/hapi": "21.4.10",
    "@re-cinq/lore-shared": "*",
    "pg": "^8.23.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/pg": "^8.23.1",
    "@vitest/coverage-v8": "^5.0.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
