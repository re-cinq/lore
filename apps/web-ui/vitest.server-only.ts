// Next.js provides `server-only` as a virtual module that throws when a server file is bundled for the browser. Vitest has no such module, so every suite that reaches a server file resolves it here instead.
export {};
