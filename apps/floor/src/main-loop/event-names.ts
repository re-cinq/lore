/** Event sources moved to `@re-cinq/lore-shared` when the event-router became a separate deployable (ADR-044); re-exported since Floor's modules have always imported it from here. */

export { SOURCES, type EventSource } from "@re-cinq/lore-shared";
