# Feature Specification: Planning a Feature Together

| Field          | Value                                       |
|----------------|---------------------------------------------|
| Feature        | Planning a Feature Together                 |
| Branch         | 7-feature-planning                          |
| Status         | In Progress                             |
| Created        | 2026-06-17                                  |
| Owner          | Platform Engineering                        |

A feature is planned as a plan people and the planning agent write together in one live document, in a fixed template, until someone approves it; approval hands the plan to the spec PR and to decomposition on the same assembly line.

## Problem Statement

Planning used to be a wizard over `lore.features`: one author typed direction per
section, the agent regenerated its whole gap analysis every round, and nobody else
could work on the draft. The people who own a change — product, engineering, the
team that will run it — could not write it together, and every round threw away
what the last one had settled.

## How a plan moves

A plan is created from a title, a template and what the author already knows, and
its planning line starts at once. The agent drafts into the live plan while people
read and write it (ADR-047). A section's **Refine** sends that section back to the
agent, whose answer comes back as a proposal someone accepts. Approval ends the
plan: the same line analyses which specs it changes, writes and pushes them, waits
for the spec PR to merge, decomposes the merged spec into stories and tasks, and
files them.

## Functional Requirements

### FR-17: Plans hosted by lore-api (planning-station)

The drafting half of a feature moves onto the collaborative plan document of
[re-cinq/planning-station](https://github.com/re-cinq/planning-station): people
and the planning agent write one plan together, in a fixed template, until it is
approved. lore-api hosts it with `@re-cinq/planning-sync`, on its own listener ([validated by creates a plan with a write token and reads back its feature sections](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L102)).

- lore-api keeps plans in `lore.plans`, `lore.plan_state` and `lore.plan_versions`, and its store passes every check of planning-sync's own `PlanStore` contract ([validated by](../../apps/lore-api/src/integration-tests/plan-store.test.ts#L24)).
- Every `/api/plans` call carries a Lore bearer token: without one it answers 401, a write with a read-only token answers 403, and a write token creates a plan any token can read back with its template's sections ([validated by answers 401 to a plan read without a bearer token](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L89), [validated by answers 403 to a plan created with a read-only token](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L96), [validated by creates a plan with a write token and reads back its feature sections](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L102)).
- A repo's plans are listed at `GET /api/repos/{owner}/{repo}/plans` ([validated by lists the plan Ana created as a draft feature of the repo](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L120)).
- The collaboration socket opens only with a collab token lore-api minted for one person on one plan of one repo; it opens no other plan, lapses after ten minutes, and is refused with 404 for a plan under another repo ([validated by mints a collab token that opens the plan as Ana](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L138), [validated by](../../apps/lore-api/src/integration-tests/plan-collab-tokens.test.ts#L45), [validated by](../../apps/lore-api/src/integration-tests/plan-collab-tokens.test.ts#L65), [validated by](../../apps/lore-api/src/integration-tests/plan-collab-tokens.test.ts#L81), [validated by answers 404 to a collab token for the plan under another repo](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L163)).

- A repo's **Plans** tab lists its plans, each linking to its page with its status, and invites a first plan with **+ Plan** when there are none ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/PlansPage.test.tsx#L29), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/PlanListView.test.tsx#L18), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/PlanListView.test.tsx#L26), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L45)).
- A plan's status reads as a coloured pill: Draft muted, Approved in the success colour, and an unknown status as itself ([validated by](../../apps/web-ui/src/lib/plan-status.test.ts#L5), [validated by](../../apps/web-ui/src/lib/plan-status.test.ts#L12), [validated by](../../apps/web-ui/src/lib/plan-status.test.ts#L19)).
- A new plan takes a title, one of the templates (feature by default) and what the author already knows; a blank title or an unknown template creates nothing ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/new/NewPlanView.test.tsx#L7), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/new/actions.test.ts#L53), [validated by](../../apps/web-ui/src/lib/plan-input.test.ts#L13), [validated by](../../apps/web-ui/src/lib/plan-input.test.ts#L29), [validated by](../../apps/web-ui/src/lib/plan-input.test.ts#L37)).
- Creating a plan records the signed-in person as its author, writes their description into the plan's intent paragraph by paragraph (leaving the template's own when it is empty), asks the planning agent for its first draft, and opens the plan ([validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L55), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L162), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/new/actions.test.ts#L66), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/new/actions.test.ts#L108), [validated by](../../apps/web-ui/src/lib/plan-input.test.ts#L49), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L75), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L125), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L162), [validated by seeds plan p1's intent with gedaiu's two paragraphs](../../apps/web-ui/src/lib/api/plans.test.ts#L140)).
- A person is their GitHub login inside a plan, shown by their display name in a colour of their own that stays the same between visits; a session signed in before the login was carried uses the display name ([validated by](../../apps/web-ui/src/lib/auth-options.test.ts#L115), [validated by](../../apps/web-ui/src/lib/plan-user.test.ts#L5), [validated by](../../apps/web-ui/src/lib/plan-user.test.ts#L14), [validated by](../../apps/web-ui/src/lib/plan-user.test.ts#L27), [validated by](../../apps/web-ui/src/lib/plan-user.test.ts#L31)).
- A plan's page opens the editor for a signed-in person and asks anyone else to sign in; a plan is not found under a repo it does not belong to ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L38), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L148), [validated by](../../apps/web-ui/src/lib/plan-user.test.ts#L21), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailPage.test.tsx#L20), [validated by approves plan p1 of re-cinq/lore in gedaiu's name through lore's own route](../../apps/web-ui/src/lib/api/plans.test.ts#L85)).
- While the planning agent writes the first draft (the run is queued, just started, or on its first `analyze` visit), the page shows a "writing this plan" placeholder instead of the editor, since the draft replaces anything typed meanwhile, and reloads every 5 seconds until the draft is in. A Refine only proposes one section, so the editor stays open while a section is refined, and a failed run opens it too ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L51), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/DraftingPlan.test.tsx#L11), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/DraftingPlan.test.tsx#L19), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L125), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L133)).
- A plan's page shows its planning run above the editor: its OPEN `feature-planning` run keyed `plan:<id>`, else its newest — a newer run can be over while an older one was reopened *(amended 2026-09-24)*, an **Open the run →** button to the run page (which draws the line), and the spec PR once one is open, or "No planning run yet." before one starts ([validated by](../../apps/web-ui/src/lib/plan-run.test.ts#L51), [validated by](../../apps/web-ui/src/lib/plan-run.test.ts#L62), [validated by](../../apps/web-ui/src/lib/plan-run.test.ts#L72), [validated by](../../apps/web-ui/src/lib/plan-run.test.ts#L81), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L197), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L204), [validated by says the plan waits for its people and links run-1's page](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L35), [validated by links the spec PR once the run opened one](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L56), [validated by says no run has started for a plan without one](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L76)).
- The run says in one sentence what it is doing: waiting for a runner, the agent drafting or refining a section, waiting for its people to refine or approve, writing the specs, waiting for the spec PR to merge, breaking the spec into stories and tasks, failed with its reason (a run that finished with any outcome but `completed` failed, whatever its status says), the agent's last pass failed while the line waits on its people, or done ([validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L13), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L20), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L27), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L39), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L48), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L57), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L64), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L72), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L81), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L95), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L113)).
- A plan whose planning run failed, whose line waits on its people at `author` (including after a pass that failed, or one whose file never landed), or whose run never started, offers a red **Regenerate plan** button at the right of the run line. It asks first in a popup that warns the agent drafts the plan again and can replace what its sections say; only on confirm does it start a fresh drafting run, once GitHub confirms the person can see the repo, and reload the page onto it. Cancelling starts nothing. The run page cannot retry here, since a run that fails on its first node has nothing before it to fork from. While the agent is at work, or once the plan is past approval, it offers no regeneration ([validated by regenerates the failed plan only after the confirmation popup, then refreshes](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L101), [validated by closes the popup without regenerating when cancelled](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L134), [validated by offers a regeneration for a plan no run was ever started for](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L173), [validated by offers a regeneration for a plan no run was ever started for](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L173), [validated by offers a regeneration while the run waits on its people at author](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L188), [validated by asks the planning agent for a fresh draft of plan p1 in gedaiu's name](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L243), [validated by starts no draft for someone GitHub does not let see the repo](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L259), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L151), [validated by](../../apps/web-ui/src/lib/plan-run-phase.test.ts#L162), [validated by warns in the popup that the agent's draft can replace what the plan's sections say](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L157)).
- The page asks lore-api for a collab token only after GitHub confirms the person can see the repo, and hands the browser the plan's document name and the token; the socket itself is the tab's one live socket (ADR-048), so the page names no socket address ([validated by hands gedaiu a token minted for plan p1 and the plan's document name](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L55), [validated by mints nothing for someone GitHub does not let see the repo](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L64), [validated by asks a visitor without a session to sign in](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L76), [validated by mints a write collab token on plan p1 of re-cinq/lore for gedaiu](../../apps/web-ui/src/lib/api/plans.test.ts#L125), [validated by](../../apps/web-ui/src/lib/live-socket/channel-websocket.test.ts#L37)).
- **Approve plan** sits under the plan outline, off while the outline lists what the plan still needs. It asks first in a popup that says the agent will write the specs and open a spec PR; only on confirm does it approve and reload the page, and a refusal from lore-api shows its reason in place ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/ApprovePlanButton.test.tsx#L11), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/ApprovePlanButton.test.tsx#L37), [validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/ApprovePlanButton.test.tsx#L60)).
- Approving records the signed-in person as the approver, and a plan lore-api refuses is reported as not ready yet ([validated by approves plan p1 in gedaiu's name through lore's route](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L86), [validated by reports a plan lore-api refuses to approve as not ready](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L102), [validated by approves plan p1 of re-cinq/lore in gedaiu's name through lore's own route](../../apps/web-ui/src/lib/api/plans.test.ts#L85)).
- A section's Refine reaches lore-api only after GitHub confirms the person can see the repo; a Refine lore-api refuses while the agent is still drafting is reported, so the editor withdraws the ask ([validated by asks the planning agent to refine the intent section of plan p1](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L221), [validated by reports a Refine lore-api refuses while the agent is still drafting](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L233), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L172)).

- The page derives ONE state from the plan's status and where its run is — drafting, writing, refining, reopened, answering, spec-work, spec-pr-open, question, spec-work-failed, delivering, delivered — and every control reads that state rather than the run alone; a plan with no run is writing while a draft and spec-work-failed once approved ([validated by is drafting while the run is queued](../../apps/web-ui/src/lib/plan-page-state.test.ts#L23), [validated by is writing while the line waits on the author, and after a run that failed before approval](../../apps/web-ui/src/lib/plan-page-state.test.ts#L27), [validated by is refining while analyze runs again for a section](../../apps/web-ui/src/lib/plan-page-state.test.ts#L35), [validated by is reopened while the line waits on the author with spec PR #7 already open](../../apps/web-ui/src/lib/plan-page-state.test.ts#L47), [validated by is spec-work while analyse-specs, write or push runs](../../apps/web-ui/src/lib/plan-page-state.test.ts#L79), [validated by is spec-pr-open while the merged node is parked](../../apps/web-ui/src/lib/plan-page-state.test.ts#L87), [validated by is question while the line came back to the author](../../apps/web-ui/src/lib/plan-page-state.test.ts#L93), [validated by is spec-work-failed after a run that failed or ended short of completing, and with no run at all](../../apps/web-ui/src/lib/plan-page-state.test.ts#L105), [validated by is delivering while decompose or issues runs, and delivered once the run completed](../../apps/web-ui/src/lib/plan-page-state.test.ts#L116)).
- **An approved plan is read-only.** Its editor refuses edits (lore-api's socket already does; the editor says so instead of swallowing them), the header says who approved it and when, and the outline footer carries the actions its state calls for together with a link to the spec PR wherever one exists ([validated by](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L76), [validated by offers only the spec PR link while the specs are being written](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L62), [validated by links spec PR #7 and offers Reopen plan while the PR is open](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L68)).
- **Approve plan** reaches lore-api's own approve route, which refuses BEFORE the status flips while the planning agent is still refining a section; the page shows that reason in place and holds the button with it while a Refine is in flight, and a refusal carrying the validation report still reads as "not ready" ([validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L85), [validated by approves plan p1 in gedaiu's name through lore's route](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L86), [validated by reports a plan lore-api refuses to approve as not ready](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L102), [validated by reports lore-api's reason when the planning agent is still refining a section](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L110), [validated by offers Approve plan while the plan is being written](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L32), [validated by holds Approve plan while the planning agent is refining a section](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L38)).
- **Reopen plan** is offered on an approved plan whose spec PR is open, whose spec work failed, or whose spec-tasks are filed — and on one whose spec work has a question for its people only if the line's own reopen (FR-18) never landed; it asks first, saying what follows — the open PR goes back to the author and the next approval updates it, or, once the specs merged, the next approval opens a NEW spec PR revising them — and a refusal from lore-api is shown as its reason. Reopened, the plan is written like a draft again, the run line says so, and Approve says which PR it updates. When the spec analysis sends the line back to its author, the question it asked — the summary of its spec plan, which lore-api serves on the run as `spec_plan_summary` whether the pod wrote the plan as JSON text or an object — is quoted in the run line, since answering it is the plan's next step. The quote sits in a panel titled "The spec writer has a question for you" that a reader who never saw the spec step can follow cold: it says the approval started an agent turning the plan into spec files, that the agent stopped on a decision it cannot make alone, and lists the clicks that answer it — Reopen, write the answer into the plan, Approve. Only a plan still approved there shows Reopen beside the question ([validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L95), [validated by reopens plan p1 in gedaiu's name](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L120), [validated by reports lore-api's reason for refusing to reopen while the specs are being written](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L136), [validated by warns that reopening a delivered plan opens a new spec PR on the next approval](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L88), [validated by says approving a reopened plan updates spec PR #7](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L49), [validated by explains that spec writing paused on a question, quotes it without its verdict word, and says to Reopen, answer, then Approve](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L236), [validated by says a reopened plan updates its spec PR on the next approval](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L301), [validated by](../../apps/lore-api/src/transport/routes/assembly-lines/assembly-lines.test.ts#L321), [validated by](../../apps/web-ui/src/lib/plan-run.test.ts#L62)).
- A plan the spec analysis sent back to its author is already open again, reopened by its line (FR-18), so the page quotes the question, asks for the answer in the plan, and offers **Approve plan** rather than Reopen ([validated by is answering while the reopened plan's line waits on the author after the spec analysis's question](../../apps/web-ui/src/lib/plan-page-state.test.ts#L64), [validated by offers Approve plan while the plan's people answer the spec analysis's question](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L56), [validated by tells the people of a plan its line already reopened to answer in the plan and Approve, with no Reopen](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L269)).
- A spec pass that failed offers **Retry the spec work** (a fresh pass from the approved plan, asked first) beside Reopen, and never Regenerate: redrafting an approved plan is not what anyone asked for. The run line says the spec work did not deliver ([validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L115), [validated by starts a fresh spec pass for plan p1 in gedaiu's name](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L183), [validated by starts nothing for someone GitHub does not let see the repo](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L199), [validated by offers Retry the spec work and Reopen plan after the spec work failed](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L82), [validated by retries the spec work only after the confirmation popup](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanOutlineActions.test.tsx#L97), [validated by offers no regeneration once the spec work failed, and says to retry or reopen instead](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanRunCard.test.tsx#L211)).
- **Delete plan** sits in the plan page's header in every state; it asks first in a popup that names the plan, says the delete cannot be undone and keeps its Delete button off until the plan's name is typed in full, and only a confirmed delete reaches lore-api, which removes the plan with its document, versions and collab tokens and answers 404 for a plan under another repo; the page then goes back to the repo's plans, and a refusal is shown as its reason ([validated by asks before deleting the plan, saying it cannot be undone](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L100), [validated by keeps Delete off until the plan's name Faster checkout is typed in full](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L109), [validated by deletes the plan once its name is typed and the delete is confirmed](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/PlanDetailView.test.tsx#L134), [validated by deletes plan p1 of re-cinq/lore and goes back to the repo's plans](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L146), [validated by deletes nothing for someone GitHub does not let see the repo](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L161), [validated by reports lore-api's reason when plan p1 is not found](../../apps/web-ui/src/app/repos/[owner]/[repo]/plans/[id]/actions.test.ts#L173), [validated by](../../apps/web-ui/src/lib/api/plans.test.ts#L105), [validated by deletes Ana's plan with its versions, so it no longer reads back](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L305), [validated by answers 404 to deleting Ana's plan under another repo, and keeps it](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L329)).

### FR-18: The planning line drives the plan

One `feature-planning` assembly line carries a plan from its first draft to its
spec-tasks. Its agent writes into the live plan through lore-api, its `author`
node parks while people write, and approval moves the same line on to the spec
PR and decomposition.

- Creating a plan starts its line: a `feature-planning` task whose brief names plan.md and carries what the author already knows, whose run is keyed `plan:<id>` so one plan has one line in flight, and whose args carry the repo and the plan's title ([validated by starts gedaiu's feature-planning task on plan p1 of re-cinq/lore with its title for the PR](../../apps/lore-api/src/work/plans/planning-line.test.ts#L51), [validated by](../../apps/lore-api/src/work/plans/plan-briefs.test.ts#L12), [validated by](../../libs/shared/src/outbound/project/assembly-runs/subject-keys.test.ts#L12), [validated by](../../libs/shared/src/outbound/project/assembly-runs/subject-keys.test.ts#L38), [validated by starts the planning agent's draft of Ana's plan as a feature-planning task](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L178)).
- The agent edits the plan as a file, moved by reference, never inline: before the pass the pod downloads the live plan as `plan.md` from the Floor, which renders it from lore-api's live document when the pod asks, and when the agent exits the supervisor uploads the edited file, whose event is then only a notice. No plan content rides the prompt, the Agent object or the event stream, so a plan of any size fits. lore-api turns what the file changed into ops: a draft's are written into the live plan as the planning agent, a Refine's become a proposal for the section it was asked about AND for every other section a settled answer it used makes wrong, each proposed against that section as it stands and each accepted or refused on its own; a section whose proposal someone is already reviewing is left alone and reported. A file with problems and nothing it could write is refused. A plan of several megabytes goes through the same way. A cluster with no files endpoint never claims a planning pass, which stays with a cluster that serves the file ([validated by requires agent-files of the cluster that claims a pod downloading plan.md, so a cluster that cannot serve it never takes the pass](../../apps/floor/src/work/assembly-run/advance-line.test.ts#L1092), [validated by](../../libs/shared/src/outbound/project/agents/agent-defaults-content.test.ts#L278), [validated by](../../apps/floor/src/work/assembly-run/launch-spec.test.ts#L178), [validated by](../../apps/floor/src/work/assembly-run/launch-spec.test.ts#L191), [validated by](../../libs/shared/src/outbound/cluster/agent-backend.test.ts#L95), [validated by](../../libs/shared/src/outbound/project/agents/agent-crd.test.ts#L146), [validated by](../../libs/shared/src/outbound/project/agents/agent-crd.test.ts#L168), [validated by](../../apps/floor/src/transport/http/routes/agent-files.test.ts#L65), [validated by](../../apps/floor/src/transport/http/routes/agent-files.test.ts#L85), [validated by](../../apps/floor/src/transport/http/routes/agent-files.test.ts#L97), [validated by](../../apps/floor/src/transport/http/routes/agent-files.test.ts#L163), [validated by](../../apps/floor/src/work/agent/agent-events.test.ts#L412), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L63), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L94), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L103), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L110), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L117), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L131), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L155), [validated by serves run-1's plan p1 as the markdown the pod downloads](../../apps/floor/src/work/agent/planning-result.test.ts#L188), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L199), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L49), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L60), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L127), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L156), [validated by serves Ana's plan as the plan.md a planning pod edits, one marked heading per section](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L404), [validated by writes a draft's plan.md into Ana's live plan as the planning agent's edits](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L412), [validated by writes a 3 MB draft of short paragraphs into Ana's live plan and serves it back whole](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L438), [validated by answers 400 to a plan.md whose only change names no section of Ana's plan](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L463), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L62), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L141)).
- A section's Refine resumes the parked `author` node with a brief naming that section by its `<!-- slot:… -->` marker (and telling the agent to add, as new sections of their own, the sections a settled answer asks for — "one section per tool" — rather than writing them into the refined one), and records the Refine on the run (slot, hash, uses), so the edited file comes back as that section's proposal without the agent copying anything: only that section's ops are proposed, edits elsewhere are reported, and an untouched section gets an empty proposal rather than an ask nobody answers. While the agent is still at work the Refine is refused with 409 and nothing is resumed ([validated by sends the author node back to the agent with the intent section's refine brief](../../apps/lore-api/src/work/plans/planning-line.test.ts#L123), [validated by refuses a Refine while the agent is still drafting, resuming nothing](../../apps/lore-api/src/work/plans/planning-line.test.ts#L148), [validated by](../../apps/lore-api/src/work/plans/plan-briefs.test.ts#L23), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L75), [validated by](../../apps/floor/src/work/agent/planning-result.test.ts#L212), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L104), [validated by](../../apps/lore-api/src/work/plans/plan-file.test.ts#L106), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L27), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L48), [validated by answers 409 to a Refine while no planning line waits on the plan](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L204)).
- A pass that fails, the first draft or a Refine, hands the plan back to its people instead of ending its line: a pass whose plan.md never landed (its upload failed, or it was never produced) is a failed pass however cleanly the agent exited, while an uploaded file counts as delivered; the line parks on `author`, where the page names the failure and they ask again. No number of rounds, refined or failed, ends the line, because every pass is asked for by a person and the node ceiling counts only the visits since a person last acted ([validated by](../../libs/assembly-lines/src/planning-author-loop.test.ts#L62), [validated by](../../libs/assembly-lines/src/planning-author-loop.test.ts#L68), [validated by](../../libs/assembly-lines/src/transition.test.ts#L292), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L149), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L162)).
- A Refine whose agent exited non-zero is not answered with the file it uploaded, which is the plan as it was downloaded: the Floor reads the exit code the supervisor sends with the upload and marks that section's Refine failed with it, so the section says the agent could not refine it and offers Refine again instead of showing an answer that changed nothing. A draft's file is still written, since a draft answers no Refine ([validated by marks the run's Refine of intent failed, writing nothing, when the agent exited 42](../../apps/floor/src/transport/http/routes/agent-files.test.ts#L130), [validated by still writes a draft whose agent exited 1, since a draft answers no Refine](../../apps/floor/src/work/agent/planning-result.test.ts#L168), [validated by marks plan p1's Refine of scope failed with the reason the pass stopped](../../apps/lore-api/src/transport/routes/plans/plan-file.test.ts#L28), [validated by answers 400 to a failure that names no section, and marks nothing](../../apps/lore-api/src/transport/routes/plans/plan-file.test.ts#L54)).
- Drafting a plan whose line waits on its people (Regenerate) sends the parked `author` node back to the agent with the draft brief. Starting a new run would only join the plan's open line and do nothing ([validated by sends the parked author node back to the agent with a fresh draft brief, creating no task](../../apps/lore-api/src/work/plans/planning-line.test.ts#L85)).
- Approving the plan resumes the `author` node with a brief naming the approved plan.md as the spec work's settled input, and the spec steps after approval (spec-analysis, spec-write) download that same file instead of reading the plan from their prompt; a plan whose line is not waiting on its people moves nothing ([validated by moves the approved plan on to the spec work as its brief](../../apps/lore-api/src/work/plans/planning-line.test.ts#L293), [validated by](../../apps/lore-api/src/work/plans/planning-line.test.ts#L182), [validated by](../../apps/lore-api/src/work/plans/plan-briefs.test.ts#L43), [validated by](../../libs/shared/src/outbound/project/agents/agent-defaults-content.test.ts#L285)).
- The planning agent gathers what the organisation knows before it writes: its prompt sends it to `lore_assemble_context` first, then the `lore_query_graph` knowledge graph for the services, teams and technologies the plan touches, and `query_trace` for the specs and tests it overlaps, so sections like who operates it and what could break come from the graph rather than a guess ([validated by](../../libs/shared/src/outbound/project/agents/agent-defaults-content.test.ts#L254)).
- The planning agent asks each question once, as a question in the section it is about; its prompt says the Open questions section holds only question fences, for a question that belongs to no other section, so it never restates its questions there as a bullet list, which the template's validation would flag as one disallowed block per bullet ([validated by tells the planning agent that Open questions holds only question fences](../../libs/shared/src/outbound/project/agents/agent-defaults-content.test.ts#L267)).
- The spec PR is titled after the plan, and the spec a spec plan chiefly creates (else updates) rides the line as `spec_path` ([validated by](../../apps/floor/src/work/assembly-run/spec-pr.test.ts#L136), [validated by](../../apps/floor/src/work/assembly-run/spec-pr.test.ts#L180), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L188), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L200), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L208), [validated by](../../apps/floor/src/work/agent/artifact-args.test.ts#L214)).
- Every spec-task the decomposition files carries its plan and that spec path, and once the last task of the group merges, merge-check flips that spec's status from the path alone ([validated by](../../apps/stations/src/work/issues/issues.test.ts#L144), [validated by](../../apps/stations/src/work/issues/issues.test.ts#L186), [validated by](../../apps/stations/src/work/merge-check/spec-status-flip.test.ts#L14), [validated by](../../apps/stations/src/work/merge-check/spec-status-flip.test.ts#L32)).

- Where a plan's line is — parked on its author, parked on its spec PR, on an agent node, ended — and what it delivered (the spec PR's number, whether that PR merged) is read once, as one shape, and every post-approval decision reads it ([validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L102), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L106), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L130), [validated by](../../libs/shared/src/outbound/project/plans/plan-run.test.ts#L151)).
- **Approval is decided before the status flips.** A line parked on its author is handed over; a plan with no open line (none yet, a spec pass that failed, or specs that already merged) gets a FRESH `feature-planning` line entered at `analyse-specs` (`line_args.entry_node`), briefed as the approved plan or, after a merged spec PR, as an amendment of what is on main naming that PR; a line the agent is on is refused with 409 naming why (still refining a section, or the specs being written), so an approval can never strand a plan. The same fresh pass is what **Retry the spec work** starts, refused while a line is running ([validated by hands the plan over when its line waits on the author](../../apps/lore-api/src/work/plans/planning-line.test.ts#L254), [validated by starts the spec work for a plan with no line, one whose spec work failed, and one whose specs merged](../../apps/lore-api/src/work/plans/planning-line.test.ts#L260), [validated by refuses while the planning agent is still refining a section, and while the specs are being written](../../apps/lore-api/src/work/plans/planning-line.test.ts#L274), [validated by moves the approved plan on to the spec work as its brief](../../apps/lore-api/src/work/plans/planning-line.test.ts#L293), [validated by starts a fresh spec pass at analyse-specs when the plan's spec work failed, resuming nothing](../../apps/lore-api/src/work/plans/planning-line.test.ts#L316), [validated by briefs the fresh spec pass as a revision of spec PR #7 when the plan's specs already merged](../../apps/lore-api/src/work/plans/planning-line.test.ts#L345), [validated by hands over nothing while the planning agent is still refining a section](../../apps/lore-api/src/work/plans/planning-line.test.ts#L355), [validated by](../../apps/lore-api/src/work/plans/plan-briefs.test.ts#L37), [validated by answers 409 to approving Ana's plan while its sections are still empty, naming what it lacks](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L241), [validated by starts a fresh spec pass at analyse-specs for Ana's approved plan whose line is not running](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L256), [validated by](../../apps/floor/src/work/assembly-run/feature-planning-acceptance.test.ts#L132)).
- **Reopening** an approved plan sends its open spec PR back to the author — the `merged → author` edge's only reporter, resumed with `changes_requested` in the person's name — and turns the plan back into a draft; a line already waiting on the author, ended, or never started needs no report; a line the spec work is on refuses (409: the specs are being written, or the spec-tasks are being filed), and a plan that is not approved cannot be reopened ([validated by sends the open spec PR back to the author in gedaiu's name](../../apps/lore-api/src/work/plans/planning-line.test.ts#L368), [validated by reports nothing for a plan whose line waits on the author, ended, or never started](../../apps/lore-api/src/work/plans/planning-line.test.ts#L386), [validated by refuses while the specs are being written, and while the merged spec is being broken into tasks](../../apps/lore-api/src/work/plans/planning-line.test.ts#L401), [validated by reopens Ana's approved plan as a draft, and refuses to reopen it twice](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L284)).
- **A line waiting on its author means the plan is open for writing.** Whenever the line parks on the `author` node while the plan is approved — the spec analysis asked a question, or someone ran the station by hand — the plan is reopened, so the people it waits on can Refine and Approve instead of facing a read-only editor. lore-api's `author-waiting` route decides it from the line itself, so it is safe to call at any time: a draft plan, or an approved one whose line is anywhere but the author, is left as it is ([validated by reopens approved plan p1 when the spec analysis sent its line back to the author](../../apps/lore-api/src/work/plans/planning-line.test.ts#L427), [validated by reopens nothing for a draft plan, nor an approved one whose line is not on the author](../../apps/lore-api/src/work/plans/planning-line.test.ts#L439), [validated by reopens Ana's approved plan when its planning line waits on the author, and leaves it be once reopened](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L358), [validated by leaves Ana's approved plan approved when no planning line waits on its author](../../apps/lore-api/src/integration-tests/plan-routes.test.ts#L384)).
- **The spec-write pass delivers.** It is a delivering recipe: it pushes what it committed, and an empty branch fails the pass itself rather than the push node after it. Plan b5ec0e24's specs were committed in the write pod and never pushed (2026-09-23): the push node, another pod with a fresh clone, found nothing, and no spec PR could be opened ([validated by](../../libs/shared/src/domain/task-types/delivering-recipes.test.ts#L5), [validated by](../../libs/shared/src/domain/task-types/delivering-recipes.test.ts#L9)).

## Carried over from the feature wizard

The statements below outlived the wizard: the behaviour they pin — decomposition,
conversation continuity, the shared view components — still runs, now for plans.

## Integration & Relationships

- **Feature-request task type (`specs/3`).** This feature is the interactive,
  multi-round successor to one-shot `feature-request`: same end artifact (a
  `spec.md` PR following repo conventions), but with a human in the loop, a
  persistent draft, and gap-closing analysis. `feature-request` remains for the
  fully-automated path: its one-shot handler commits the three spec artifacts
  (`spec.md`/`data-model.md`/`tasks.md`) and only then opens the PR, and throws
  without opening a PR when the model SKIPs every artifact. ([validated by commits the three spec artifacts then opens the PR](apps/floor/src/work/task/handle-feature-request.test.ts#L72), [throws when every artifact is SKIP and opens no PR](apps/floor/src/work/task/handle-feature-request.test.ts#L100))

### FR-1: Features Tab, List & Detail

- The feature page's long documents FOLD: the accumulated draft spec is collapsed by default and the author's prompt starts open, because the point of folding is that the spec had taken the page over while the prompt is what a reader came for. Built on native `<details>`, so a closed card keeps its content in the DOM — find-in-page and screen readers still reach a collapsed spec, which rendering the children conditionally would break — and the browser owns the keyboard and ARIA behaviour. ([validated by shows its title as the control that opens it](apps/web-ui/src/components/CollapsibleCard.test.tsx#L9), [`CollapsibleCard.test.tsx:19`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L19), [`CollapsibleCard.test.tsx:29`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L29), [`CollapsibleCard.test.tsx:39`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L39), [`CollapsibleCard.test.tsx:49`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L49))
- FR-1.6: *(added 2026-08-13)* An irreversible action on the detail view is framed by a shared `DangerZone`: what it is, what it costs, and the caller's own controls. The view composed `"spec-card danger-zone"` by hand and depended on three globals.css selectors; both now live in the component. The controls stay `children` because confirmation flows differ — a two-step confirm here, a typed name elsewhere — and baking one in would make the second caller fight it. `FeatureDetailView`'s own delete control is that two-step confirm: a "Delete feature" button swaps for a named confirm prompt with Confirm/Cancel controls, and Cancel backs out to the original button without deleting anything; the assembly-line panel renders whenever there is no live run to show instead, and the author's original-prompt card renders only when the feature carries one. ([validated by labels the section 'Danger zone' without being told](apps/web-ui/src/components/DangerZone.test.tsx#L7), [`DangerZone.test.tsx:14`](apps/web-ui/src/components/DangerZone.test.tsx#L14), [`DangerZone.test.tsx:25`](apps/web-ui/src/components/DangerZone.test.tsx#L25), [`DangerZone.test.tsx:36`](apps/web-ui/src/components/DangerZone.test.tsx#L36), [`DangerZone.test.tsx:47`](apps/web-ui/src/components/DangerZone.test.tsx#L47))
- FR-1.7: *(added 2026-08-13)* "An action is in flight" and "the submit failed" each have ONE component. `SubmitButton` takes the pending boolean from the form when it can and from a `pending` prop when the caller owns it (`useTransition`, `useActionState`), which is what had produced seven hand-written `disabled={pending}` buttons in this vertical; a caller's own `disabled` now also disables, where the prop spread used to land after it and silently RE-ENABLE the button for the duration of a submit. `FormError` renders nothing for an absent, null or empty message and announces a real one with `role="alert"` — of the five hand-rolled error paragraphs only one was announced, so a failed submit was otherwise silent to a screen reader. ([validated by uses the pending prop when the caller drives its own transition](apps/web-ui/src/components/SubmitButton.test.tsx#L42), [`SubmitButton.test.tsx:52`](apps/web-ui/src/components/SubmitButton.test.tsx#L52), [`SubmitButton.test.tsx:58`](apps/web-ui/src/components/SubmitButton.test.tsx#L58), [`SubmitButton.test.tsx:64`](apps/web-ui/src/components/SubmitButton.test.tsx#L64), [`FormError.test.tsx:7`](apps/web-ui/src/components/FormError.test.tsx#L7), [`FormError.test.tsx:13`](apps/web-ui/src/components/FormError.test.tsx#L13), [`FormError.test.tsx:19`](apps/web-ui/src/components/FormError.test.tsx#L19), [`FormError.test.tsx:25`](apps/web-ui/src/components/FormError.test.tsx#L25), [`FormError.test.tsx:32`](apps/web-ui/src/components/FormError.test.tsx#L32))

### FR-2: Smart Feature Creation & Planning

- FR-2.11: *(added 2026-08-13)* Both planning pages show the assembly line that runs them, read FROM THE FLOOR rather than from a copy. The create page previews what "Start planning" sets in motion — so the author can see there is a human step (`author`) and a wait for the spec PR (`merged`) before anything is filed — and the detail page names the line the feature is on. Fetching rather than reading the generated builtin set is the requirement: a YAML edit must show up after a Floor deploy alone, with no web-ui rebuild. The definition is fetched server-side and passed down, so the view stays pure and an unreachable or unconfigured Floor costs the preview and not the form above it. The card supplies the section title, so the graph's own heading is suppressed. On the detail page the card is handed the feature's own line, so it draws the CURRENT STATE — every station named with its status, derived from the persisted walk rows alone since a server render has no event stream — and falls back to the declared graph only when no line resolves; while a station is actually working the wizard's live card owns the graph and this server-rendered twin stands down, so the page never shows two graphs disagreeing about the same run. ([`run-walk-data.test.ts:35`](apps/web-ui/src/lib/run-walk-data.test.ts#L35), [`run-walk-data.test.ts:46`](apps/web-ui/src/lib/run-walk-data.test.ts#L46), [`run-walk-data.test.ts:59`](apps/web-ui/src/lib/run-walk-data.test.ts#L59), [`run-walk-data.test.ts:67`](apps/web-ui/src/lib/run-walk-data.test.ts#L67)[`assembly-lines.test.ts:37`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L37), [`assembly-lines.test.ts:44`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L44), [`assembly-lines.test.ts:53`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L53), [`assembly-lines.test.ts:59`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L59), [`assembly-lines.test.ts:64`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L64), [`assembly-lines.test.ts:69`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L69), [`assembly-lines.test.ts:74`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L74), [`assembly-lines.test.ts:79`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L79), [`assembly-lines.test.ts:86`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L86), [`assembly-lines.test.ts:95`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L95), [`assembly-lines.test.ts:104`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L104))

### FR-3: Gap-Closing Result

- FR-3.2: The model output is fence-stripped and JSON-parsed (a malformed body raises a contextual error naming the truncated snippet) and then validated against the shared schema; an invalid result marks the iteration failed. Validation rejects a non-object root, a missing `draft_spec_markdown`, a choice question with no or empty `options`, and a `split_suggestion` missing `proposed_features`. ([validated by `model-json.test.ts:5`](libs/shared/src/work/feature-planning/model-json.test.ts#L5), [`model-json.test.ts:9`](libs/shared/src/work/feature-planning/model-json.test.ts#L9), [`model-json.test.ts:13`](libs/shared/src/work/feature-planning/model-json.test.ts#L13), [`model-json.test.ts:19`](libs/shared/src/work/feature-planning/model-json.test.ts#L19), [`model-json.test.ts:23`](libs/shared/src/work/feature-planning/model-json.test.ts#L23), [`model-json.test.ts:27`](libs/shared/src/work/feature-planning/model-json.test.ts#L27), [`model-json.test.ts:33`](libs/shared/src/work/feature-planning/model-json.test.ts#L33))

### FR-4: Per-Section Feedback & Iteration

- FR-4.7 *(added 2026-08-14)*: The wizard is the worker of a NAMED station, not a screen the run happens to be watched from. The round's pause is a `feature_review` human station whose `route` names this page, so the run graph can link a reader straight to the form that unblocks it, and the interface stops keeping its own map of which node id means which phase — a map that silently omitted every node nobody remembered to add to it. The station's three outcomes are the three things the author can do: accept, refine, abandon. ([validated by the author abandoning fails the run](apps/floor/src/work/assembly-run/feature-planning-acceptance.test.ts#L80))

### FR-7: Persistence & Project Port

- FR-7.3: The web UI reads features directly (read-only) and routes lifecycle/task-spawning writes through the mcp-server API. ([`client.test.ts:62`](apps/web-ui/src/lib/api/client.test.ts#L62))

### FR-10: Planning Reliability & Recovery


### FR-11: Spec Decomposition (planning → implementation handoff)

- FR-11.1: When a feature's spec PR merges, decomposition runs as the TAIL OF THE SAME LINE rather than as a new task. The line parks on a `merged` wait node after `push`, and merging is that node's outcome — reported through exactly the mechanism the author's accept already uses. This replaced a kick that minted a `feature-decompose` task only for a `feature-finalize` task: once finalize became a resume of the planning line the owning task became `feature-planning`, the predicate stopped matching, and no feature planned on the merged line was ever decomposed. A PR may carry other open lines (a code-review line has no `merged` node), so only a line actually parked on that node is a target, and a node that already reported is skipped so a re-delivered merge cannot advance the walk twice. ([validated by targets the merged node a pushed line is parked on](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L21), [`decompose-resume.test.ts:32`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L32), [`decompose-resume.test.ts:62`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L62), [`decompose-resume.test.ts:73`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L73), [`decompose-resume.test.ts:84`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L84), [`decompose-resume.test.ts:125`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L125), [`decompose-resume.test.ts:139`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L139), [`decompose-resume.test.ts:150`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L150), [`decompose-resume.test.ts:175`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L175); implemented by [`decompose-resume.ts:1`](libs/shared/src/outbound/project/assembly-runs/decompose-resume.ts#L1), [`merge-check.ts:331`](apps/stations/src/work/merge-check/merge-check.ts#L331))
- FR-11.2: The decomposition agent reads the merged `specs/<slug>/spec.md` and returns a schema-validated `DecompositionResult`: an ordered list of user stories, each with a summary, acceptance criteria, and its implementable tasks (id, description, dependencies, phase, parallelizable, file hint). An invalid result fails the task — the root must be an object, `stories` an array (empty is allowed), and each story must carry a title; the parser defaults a story's missing summary/criteria/tasks, coerces a single-string `acceptance_criteria` to a list, normalizes tasks given as bare strings (minting sequential ids) or with `text`/single-string `depends_on` drift, and rejects a task with no description. ([validated by parseDecomposition accepts a valid stories payload](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L38), [`decomposition-result.test.ts:42`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L42), [`decomposition-result.test.ts:48`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L48), [`decomposition-result.test.ts:55`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L55), [`decomposition-result.test.ts:59`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L59), [`decomposition-result.test.ts:65`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L65), [`decomposition-result.test.ts:78`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L78), [`decomposition-result.test.ts:86`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L86), [`decomposition-result.test.ts:109`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L109), [`decomposition-result.test.ts:129`](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L129), [impl](libs/shared/src/domain/feature-planning/decomposition-result.ts))
- FR-11.5: The decomposition prompt and model resolve from the `feature-decompose` agent definition (editable org default, project override); its shipped default is the `feature-decompose.md` body, which documents the output contract (`stories`/`acceptance_criteria`/`depends_on`/`tasks`) and frames the job as turning the settled spec into user-story work rather than re-planning it, alongside a `DECOMPOSITION_EXAMPLE` that parses cleanly and shows a cross-task dependency. ([documents the output contract — stories, acceptance criteria, and task dependencies](libs/shared/src/domain/feature-planning/decomposition-instructions.test.ts#L18), [frames decomposition as turning the spec into work, not re-planning it](libs/shared/src/domain/feature-planning/decomposition-instructions.test.ts#L29), [parses cleanly through parseDecomposition](libs/shared/src/domain/feature-planning/decomposition-instructions.test.ts#L36), [shows a cross-task dependency so the model learns to wire phases](libs/shared/src/domain/feature-planning/decomposition-instructions.test.ts#L43), [impl](libs/shared/src/domain/feature-planning/decomposition-instructions.ts))
- FR-11.6: *(rewritten 2026-08-12)* Decomposition runs as an ASSEMBLY LINE — `decompose` (agent) → `issues` (station) → done — not in-process. The agent judges (which stories, which tasks, which labels) and the station only writes what the artifact says, which is the same split every other line uses and retires the last in-process execution path. The decomposition crosses between them as a declared artifact (FR6.17). `feature-decompose` therefore joins planning and finalize as a feature-lifecycle type: it runs its own line regardless of dark-factory, and it opens no per-TASK Issue, because the line files Issues itself — one per user story. Both behaviours are one named decision, since decompose was missing from both when its handler was retired. ([validated by isFeatureLifecycleType covers planning, finalize and decompose](apps/floor/src/work/task/worker.test.ts#L241), [`worker.test.ts:247`](apps/floor/src/work/task/worker.test.ts#L247))
- FR-11.8: *(added 2026-08-12)* The agent chooses labels from the repo's REAL label list (it runs `gh label list` in its own pod) and the station VALIDATES them before filing. GitHub's create-issue silently CREATES a label it does not know, so an invented `area:frontend` would not fail — it would permanently join the taxonomy. An unknown label sends the decomposition back as `changes_requested` naming every unknown label at once, so one correction fixes them all; nothing is filed in the meantime, because a half-filed decomposition cannot be re-run cleanly. A story that breaks into no tasks, and a decomposition with no stories, are rejected the same way — a story nobody can start is not an implementation plan. A repo with NO labels is not an error: the work still files under `lore-managed`/`user-story`. A missing artifact is a wiring failure, not a bad decomposition, so it FAILS rather than asking the agent to fix something it did nothing wrong about. ([validated by decideIssueWork requests changes naming a label the repo does not have](libs/shared/src/work/feature-planning/issue-work.test.ts#L59), [`issue-work.test.ts:74`](libs/shared/src/work/feature-planning/issue-work.test.ts#L74), [`issue-work.test.ts:98`](libs/shared/src/work/feature-planning/issue-work.test.ts#L98), [`issue-work.test.ts:107`](libs/shared/src/work/feature-planning/issue-work.test.ts#L107), [`issue-work.test.ts:120`](libs/shared/src/work/feature-planning/issue-work.test.ts#L120), [`issue-work.test.ts:39`](libs/shared/src/work/feature-planning/issue-work.test.ts#L39), [`issue-work.test.ts:51`](libs/shared/src/work/feature-planning/issue-work.test.ts#L51), [`issues.test.ts:65`](apps/stations/src/work/issues/issues.test.ts#L65), [`issues.test.ts:87`](apps/stations/src/work/issues/issues.test.ts#L87), [`issues.test.ts:104`](apps/stations/src/work/issues/issues.test.ts#L104), [`issues.test.ts:123`](apps/stations/src/work/issues/issues.test.ts#L123), [`issues.test.ts:136`](apps/stations/src/work/issues/issues.test.ts#L136))

### FR-14: Result Delivery

- FR-14.3: A failed ATTEMPT is not a failed ROUND. An attempt that produces no usable artifact — the subsystem's `missing`/`too-large`/`unreadable`/`upload-failed`, or a plan.md lore-api cannot read — is reported and logged, but the round stays `running`: the line parks on `author`, where its people see the failure and ask again, rather than a self-retry deciding for them. The line's terminal settlement is the SINGLE owner of "this round failed", so the two cannot disagree. Recording it here showed the author "Planning round N failed" beside a Retry button while a retry was already in flight — and a third attempt is what that button would have started. ()

### FR-15: Conversation Continuity

- FR-15.1: A node declares what it continues with `continues: {node, key}`. `node` names the work; `key` names the THREAD — `line`, `task`, or `args.<name>` — which is what stops two features running the same definition concurrently from continuing each other, a cross-feature leak rather than merely a wrong answer. Both halves fail at LOAD, because an unresolvable reference would silently start a fresh conversation, and that is indistinguishable from one that continued and remembered nothing. The key is deliberately domain-free: the engine never learns what a feature is, and planning keys on `args.feature_id` exactly as detect lines carry `args.job_run_id`. ([validated by parseAssemblyLine accepts a node reference keyed by an arg](libs/assembly-lines/src/loader.test.ts#L978), [`loader.test.ts:966`](libs/assembly-lines/src/loader.test.ts#L989), [`loader.test.ts:979`](libs/assembly-lines/src/loader.test.ts#L1002), [`loader.test.ts:987`](libs/assembly-lines/src/loader.test.ts#L1010), [`loader.test.ts:995`](libs/assembly-lines/src/loader.test.ts#L1018))
- FR-15.2: Conversations are indexed by `(kind, value, node)` and never by anything feature-shaped, so one thread's history is unreachable from another's and each node in a thread keeps its own. A run is never offered its own conversation. ([validated by ConversationsPort offers the newest saved conversation for a thread](libs/shared/src/outbound/project/conversations/conversations.test.ts#L16), [`conversations.test.ts:12`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L12), [`conversations.test.ts:37`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L37), [`conversations.test.ts:48`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L48), [`conversations.test.ts:58`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L58), [`conversations.test.ts:73`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L73))
- FR-15.3: A conversation reserved at dispatch but never uploaded is never offered to a later run — resuming it would send a pod after an object that does not exist, and that fetch fails silently by design, so it would present as a fresh start rather than an error. *(amended 2026-08-21)* This is also why a relaunch reserves a FRESH pin rather than needing an idempotent reserve: a crash between the row insert and the launch leaves exactly such an orphan, which nothing will ever be offered. ([validated by ConversationsPort skips a reserved conversation whose run never uploaded](libs/shared/src/outbound/project/conversations/conversations.test.ts#L27), [`conversations.test.ts:67`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L67), )
- FR-15.4: The archive moves as BYTES end to end. A transcript is gzip, and the Floor's utf-8 body helper replaces every sequence above 0x7F with U+FFFD — corruption that would surface only later, as a tar that will not extract inside a pod. ([validated by GET /api/agent-conversations returns the archive bytes unchanged](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L100), [`agent-conversations.test.ts:38`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L38))
- FR-15.5: The registry never fails a run. An unreserved id is still stored rather than lost over a bookkeeping mismatch, a missing bucket answers 202 rather than erroring, and an absent or unreadable archive answers 404 so the pod's best-effort restore degrades to a fresh conversation. Both routes require the internal token. ([validated by POST /api/agent-conversations still accepts an archive for an id nobody reserved](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L62), [`agent-conversations.test.ts:74`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L74), [`agent-conversations.test.ts:88`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L88), [`agent-conversations.test.ts:124`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L124), [`agent-conversations.test.ts:140`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L140), [`agent-conversations.test.ts:150`](apps/floor/src/transport/http/routes/agent-conversations.test.ts#L150))
- FR-15.6: A thread key resolves against what the RUN carries, so the engine stays domain-free: `line` names this execution, `task` the task behind it, and `args.<name>` whatever value the caller put under that name. A key the run cannot satisfy is an ERROR, never an empty thread — falling back to "no conversation" would start fresh forever, and that is indistinguishable from continuity that remembered nothing. ([validated by keys a thread on the run for `line`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L11), [`conversation-thread.test.ts:18`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L18), [`conversation-thread.test.ts:25`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L25), [`conversation-thread.test.ts:32`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L32), [`conversation-thread.test.ts:40`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L40), [`conversation-thread.test.ts:49`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L49), [`conversation-thread.test.ts:55`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L55), [`resolve-conversation.test.ts:118`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L118))
- FR-15.7: A RETRY never continues. When the walk revisits a node through an `iteration_max` back-edge it re-runs the same work with the same prompt and no inherited state — a retry exists because the last attempt failed, and inheriting that attempt's context would make the rerun path-dependent rather than reproducible. ([validated by continues on a node\u2019s first execution](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L63), [`conversation-thread.test.ts:63`](apps/floor/src/work/assembly-run/conversation-thread.test.ts#L63), [`resolve-conversation.test.ts:104`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L104))
- FR-15.8: Each run FORKS: it resumes the thread's newest archived conversation and saves under a NEW id reserved before dispatch. Forking is what makes rewind possible at all — round 2's transcript still exists after rounds 3 and 4 ran, so continuing from it is just resuming a different id; resume-in-place would overwrite the very thing rewind needs. The id is reserved in advance because the pod is TOLD what to save as rather than reporting back. A node that declares no `continues` wires nothing. ([validated by continues the thread\u2019s newest conversation and saves as a new id](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L33), [`resolve-conversation.test.ts:58`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L58), [`resolve-conversation.test.ts:71`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L71), [`resolve-conversation.test.ts:93`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L93), [`resolve-conversation.test.ts:133`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L133))
- FR-15.9: The resolved conversation rides the PER-TASK recipe clone, exactly as the repo token does — which previous run to resume and which id to save as are per-run facts the shared catalog recipe cannot carry. The Floor threads the feature id into the line's args at dispatch, which is what lets planning's `args.feature_id` key resolve without the engine ever learning what a feature is. ([validated by wires resources.conversation onto the per-task clone when the run continues one](libs/shared/src/outbound/cluster/per-task-token.test.ts#L117), [`assembly-run-station-backend.test.ts:111`](apps/floor/src/work/assembly-run/assembly-run-station-backend.test.ts#L111), [`assembly-run-station-backend.test.ts:101`](apps/floor/src/work/assembly-run/assembly-run-station-backend.test.ts#L101))
- FR-15.11: Which form a round sends is decided at DISPATCH, from the resolved conversation's id alone — that id is empty for a first round, a retry, and a thread whose only prior run never uploaded its state, and every one of those needs the full composition. No second flag tracks it, because a flag set where the prompt is composed (lore-api) could not know what the Floor would later resolve. Both forms therefore ride the line's args, and a blank feedback turn falls back rather than dispatching an empty prompt. ([validated by sends only the new feedback when the run resumed a conversation](apps/floor/src/work/assembly-run/round-content.test.ts#L12), [`round-content.test.ts:24`](apps/floor/src/work/assembly-run/round-content.test.ts#L24), [`round-content.test.ts:36`](apps/floor/src/work/assembly-run/round-content.test.ts#L36), [`round-content.test.ts:48`](apps/floor/src/work/assembly-run/round-content.test.ts#L48), [`round-content.test.ts:57`](apps/floor/src/work/assembly-run/round-content.test.ts#L57), [`assembly-run-station-backend.test.ts:53`](apps/floor/src/work/assembly-run/assembly-run-station-backend.test.ts#L53), [`assembly-run-station-backend.test.ts:44`](apps/floor/src/work/assembly-run/assembly-run-station-backend.test.ts#L44))
- FR-15.14: The round content the dispatch chooses becomes the CR's **description**, not only its prompt. The recipe the pod runs renders `{description}`; `spec.prompt` is not what reaches the agent, so setting only the prompt handed every resumed round the full draft again — the exact re-briefing this feature exists to end, while every other signal (recipe field, reserved id, archive on disk) said continuity was working. The two fields are set from one value, because them disagreeing about what a run is working from is a bug in either direction. ([validated by dispatches the resumed round's feedback as the CR's description, not just its prompt](apps/floor/src/work/assembly-run/advance-line.test.ts#L295), [`advance-line.test.ts:324`](apps/floor/src/work/assembly-run/advance-line.test.ts#L326))
- FR-15.13: Every run option reaches the Station spec. The runner rebuilds that spec field by field, so an option it forgets is dropped in SILENCE — `featureId` and `roundFeedback` were both added and both lost that way, and the symptom was a run that dispatched perfectly and simply continued no conversation, which is indistinguishable from continuity that remembered nothing. *(amended 2026-08-21, #1466)* Third victim, and the reason the walk now has exactly ONE launch-spec builder: the assembly-run reaper's relaunch arm rebuilt the spec from the raw task description, dropping both `resources.conversation` and the round content — and because every launch RE-PROVISIONS the per-task recipe clone, that relaunch did not merely dispatch a thinner pod, it replaced a live pod's recipe with one that had no conversation. Both dispatch doors call `nodeLaunchSpec`, so a field added there reaches both or neither. ([validated by cluster mode forwards every run option to the Station spec](libs/shared/src/outbound/project/agents/agent-runner.test.ts#L64), )
- FR-15.12: The archive has a single-machine backend so continuity can be exercised off a cluster. A bucket wins where one is configured; `LORE_ARCHIVE_DIR` is opt-in rather than a default, because a deployment that lost its bucket config would otherwise write conversations to pod-local disk that vanishes with the pod — which reads as continuity right up until it isn't. Bytes round-trip unchanged (a gzip archive through a utf-8 hop loses every byte above 0x7F), a missing object reads null rather than throwing so the pod's best-effort restore still degrades to a fresh conversation, and a key that would escape the root is refused — conversation ids arrive over HTTP, so traversal is not something to trust upstream. ([validated by round-trips gzip bytes unchanged](libs/shared/src/outbound/project/archive/archive-file.test.ts#L18), [`archive-file.test.ts:31`](libs/shared/src/outbound/project/archive/archive-file.test.ts#L31), [`archive-file.test.ts:41`](libs/shared/src/outbound/project/archive/archive-file.test.ts#L41), [`archive-file.test.ts:49`](libs/shared/src/outbound/project/archive/archive-file.test.ts#L49), [`archive-file.test.ts:56`](libs/shared/src/outbound/project/archive/archive-file.test.ts#L56), [`archive-file.test.ts:66`](libs/shared/src/outbound/project/archive/archive-file.test.ts#L66))

### FR-16: Rewind

- FR-16.3: A round is addressed by the task it ran as — what the caller already holds — and its conversation by the assembly line that reserved it. An explicit choice resolving to nothing starts FRESH rather than falling through to the newest round, because quietly resuming round 4 when the author asked for round 2 is indistinguishable from a rewind that worked. A retried round ran more than one line; the NEWEST is the attempt whose state the author actually saw, and since `listForTask` orders `created_at DESC` that attempt is its FIRST element — reading the last one resumed the discarded first attempt. ([validated by resumes the round the author chose, not the newest one](libs/shared/src/outbound/project/conversations/conversations.test.ts#L90), [`conversations.test.ts:113`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L113), [`conversations.test.ts:135`](libs/shared/src/outbound/project/conversations/conversations.test.ts#L135), [`resolve-conversation.test.ts:171`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L171), [`resolve-conversation.test.ts:197`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L197), [`resolve-conversation.test.ts:222`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L222), [`resolve-conversation.test.ts:235`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L235), [`resolve-conversation.test.ts:367`](apps/floor/src/work/assembly-run/resolve-conversation.test.ts#L367))
- FR-11.9: *(added 2026-08-13)* A spec-task the station files MUST be indistinguishable from one the `tasks.md` sync files. It carries `spec_task_id` — the name every other producer and reader of a spec-task uses (`lore_list_pipeline_tasks`, the decomposition view) — and, when the line carries one, `feature_id`. Spreading the agent's artifact instead published the agent's own vocabulary (`id`) and no feature link at all, so the detail page's `context_bundle->>'feature_id'` filter matched zero rows for every feature ever decomposed, and merge-check's spec-status flip, which reads the same key, never fired. `feature_id` is OMITTED rather than null when the line has none: the filter is a JSON text match, so a literal null would match nothing while looking set. ([validated by stamps the feature a spec-task belongs to](apps/stations/src/work/issues/issues.test.ts#L144), [`issues.test.ts:165`](apps/stations/src/work/issues/issues.test.ts#L169), [`issues.test.ts:182`](apps/stations/src/work/issues/issues.test.ts#L186); implemented by [`issues.ts:101`](apps/stations/src/work/issues/issues.ts#L101))
- FR-11.10: *(added 2026-08-13)* A feature's spec-tasks are readable through the Project facade, not by querying `pipeline.tasks` from a page. `specTasksForFeature` keys on `context_bundle->>'feature_id'` — the key the issues station stamps (FR-11.9) — scopes to the repo, and orders by `spec_task_id` so the story tree renders in the agent's intended order rather than insertion order. The in-memory double is the behavioural spec of the Pg adapter: same filter, same ordering, same empty result for a feature that was never decomposed. (; implemented by [`task-store-port.ts:100`](libs/shared/src/outbound/project/tasks/task-store-port.ts#L100))

### FR-12: Round Observability

- FR-12.6a: *(added 2026-08-14)* A denial says WHY, in the server log. The gate answers one flat "Access denied" to the caller — correct, since telling an outsider which of three things went wrong is itself a disclosure — but it discarded GitHub's status entirely, so the operator had nothing either. The three reasons need different actions: **404** usually means the OAuth app has no access to the org rather than that the repo is missing, **401** means the session's token is stale or revoked (sign out and back in mints a new one), **403** means rate-limited or blocked by the org's app policy; a transport failure is named as such rather than reported as a refusal. The token never appears in the log, and a granted request logs nothing at all. ([validated by names the repo and GitHub's status when access is refused](apps/web-ui/src/lib/user-repo-access.test.ts#L44), [`user-repo-access.test.ts:54`](apps/web-ui/src/lib/user-repo-access.test.ts#L54), [`user-repo-access.test.ts:64`](apps/web-ui/src/lib/user-repo-access.test.ts#L64), [`user-repo-access.test.ts:76`](apps/web-ui/src/lib/user-repo-access.test.ts#L76), [`user-repo-access.test.ts:84`](apps/web-ui/src/lib/user-repo-access.test.ts#L84); implemented by [`user-repo-access.ts:54`](apps/web-ui/src/lib/user-repo-access.ts#L54))
- FR-12.8: *(added 2026-08-13)* While a node runs, the card reports the two numbers that say whether to keep waiting: elapsed against THE DEADLINE THE NODE IS ACTUALLY KILLED AT, and the tokens spent so far. The budget comes from the definition's node — its `timeout_minutes` else the reaper's own default, plus the reaper's grace — because the number shown before was the feature-planning agent definition's timeout, which nothing enforces and which is one number for a line whose nodes have separate budgets: the spec phase counted against the planning round's clock. A feature that resolves no line keeps the round's budget, having no definition to read. A `wait` node has no budget at all, since it is parked on a person. The token count is summed from `pipeline.agent_run_turns` rather than `pipeline.llm_calls`: a cost row lands only when an agent run ENDS, which for a planning round is the moment this card disappears, so the authoritative table can never answer "so far". Cached prompt tokens count toward the total — they are billed, and hiding them would understate a cached run by an order of magnitude — and a run that has reported no usage shows no counter rather than a zero that reads as "nothing is happening". (; the budget itself [`node-budget.test.ts:16`](apps/web-ui/src/lib/node-budget.test.ts#L16), [`node-budget.test.ts:25`](apps/web-ui/src/lib/node-budget.test.ts#L25), [`node-budget.test.ts:34`](apps/web-ui/src/lib/node-budget.test.ts#L34), [`node-budget.test.ts:44`](apps/web-ui/src/lib/node-budget.test.ts#L44), [`node-budget.test.ts:53`](apps/web-ui/src/lib/node-budget.test.ts#L53), [`node-budget.test.ts:57`](apps/web-ui/src/lib/node-budget.test.ts#L57), [`node-budget.test.ts:66`](apps/web-ui/src/lib/node-budget.test.ts#L66); the usage total [`run-tokens.test.ts:13`](apps/web-ui/src/lib/run-tokens.test.ts#L13), [`run-tokens.test.ts:17`](apps/web-ui/src/lib/run-tokens.test.ts#L17), [`run-tokens.test.ts:26`](apps/web-ui/src/lib/run-tokens.test.ts#L26), [`run-tokens.test.ts:39`](apps/web-ui/src/lib/run-tokens.test.ts#L39), [`run-tokens.test.ts:49`](apps/web-ui/src/lib/run-tokens.test.ts#L49), [`run-tokens.test.ts:53`](apps/web-ui/src/lib/run-tokens.test.ts#L53), [`run-tokens.test.ts:57`](apps/web-ui/src/lib/run-tokens.test.ts#L57), [`run-tokens.test.ts:61`](apps/web-ui/src/lib/run-tokens.test.ts#L61); implemented by [`node-budget.ts:1`](apps/web-ui/src/lib/node-budget.ts#L1), [`run-tokens.ts:1`](apps/web-ui/src/lib/run-tokens.ts#L1))
- FR-12.11: *(added 2026-08-19)* Finalize refuses to start a SECOND run for a feature something is already working, and the refusal names it: `409` carrying the in-flight run id under both spellings. `canFinalize` gates on `feature.status`, which does not move until a spec PR lands roughly eighteen minutes later, so every press inside that window minted another task, another run, another branch and another spec PR — four presses in ninety-two seconds produced three duplicate spec PRs, two of them writing different content to the same spec path. The id rides the refusal because a second press means "show me what is happening", not "you broke something", so the caller can draw the live run instead of an error. *(restated 2026-08-22: the guard is structural)* With the legacy path retired there is nothing left to guard against by counting runs — the parked node IS the permission. Reporting an outcome to the author node un-parks it, so the press that follows an accept finds nothing to report to and is refused, and the refusal still carries the run id because a duplicate press means "show me". One guard replaces two: the `findOpenBySubject` count and refine's iteration-scoped `roundInFlight` were overlapping answers to the same question, and two guards in one handler is a precedence question nobody should have to answer. Beneath both, the store's unique index is the atomic backstop for two presses that race the read, and a task whose start JOINED an existing run is settled rather than left running (specs/6-dark-factory FR6.49–FR6.50). ([`assembly-run-station-backend.test.ts:140`](apps/floor/src/work/assembly-run/assembly-run-station-backend.test.ts#L140); implemented by `feature-round-routes.ts:runAlreadyWorking`)

### FR-13: Failure Visibility

- FR-13.1: A planning round is finished only when its GapResult actually landed. A line that ends without one — whatever its outcome — marks the iteration `failed` and restores the feature per the same rule the synchronous path uses, instead of leaving the round `running` and the wizard spinning forever. A round that did post its result is left untouched. ([impl](apps/floor/src/work/assembly-run/settle-task.ts))
- FR-13.4: A per-task GitHub token that mints empty MUST fail at the mint, naming the repo and the App variables to check. An empty token writes a present-but-useless Secret key, so the pod starts and then dies in its init container on `git clone` with GitHub's deliberately uninformative "Repository not found" — a cause visible only in pod logs. ([validated by GithubTokenMinter throws naming the repo and the App vars when the token comes back empty](apps/cluster-agent/src/outbound/kube-token-provisioner.test.ts#L19), [`kube-token-provisioner.test.ts:11`](apps/cluster-agent/src/outbound/kube-token-provisioner.test.ts#L11), [impl](apps/cluster-agent/src/outbound/kube-token-provisioner.ts))

### Scenario 6: A merged spec decomposes into stories + tasks

- Decomposition runs on spec-PR merge, by resuming the line's parked `merged` node rather than minting a task. ([validated by reports success to the parked merged node of the PR's line](libs/shared/src/outbound/project/assembly-runs/decompose-resume.test.ts#L125), [validated by the merge resume walks the parked line into decompose](apps/floor/src/work/assembly-run/feature-planning-acceptance.test.ts#L117))
- The agent's `DecompositionResult` validates against the shared schema; an invalid result fails the round. ([validated by parseDecomposition accepts a valid stories payload](libs/shared/src/domain/feature-planning/decomposition-result.test.ts#L38))
