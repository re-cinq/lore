# Feature Specification: Smart Feature Planning

| Field          | Value                                       |
|----------------|---------------------------------------------|
| Feature        | Smart Feature Planning                      |
| Branch         | 7-feature-planning                          |
| Status         | In Progress                             |
| Created        | 2026-06-17                                  |
| Owner          | Platform Engineering                        |

Smart Feature Planning turns the fire-and-forget feature-request flow into an interactive, section-by-section drafting experience backed by a first-class draft Feature entity, letting a product owner shape a spec through gap-closing analysis and per-section direction before any PR is opened.

## Problem Statement

Lore can already turn a one-line intent into a spec PR through the
`feature-request` task type, but it is fire-and-forget: a single LLM pass
commits `spec.md`/`data-model.md`/`tasks.md` and opens a PR with no human in
the loop and nowhere for a half-formed idea to live. A product owner cannot
shape the spec interactively, cannot see the gaps the model identified, and
cannot steer the architecture before the PR exists. There is also no
first-class, persistent **Feature** entity: features exist only implicitly as
`specs/<n>-<name>/` folders, computed into the spec-trace graph after the fact.
A draft feature — one being thought through but not yet committed — has no home.

The result: the spec authoring that should be the most collaborative,
context-aware step in the pipeline is the least interactive one.

## Vision

A repo page has a **Features** tab listing every feature — drafts in progress
and shipped — as a browsable list with a detail view. A **"+ Feature"** button
opens a **smart feature page**: the author writes a prompt describing what they
want. On submit, the platform assembles the *whole feature timeline plus project*
context and an agent produces a **gap-closing analysis** as an **adaptive set of
sections** — an **Overview** first, then whatever the feature actually needs (data
model, API, migration, integration, edge cases…), each carrying inline generated
mockups and **per-section follow-up questions** where they help, plus an
invitation for free-form direction. The author reviews each section, leaves
**per-section comments and direction**, and either refines (another round) or
**finalizes**.

Finalize writes `specs/<slug>/spec.md`, commits it to a branch, and opens a
**PR** (never a direct commit to `main`) — plus a user-story GitHub Issue when
the repo's dark-factory `create_issue` policy calls for one. If the planner
judges the feature too large, it suggests **splitting** it into smaller focused
features, and the author can spin up a child draft **without leaving the page**.

The persistent Feature node is wired into the existing graph: on the Graph tab
it **replaces** the computed folder node, carrying its lifecycle status.

## Integration & Relationships

This feature is not an island — it sits on top of the existing pipeline, graph,
and spec machinery and feeds the rest of the system.

- **Task-to-Agent Pipeline (`graveyard/specs/3-task-agent-pipeline`).** `feature-planning`
  and `feature-finalize` are two new task types on the existing pipeline. *(As designed,
  planning ran in-process and finalize on the LoreTask CRD path; since the ADR-031 cutover
  both run their own assembly-line definitions on the Station backend — see FR-2.2 for the
  current routing.)* No new orchestration runtime; it reuses task creation, the pod
  lifecycle, and PR creation.
- **Feature-request task type (`specs/3`).** This feature is the interactive,
  multi-round successor to one-shot `feature-request`: same end artifact (a
  `spec.md` PR following repo conventions), but with a human in the loop, a
  persistent draft, and gap-closing analysis. `feature-request` remains for the
  fully-automated path: its one-shot handler commits the three spec artifacts
  (`spec.md`/`data-model.md`/`tasks.md`) and only then opens the PR, and throws
  without opening a PR when the model SKIPs every artifact. ([validated by commits the three spec artifacts then opens the PR](apps/floor/src/jobs/task/handle-feature-request.test.ts#L78), [throws when every artifact is SKIP and opens no PR](apps/floor/src/jobs/task/handle-feature-request.test.ts#L106))
- **Dark Factory (`specs/6-dark-factory`, ADR-016).** Finalize reuses
  `dark_factory.create_issue` and the `decideIssueCreate` helper for the
  conditional user-story Issue, the `Lore-Task` PR-body trailer, and the
  per-repo trust level (`TRUST_LEVELS`) that gates who may run planning.
- **Spec-traceability graph (`specs/spec-traceability-graph`) & coverage
  (`specs/spec-test-coverage`).** The finalized `specs/<slug>/spec.md` is ingested
  like any spec: it becomes a Dgraph Feature/Spec/Statement subtree, and its
  acceptance criteria can carry inline `([validated by])` links. The persistent
  Feature node merges onto (and replaces) the computed folder node in the spec
  graph — so planning *closes the loop*, producing the spec that implementation
  tasks, spec-coverage, and drift detection then operate on.
- **Context assembly (`specs/context-assembly`, ADR-021/022).** The planning
  round composes the whole-feature timeline into its prompt; repo context comes
  from the pod's own `lore_assemble_context` call, the same way every other agent
  node gets it (dispatch-time hydration was removed 2026-08-28).
- **Project facade & ports (ADR-024).** Feature lifecycle is a new `features`
  port on the Project object, alongside `tasks`, `pulls`, `audit`, etc. Execution
  honors the BYO container image (ADR-025) like any Station.
- **Downstream features.** A feature can spawn child features (split), forming a
  parent/child tree in `lore.features` and `splits_into` relationships; each child
  is itself a normal feature that finalizes to its own spec PR.

## User Personas

### Feature Author (Product Owner / PM)

Describes intent in plain language via the smart feature page. Iterates on the
returned gap analysis, steering each section. Wants the end result to be a spec
that stands as a user story in the repo. Does not write code.

### Developer

Browses the Features tab to understand what is planned and shipped. Reviews the
finalized spec PR alongside the code. May start a feature draft from a technical
prompt and split it into implementable pieces.

### Platform Engineer

Configures whether finalize creates a user-story Issue (dark-factory
`create_issue`), monitors planning runs via the pipeline/audit views,
and owns the trust level that gates who may run planning in a repo.

## User Scenarios & Acceptance Criteria

### Scenario 1: Draft from a prompt → planning round → gap analysis

**Actor:** Feature Author

**Flow:**
1. Author opens the **Features** tab, clicks **+ Feature**, writes a prompt.
2. A draft `lore.features` row is created and a `feature-planning` task is kicked.
3. The planning agent runs a single LLM→JSON round against the assembled
   whole-feature-timeline plus project context and produces a structured gap analysis.
4. The result is persisted and the page renders the gap as an adaptive list of
   sections (an Overview first, then feature-specific sections with inline mockups,
   per-section follow-up questions, and a free-form prompt).

**Acceptance Criteria:**
- The planning round produces a `GapResult` (an adaptive `sections[]` list) that validates against the shared schema. ([validated by parseGapResult valid sections payload](libs/shared/src/feature-planning/gap-result.test.ts#L64))
- Planning makes no commits and opens no PR (the result is persisted, not committed).
- The planning round runs only after the whole-feature timeline is composed into the prompt ahead of the agent call. ([validated by composePlanningPrompt renders the prior timeline](libs/shared/src/feature-planning/planning-prompt.test.ts#L65))
- Generated mockups are sanitized so no embedded script, event handler, or external reference can execute when rendered. *(Re-amended 2026-08-18: a mermaid-RENDERED svg is framed WITHOUT a DOMPurify pass — DOMPurify's mXSS defense strips foreignObject INTERIORS under every configuration, and mermaid v11 puts ER labels and flowchart edge labels there, so purifying mermaid output structurally guarantees an unlabeled skeleton; the first amendment's allow-the-tag approach kept empty shells. Mermaid output's boundaries are mermaid's own `securityLevel:"strict"` render, a script-tag tripwire, and the no-script sandboxed frame. Author-supplied RAW svg/html still passes DOMPurify, with `foreignObject` forbidden for raw svg.)* A mockup is sanitized only where there is a DOM: this is a client component that still renders on the SERVER, where DOMPurify's default export is a factory with no `sanitize` on it, and calling it during render took the whole feature page down as soon as a plan carried a mockup. ([validated by sanitizeSvg strips script](libs/shared/src/feature-planning/gap-result.test.ts#L217), [`mockup-frame.test.ts:99`](apps/web-ui/src/lib/mockup-frame.test.ts#L99), [`mockup-frame.test.ts:105`](apps/web-ui/src/lib/mockup-frame.test.ts#L105), [`mockup-frame.test.ts:115`](apps/web-ui/src/lib/mockup-frame.test.ts#L115), [`mockup-frame.test.ts:154`](apps/web-ui/src/lib/mockup-frame.test.ts#L154), [`mockup-frame.test.ts:169`](apps/web-ui/src/lib/mockup-frame.test.ts#L169))

### Scenario 2: Iterative refinement with per-section direction

**Actor:** Feature Author

**Flow:**
1. The author reads each gap section.
2. Per section, the author leaves a comment and picks a direction (keep / refine / redirect).
3. The author optionally answers follow-up questions inline and adds free-form input.
4. **Refine again** spawns a new immediate planning round whose context includes
   the prior rounds' results and the author's per-section feedback.

**Acceptance Criteria:**
- Each gap section exposes its own comment and direction control.
- Per-section feedback, question answers, and free-form input persist as the round's `user_answers` and round-trip through storage. ([validated by appendIteration round-trips user_answers](libs/shared/src/project/features/features-pg.test.ts#L68))
- A refinement round's context includes the full prior feature timeline (each prior section's content plus the author's per-section comments and answers). ([validated by composePlanningPrompt renders prior sections + comments](libs/shared/src/feature-planning/planning-prompt.test.ts#L65))
- The feature status reflects whether the round needs author input or is ready to finalize: `awaiting-input` when any section carries questions or a split is suggested, `spec-ready` when neither. ([validated by decideFeatureStatus awaiting-input vs spec-ready](libs/shared/src/feature-planning/gap-result.test.ts#L399), [`gap-result.test.ts:279`](libs/shared/src/feature-planning/gap-result.test.ts#L403), [`gap-result.test.ts:292`](libs/shared/src/feature-planning/gap-result.test.ts#L416))

### Scenario 3: Finalize → spec PR (+ conditional user-story Issue)

**Actor:** Feature Author

*(amended 2026-08-22: the vocabulary)* Nothing here "finalizes". The author's accept is a
station outcome that lets the SAME line walk on to the spec work, and a human still reviews
the spec PR that comes out — so the endpoint is `POST .../features/{id}/create-spec-file`
(the old `/finalize` path is served alongside it until the UI deploys), the recipe that
writes the spec is `spec-write` (pairing with `spec-analysis`), and the separate
`feature-finalize` task type and its assembly line are retired.

**Flow:**
1. When satisfied, the author clicks **Create the spec PR**.
2. The `spec-write` Station writes `specs/<slug>/spec.md` from the accumulated draft, commits, and pushes a branch.
3. The watcher opens a spec PR; if the repo's dark-factory `create_issue` policy applies, it also opens a user-story Issue cross-linked via the `Lore-Task` trailer.
4. The page shows the PR link and live PR status.

**Acceptance Criteria:**
- Accepting is allowed only from a settled planning state (`spec-ready`/`awaiting-input`) and runs as a Station that opens a PR on a branch, never committing to `main`. ([validated by handleFeaturesRoute refuses finalize unless settled](apps/lore-api/src/api/routes/features/features.test.ts#L131))
- A user-story Issue is created if and only if the repo's `create_issue` policy calls for it.
- The feature transitions to `pr-open` and stores the PR (and Issue) references. ([validated by handleFeaturesRoute kicks finalize from spec-ready](apps/lore-api/src/api/routes/features/features.test.ts#L148))
- The committed `specs/<slug>/spec.md` contains the feature overview and the macro decisions made during planning.

### Scenario 4: Split an oversized feature

**Actor:** Feature Author or Developer

**Flow:**
1. The planner judges the feature too large and returns a split suggestion with proposed sub-features.
2. The page renders the suggestion with a **Create draft** control per proposed sub-feature.
3. The author creates one or more child drafts without navigating away.

**Acceptance Criteria:**
- A split suggestion renders proposed sub-features the author can act on individually.
- Creating a child draft inserts a feature row linked to its parent and does not navigate away from the page. ([validated by createSplitChild links parent](libs/shared/src/project/features/features-pg.test.ts#L191))
- The child draft is immediately plannable from its proposed scope. ([validated by handleFeaturesRoute creates a split child from the suggestion](apps/lore-api/src/api/routes/features/features.test.ts#L330))

### Scenario 5: Features tab + graph integration

**Actor:** Developer

**Flow:**
1. The developer opens the **Features** tab and browses drafts and shipped features with status badges.
2. Opening a feature shows its draft spec, iteration history, gap sections, and PR status when finalized.
3. On the **Graph** tab, the persistent Feature node represents the feature, colored by status.

**Acceptance Criteria:**
- The Features list shows drafts and shipped features with a lifecycle status badge. ([validated by statusBadge maps status](apps/web-ui/src/app/repos/[owner]/[repo]/features/feature-status.test.ts#L10))
- When a persistent feature exists, the Graph tab uses it in place of the computed folder node, joined by feature path. ([validated by mergePersistentFeatures enriches by path](libs/shared/src/spec-trace/merge-persistent-features.test.ts#L23))
- A draft feature with no spec yet appears as an injected node so it is visible before any spec exists. ([validated by mergePersistentFeatures injects draft](libs/shared/src/spec-trace/merge-persistent-features.test.ts#L41))
- A persistent Feature node is rendered with a color derived from its lifecycle status.

## Functional Requirements

### FR-1: Features Tab, List & Detail

The system MUST surface features as a first-class browsable entity per repo.

- FR-1.1: A **Features** tab is registered on the repo page between Specs and ADRs.
- FR-1.2: The list shows drafts and shipped features for the repo, newest first, each with a status badge and a link to detail.
- FR-1.3: The detail view shows the current draft spec, the per-round iteration history, the latest gap sections, and — once finalized — the PR status and Issue link, plus a deep-link into the Graph tab.
- The feature page's long documents FOLD: the accumulated draft spec is collapsed by default and the author's prompt starts open, because the point of folding is that the spec had taken the page over while the prompt is what a reader came for. Built on native `<details>`, so a closed card keeps its content in the DOM — find-in-page and screen readers still reach a collapsed spec, which rendering the children conditionally would break — and the browser owns the keyboard and ARIA behaviour. ([validated by shows its title as the control that opens it](apps/web-ui/src/components/CollapsibleCard.test.tsx#L9), [`CollapsibleCard.test.tsx:19`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L19), [`CollapsibleCard.test.tsx:29`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L29), [`CollapsibleCard.test.tsx:39`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L39), [`CollapsibleCard.test.tsx:51`](apps/web-ui/src/components/CollapsibleCard.test.tsx#L51))

- FR-1.4: The list degrades to an empty state when the `lore.features` table is absent (pre-migration safety) rather than erroring.
- FR-1.5: The feature GET endpoint returns 404 for an unknown feature id. ([validated by handleFeaturesRoute returns 404 for a missing feature on GET](apps/lore-api/src/api/routes/features/features.test.ts#L357))

- FR-1.6: *(added 2026-08-13)* An irreversible action on the detail view is framed by a shared `DangerZone`: what it is, what it costs, and the caller's own controls. The view composed `"spec-card danger-zone"` by hand and depended on three globals.css selectors; both now live in the component. The controls stay `children` because confirmation flows differ — a two-step confirm here, a typed name elsewhere — and baking one in would make the second caller fight it. ([validated by labels the section 'Danger zone' without being told](apps/web-ui/src/components/DangerZone.test.tsx#L7), [`DangerZone.test.tsx:14`](apps/web-ui/src/components/DangerZone.test.tsx#L14), [`DangerZone.test.tsx:25`](apps/web-ui/src/components/DangerZone.test.tsx#L25), [`DangerZone.test.tsx:36`](apps/web-ui/src/components/DangerZone.test.tsx#L36), [`DangerZone.test.tsx:47`](apps/web-ui/src/components/DangerZone.test.tsx#L47))
- FR-1.7: *(added 2026-08-13)* "An action is in flight" and "the submit failed" each have ONE component. `SubmitButton` takes the pending boolean from the form when it can and from a `pending` prop when the caller owns it (`useTransition`, `useActionState`), which is what had produced seven hand-written `disabled={pending}` buttons in this vertical; a caller's own `disabled` now also disables, where the prop spread used to land after it and silently RE-ENABLE the button for the duration of a submit. `FormError` renders nothing for an absent, null or empty message and announces a real one with `role="alert"` — of the five hand-rolled error paragraphs only one was announced, so a failed submit was otherwise silent to a screen reader. ([validated by uses the pending prop when the caller drives its own transition](apps/web-ui/src/components/SubmitButton.test.tsx#L42), [`SubmitButton.test.tsx:55`](apps/web-ui/src/components/SubmitButton.test.tsx#L55), [`SubmitButton.test.tsx:61`](apps/web-ui/src/components/SubmitButton.test.tsx#L61), [`SubmitButton.test.tsx:69`](apps/web-ui/src/components/SubmitButton.test.tsx#L69), [`FormError.test.tsx:7`](apps/web-ui/src/components/FormError.test.tsx#L7), [`FormError.test.tsx:13`](apps/web-ui/src/components/FormError.test.tsx#L13), [`FormError.test.tsx:20`](apps/web-ui/src/components/FormError.test.tsx#L20), [`FormError.test.tsx:26`](apps/web-ui/src/components/FormError.test.tsx#L26), [`FormError.test.tsx:35`](apps/web-ui/src/components/FormError.test.tsx#L35))
### FR-2: Smart Feature Creation & Planning

The system MUST analyze a feature prompt in the context of the whole project.

- FR-2.1: **+ Feature** opens a page where the author submits a free-text prompt; submit creates a draft feature row and kicks a `feature-planning` task. ([validated by handleFeaturesRoute creates a draft and kicks round 1](apps/lore-api/src/api/routes/features/features.test.ts#L102))

The submitted title and prompt are trimmed and required, capped at 256 and 8000 characters respectively, and rejected with a `ValidationError` otherwise — the route echoes this as a `400 { error: "title and prompt are required" }` before touching the project. ([validated by `feature-input.test.ts:9`](libs/shared/src/feature-planning/feature-input.test.ts#L9), [`feature-input.test.ts:18`](libs/shared/src/feature-planning/feature-input.test.ts#L18), [`feature-input.test.ts:24`](libs/shared/src/feature-planning/feature-input.test.ts#L24), [`feature-input.test.ts:30`](libs/shared/src/feature-planning/feature-input.test.ts#L30), [`feature-input.test.ts:36`](libs/shared/src/feature-planning/feature-input.test.ts#L36), [handleFeaturesRoute rejects a blank-title create with 400](apps/lore-api/src/api/routes/features/features.test.ts#L122))
- FR-2.2: *(restated late 2026-07)* Both task types run their own assembly line on the Station backend regardless of dark-factory enablement — the worker always forwards the definition name for them, so `feature-planning` runs its single `analyze` agent node (no commit or PR — the pod posts the GapResult back to the features API) and `feature-finalize` runs `write → push` (one Agent CR per node); `feature-finalize`'s original in-process handler survives behind the explicit `LORE_STATION_BACKEND=inprocess` escape hatch for a dev machine without Docker or credentials. `feature-planning` has **no** in-process arm: a second execution path meant a second prompt, and the two drifted until the agent was asked for a `GapResult` it was never shown ([impl](apps/floor/src/jobs/task/worker.ts))
- FR-2.3: The planning round receives the feature id and iteration; its prompt carries the full feature timeline (prior rounds' results + per-section answers), and repo context is assembled by the pod itself. Round one wraps the title and user prompt and omits the draft spec; later rounds render each prior section's content, the author's per-section comment, and each follow-up question with its asked text and answer (or an unanswered marker). ([validated by composePlanningPrompt renders the prior timeline](libs/shared/src/feature-planning/planning-prompt.test.ts#L65), [`planning-prompt.test.ts:21`](libs/shared/src/feature-planning/planning-prompt.test.ts#L52), [`planning-prompt.test.ts:59`](libs/shared/src/feature-planning/planning-prompt.test.ts#L90), [`planning-prompt.test.ts:71`](libs/shared/src/feature-planning/planning-prompt.test.ts#L102))
- FR-2.4: The planning prompt and model resolve from the `feature-planning` agent definition — an editable org default, overridable per project. The definition's prompt is the `task-types.yaml` template verbatim, with no code-side override: the override that used to sit here served a constant the pod never ran, so the editable row and the delivered recipe disagreed. ([validated by serves the feature-planning prompt straight from the yaml template](libs/shared/src/project/agents/agent-defs-yaml.test.ts#L83))
- FR-2.5: The planning round bills org credentials (`ANTHROPIC_API_KEY`) by default; a developer's local Claude subscription is used only with explicit opt-in (`LORE_STATION_ALLOW_PERSONAL_AUTH`), never silently.
- FR-2.6: **`scripts/task-types.yaml` holds the whole planning prompt** and is its only copy — gen-catalog renders it verbatim into the seeded `AgentDefinition` recipe the pod runs, as a literal block scalar so an indented JSON schema survives the round trip. It defines the contract the agent is measured on: a two-phase orient-then-deliver working order, `result.json` as the entire deliverable, an Overview-first non-empty `sections` list, a `## Integration` focus naming real repository paths, no task-sizing / user-story / ordering questions, and the `{description}`/`{context}` placeholders the dispatcher fills — the latter with a fixed instruction to assemble context, not with context. The worked example it embeds is itself parsed against the `GapResult` schema, so an example demonstrating a shape the parser rejects cannot ship. ([validated by embeds an example that parses cleanly against the GapResult schema](libs/shared/src/feature-planning/planning-prompt.test.ts#L115), [`planning-prompt.test.ts:120`](libs/shared/src/feature-planning/planning-prompt.test.ts#L123), [`planning-prompt.test.ts:127`](libs/shared/src/feature-planning/planning-prompt.test.ts#L130), [`planning-prompt.test.ts:136`](libs/shared/src/feature-planning/planning-prompt.test.ts#L157), [`planning-prompt.test.ts:143`](libs/shared/src/feature-planning/planning-prompt.test.ts#L168))
- FR-2.7: A mockup declares its `format` — `mermaid` for node graphs, `html` for UI, `svg` for anything spatial — and an unrecognized one falls back to `svg` only when the markup is visibly an SVG document, otherwise the mockup is DROPPED: mermaid source rendered as HTML and an HTML fragment rendered as an SVG are both nonsense on screen, so guessing is worse than omitting. An `html` mockup carries the pixel `height` it needs, because its frame is sandboxed without same-origin access and cannot measure itself. ([validated by keeps a mermaid mockup's source and marks its format](libs/shared/src/feature-planning/gap-result.test.ts#L251), [`gap-result.test.ts:271`](libs/shared/src/feature-planning/gap-result.test.ts#L275), [`gap-result.test.ts:309`](libs/shared/src/feature-planning/gap-result.test.ts#L313), [`gap-result.test.ts:295`](libs/shared/src/feature-planning/gap-result.test.ts#L299))
- FR-2.10: `mockup_stylesheet` MUST BE SELF-CONTAINED. The frame loads none of the planned repo's stylesheets, so a custom property the agent only REFERENCES is undefined there — `var(--text)` with no definition makes the whole declaration invalid, leaving the mockup as unstyled black text on a blank page, which over a dark dashboard is simply invisible. The prompt therefore requires a `:root` block declaring every token used, and the worked example demonstrates it: an example that references undefined tokens teaches the agent to produce them, which is precisely what round 14 of feature 275343d1 did. The frame also paints an explicit ground and text colour rather than inheriting, so an incomplete stylesheet degrades to a legible wireframe instead of nothing at all. ([validated by embeds a self-contained example stylesheet, defining every token it uses](libs/shared/src/feature-planning/planning-prompt.test.ts#L139), [`mockup-frame.test.ts:47`](apps/web-ui/src/lib/mockup-frame.test.ts#L47))
- FR-2.8: Mockup styling comes from the **planned** repository, not from Lore: the result carries one `mockup_stylesheet` the agent lifted while orienting, shared by every mockup, and absent for a repo with no styles to speak of. Sanitisation is per format — an SVG sanitizer over mermaid source would corrupt a diagram to remove nothing — and the stylesheet loses `@import` and `url()`, which the mockup frame could not fetch anyway. ([validated by carries the result-level mockup stylesheet](libs/shared/src/feature-planning/gap-result.test.ts#L327), [`gap-result.test.ts:335`](libs/shared/src/feature-planning/gap-result.test.ts#L339), [`gap-result.test.ts:358`](libs/shared/src/feature-planning/gap-result.test.ts#L362))
- FR-2.9: Every mockup renders inside its OWN sandboxed frame — `sandbox=""`, so no scripting, no same-origin, no parent access. That is what makes repo-supplied styling safe at all: a `<style>` block inside an inline SVG applies document-wide, so the previous inline rendering would have let a mockup restyle the wizard around it. The frame layers a minimal reset under the planned repo's stylesheet so the repo's own rules win, and injects none of the dashboard's theme tokens — a repo with no styles to lend gets a neutral system default and reads as a wireframe rather than borrowing an identity it does not have. Because the frame cannot measure itself, an `html` mockup's height is the declared one, defaulted when absent and capped so one mockup cannot own the page; a rendered mermaid diagram's frame takes its height from the svg's own viewBox instead (same cap) — the 420px default clipped a tall flowchart to its top third. ([validated by puts the planned repo's stylesheet after the reset so the repo wins](apps/web-ui/src/lib/mockup-frame.test.ts#L17), [`mockup-frame.test.ts:28`](apps/web-ui/src/lib/mockup-frame.test.ts#L28), [`mockup-frame.test.ts:34`](apps/web-ui/src/lib/mockup-frame.test.ts#L34), [`mockup-frame.test.ts:43`](apps/web-ui/src/lib/mockup-frame.test.ts#L43), [`mockup-frame.test.ts:47`](apps/web-ui/src/lib/mockup-frame.test.ts#L47), [`mockup-frame.test.ts:51`](apps/web-ui/src/lib/mockup-frame.test.ts#L63), [`mockup-frame.test.ts:57`](apps/web-ui/src/lib/mockup-frame.test.ts#L69), [`mockup-frame.test.ts:69`](apps/web-ui/src/lib/mockup-frame.test.ts#L77), [`mockup-frame.test.ts:71`](apps/web-ui/src/lib/mockup-frame.test.ts#L83), [`mockup-frame.test.ts:131`](apps/web-ui/src/lib/mockup-frame.test.ts#L131), [`mockup-frame.test.ts:137`](apps/web-ui/src/lib/mockup-frame.test.ts#L137), [`mockup-frame.test.ts:141`](apps/web-ui/src/lib/mockup-frame.test.ts#L141))
- FR-2.11: *(added 2026-08-13)* Both planning pages show the assembly line that runs them, read FROM THE FLOOR rather than from a copy. The create page previews what "Start planning" sets in motion — so the author can see there is a human step (`author`) and a wait for the spec PR (`merged`) before anything is filed — and the detail page names the line the feature is on. Fetching rather than reading the generated builtin set is the requirement: a YAML edit must show up after a Floor deploy alone, with no web-ui rebuild. The definition is fetched server-side and passed down, so the view stays pure and an unreachable or unconfigured Floor costs the preview and not the form above it. The card supplies the section title, so the graph's own heading is suppressed. On the detail page the card is handed the feature's own line, so it draws the CURRENT STATE — every station named with its status, derived from the persisted walk rows alone since a server render has no event stream — and falls back to the declared graph only when no line resolves; while a station is actually working the wizard's live card owns the graph and this server-rendered twin stands down, so the page never shows two graphs disagreeing about the same run. ([validated by draws the declared graph when there is no run yet](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L38), [`FeatureAssemblyLine.test.tsx:98`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L102), [`FeatureAssemblyLine.test.tsx:106`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L110), [`FeatureAssemblyLine.test.tsx:112`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L116), [`run-walk-data.test.ts:35`](apps/web-ui/src/lib/run-walk-data.test.ts#L35), [`run-walk-data.test.ts:46`](apps/web-ui/src/lib/run-walk-data.test.ts#L46), [`run-walk-data.test.ts:59`](apps/web-ui/src/lib/run-walk-data.test.ts#L59), [`run-walk-data.test.ts:65`](apps/web-ui/src/lib/run-walk-data.test.ts#L65), [`FeatureAssemblyLine.test.tsx:43`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L47), [`FeatureAssemblyLine.test.tsx:50`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L54), [`FeatureAssemblyLine.test.tsx:55`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L59), [`FeatureAssemblyLine.test.tsx:62`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L66), [`FeatureAssemblyLine.test.tsx:74`](apps/web-ui/src/components/FeatureAssemblyLine.test.tsx#L78), [`assembly-lines.test.ts:37`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L37), [`assembly-lines.test.ts:44`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L44), [`assembly-lines.test.ts:53`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L53), [`assembly-lines.test.ts:59`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L59), [`assembly-lines.test.ts:64`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L64), [`assembly-lines.test.ts:69`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L69), [`assembly-lines.test.ts:74`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L74), [`assembly-lines.test.ts:80`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L80), [`assembly-lines.test.ts:89`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L89), [`assembly-lines.test.ts:101`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L100), [`assembly-lines.test.ts:110`](apps/web-ui/src/lib/api/assembly-lines.test.ts#L109))
### FR-3: Gap-Closing Result

The planning agent MUST return a structured, schema-validated gap analysis.

- FR-3.1: The `GapResult` contract is an ordered, **adaptive `sections[]`** list — the agent names the sections the feature needs (Overview first, then e.g. data model / API / migration / integration / edge cases), each carrying optional prose `content`, self-contained SVG `mockups`, and per-section follow-up `questions` — plus an optional split suggestion and the accumulated draft spec markdown. The parser tolerates model drift: an absent split suggestion, a question given via `text` (minting the missing `id`/`why`), a mockup given as a bare SVG string, an explicit empty sections list, and a legacy `architecture`/`user_flows` payload folded into named sections. ([validated by parseGapResult valid sections payload](libs/shared/src/feature-planning/gap-result.test.ts#L64), [`gap-result.test.ts:90`](libs/shared/src/feature-planning/gap-result.test.ts#L90), [`gap-result.test.ts:96`](libs/shared/src/feature-planning/gap-result.test.ts#L96), [`gap-result.test.ts:118`](libs/shared/src/feature-planning/gap-result.test.ts#L118), [`gap-result.test.ts:131`](libs/shared/src/feature-planning/gap-result.test.ts#L131), [`gap-result.test.ts:152`](libs/shared/src/feature-planning/gap-result.test.ts#L152))
- FR-3.2: The model output is fence-stripped and JSON-parsed (a malformed body raises a contextual error naming the truncated snippet) and then validated against the shared schema; an invalid result marks the iteration failed. Validation rejects a non-object root, a missing `draft_spec_markdown`, a choice question with no or empty `options`, and a `split_suggestion` missing `proposed_features`. ([validated by `model-json.test.ts:5`](libs/shared/src/feature-planning/model-json.test.ts#L5), [`model-json.test.ts:9`](libs/shared/src/feature-planning/model-json.test.ts#L9), [`model-json.test.ts:13`](libs/shared/src/feature-planning/model-json.test.ts#L13), [`model-json.test.ts:19`](libs/shared/src/feature-planning/model-json.test.ts#L19), [`model-json.test.ts:23`](libs/shared/src/feature-planning/model-json.test.ts#L23), [`model-json.test.ts:27`](libs/shared/src/feature-planning/model-json.test.ts#L27), [`model-json.test.ts:33`](libs/shared/src/feature-planning/model-json.test.ts#L33), [`gap-result.test.ts:68`](libs/shared/src/feature-planning/gap-result.test.ts#L68), [`gap-result.test.ts:74`](libs/shared/src/feature-planning/gap-result.test.ts#L74), [`gap-result.test.ts:159`](libs/shared/src/feature-planning/gap-result.test.ts#L159), [`gap-result.test.ts:146`](libs/shared/src/feature-planning/gap-result.test.ts#L146), [`gap-result.test.ts:181`](libs/shared/src/feature-planning/gap-result.test.ts#L181))
- FR-3.3: Mockup markup is sanitized (no script, event handlers, foreignObject, or external references) before persistence and again client-side via DOMPurify when rendered inline (no iframe); `sanitizeSvg` strips inline event-handler attributes, `javascript:` hrefs, and `foreignObject` elements while leaving a clean SVG (including a fragment `href`) unchanged. ([validated by sanitizeGapResult sanitizes mockup markup across every section](libs/shared/src/feature-planning/gap-result.test.ts#L375), [`gap-result.test.ts:223`](libs/shared/src/feature-planning/gap-result.test.ts#L223), [`gap-result.test.ts:229`](libs/shared/src/feature-planning/gap-result.test.ts#L229), [`gap-result.test.ts:235`](libs/shared/src/feature-planning/gap-result.test.ts#L235), [`gap-result.test.ts:243`](libs/shared/src/feature-planning/gap-result.test.ts#L243))
- FR-3.4: The UI renderer is presence-driven (renders whatever fields a section carries) and resilient to missing or unknown sections; `sectionsOf` returns the sections of a new-shape result, derives them from a raw legacy-shape object, and returns `[]` for null or unrecognized input. ([validated by `gap-result.test.ts:195`](libs/shared/src/feature-planning/gap-result.test.ts#L195), [`gap-result.test.ts:202`](libs/shared/src/feature-planning/gap-result.test.ts#L202), [`gap-result.test.ts:210`](libs/shared/src/feature-planning/gap-result.test.ts#L210))

### FR-4: Per-Section Feedback & Iteration

The system MUST let the author steer each section and refine across rounds.

- FR-4.1: Each gap section exposes a comment field and a direction control (keep / refine / redirect). The answers parser keeps a known direction with its comment, drops an unknown direction, and returns null for a non-object or wholly-empty payload. ([validated by `feature-input.test.ts:56`](libs/shared/src/feature-planning/feature-input.test.ts#L56), [`feature-input.test.ts:44`](libs/shared/src/feature-planning/feature-input.test.ts#L44), [`feature-input.test.ts:50`](libs/shared/src/feature-planning/feature-input.test.ts#L50))
- FR-4.2: Follow-up questions are answerable inline; a free-form input captures anything else. The parser keeps only string question answers and coerces free-form text, filling empty section and question maps. ([validated by `feature-input.test.ts:70`](libs/shared/src/feature-planning/feature-input.test.ts#L70), [`feature-input.test.ts:78`](libs/shared/src/feature-planning/feature-input.test.ts#L78))
- FR-4.3: **Refine again** spawns a new immediate `feature-planning` task carrying the per-section feedback; the UI polls the round and renders the new result. ([validated by posts user_answers to the feature's iterations path](apps/web-ui/src/lib/api/features.test.ts#L54), [`features.test.ts:63`](apps/web-ui/src/lib/api/features.test.ts#L63), [`features.test.ts:69`](apps/web-ui/src/lib/api/features.test.ts#L69), [`features.test.ts:75`](apps/web-ui/src/lib/api/features.test.ts#L75))
- FR-4.4: Iteration rounds are stored one row per round, keyed by `(feature_id, iteration)`, each linked to the planning task that produced it. ([validated by appendIteration inserts a running row at the minted iteration](libs/shared/src/project/features/features-pg.test.ts#L68))
- FR-4.5: A new planning round is rejected while one is already in flight for the same feature (a stale page or double-click must not spawn a second round). ([validated by handleFeaturesRoute rejects a concurrent planning round with 409](apps/lore-api/src/api/routes/features/features.test.ts#L394))
- FR-4.6: The in-memory features double is the behavioral spec of the Pg adapter over `lore.features` + `lore.feature_iterations`: `create` inserts a draft with slug/path derived from the title (creator defaulting to `ui`) and `createSplitChild` links the child to its parent; `get` returns iterations oldest-first and null for the wrong repo; `list` filters by repo and status newest-updated first; `appendIteration` mints the next counter, flips the feature to `planning`, inserts a `running` round, and throws for a missing feature (the Pg adapter's unguarded RETURNING deref); iteration writes are repo-scoped through the owning feature so a wrong repo writes nothing; `transitionStatus` applies only the defined `FeaturePatch` fields; and `delete` cascades the iterations, returning false when absent. ([validated by `features-memory.test.ts:14`](libs/shared/src/project/features/features-memory.test.ts#L14), [`features-memory.test.ts:34`](libs/shared/src/project/features/features-memory.test.ts#L34), [`features-memory.test.ts:51`](libs/shared/src/project/features/features-memory.test.ts#L51), [`features-memory.test.ts:63`](libs/shared/src/project/features/features-memory.test.ts#L63), [`features-memory.test.ts:81`](libs/shared/src/project/features/features-memory.test.ts#L81), [`features-memory.test.ts:102`](libs/shared/src/project/features/features-memory.test.ts#L102), [`features-memory.test.ts:112`](libs/shared/src/project/features/features-memory.test.ts#L112), [`features-memory.test.ts:140`](libs/shared/src/project/features/features-memory.test.ts#L140), [`features-memory.test.ts:161`](libs/shared/src/project/features/features-memory.test.ts#L161), [`features-memory.test.ts:174`](libs/shared/src/project/features/features-memory.test.ts#L174))

- FR-4.7 *(added 2026-08-14)*: The wizard is the worker of a NAMED station, not a screen the run happens to be watched from. The round's pause is a `feature_review` human station whose `route` names this page, so the run graph can link a reader straight to the form that unblocks it, and the interface stops keeping its own map of which node id means which phase — a map that silently omitted every node nobody remembered to add to it. The station's three outcomes are the three things the author can do: accept, refine, abandon. ([validated by the author abandoning fails the run](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L88))

### FR-5: Finalize Output

The system MUST produce a reviewable spec artifact, never a direct main commit.

- FR-5.1: Finalize runs a Station that writes `specs/<slug>/spec.md` from the draft, commits to a branch, and pushes. The slug comes from ONE slugger, capped per caller (60 for a feature directory, 30 for a task branch) — two copies with two caps produced two `specs/<slug>` directories for one title depending on which path created the feature, and the trailing-dash trim existed in only one of them, so the other could still name a directory `foo-bar-`. ([validated by handleFeaturesRoute kicks finalize from a spec-ready feature](apps/lore-api/src/api/routes/features/features.test.ts#L148), [`features-port.test.ts:124`](libs/shared/src/project/features/features-port.test.ts#L124), [`features-port.test.ts:132`](libs/shared/src/project/features/features-port.test.ts#L132), [`features-port.test.ts:137`](libs/shared/src/project/features/features-port.test.ts#L137))
- FR-5.2: The watcher opens the spec PR with the standard `Lore-Task` footer trailer.
- FR-5.3: A user-story Issue is created when, and only when, the repo's dark-factory `create_issue` policy resolves to create (reusing the existing decision helper).
- FR-5.4: The feature transitions to `pr-open` and records the PR number/URL, Issue number/URL, and `spec_path`.

### FR-6: Feature Splitting

The system MUST support decomposing an oversized feature in place.

- FR-6.1: When the agent returns a split suggestion, the UI renders the proposed sub-features.
- FR-6.2: Creating a child draft inserts a feature row with `parent_feature_id` set and seeds its prompt from the proposed scope, without navigating away. ([validated by createSplitChild inserts a child with parent_feature_id](libs/shared/src/project/features/features-pg.test.ts#L191))
- FR-6.3: The split endpoint refuses (409) when the latest ready planning round carries no split suggestion. ([validated by handleFeaturesRoute refuses split without a suggestion](apps/lore-api/src/api/routes/features/features.test.ts#L308))

### FR-7: Persistence & Project Port

The system MUST persist feature lifecycle through the Project facade.

- FR-7.1: Feature lifecycle and draft state live in `lore.features` and `lore.feature_iterations` (the `lore` schema, owned by the migration runner). ([validated by PgFeatures.create inserts into lore.features](libs/shared/src/project/features/features-pg.test.ts#L27))
- FR-7.2: Access is a `features` port on the Project facade (`project.features`): create, get, list, append iteration, set iteration result, transition status, create split child. ([validated by the Features facade stamps the bound repo on every call](libs/shared/src/project/features/features.test.ts#L35))
- FR-7.3: The web UI reads features directly (read-only) and routes lifecycle/task-spawning writes through the mcp-server API. ([validated by posts the title and prompt to the repo's features collection](apps/web-ui/src/lib/api/features.test.ts#L44), [`features.test.ts:83`](apps/web-ui/src/lib/api/features.test.ts#L83), [`features.test.ts:102`](apps/web-ui/src/lib/api/features.test.ts#L102), [`features.test.ts:113`](apps/web-ui/src/lib/api/features.test.ts#L113), [`client.test.ts:63`](apps/web-ui/src/lib/api/client.test.ts#L63), [`features.test.ts:123`](apps/web-ui/src/lib/api/features.test.ts#L123), [`features.test.ts:129`](apps/web-ui/src/lib/api/features.test.ts#L129), [`features.test.ts:135`](apps/web-ui/src/lib/api/features.test.ts#L135), [`features.test.ts:143`](apps/web-ui/src/lib/api/features.test.ts#L143))
- FR-7.4: The feature DELETE endpoint requires a `write`-scoped token — a read-scoped token is refused 403 before the project is touched — and returns `{ ok: true }` (200) when a row is removed or 404 when nothing matched. ([validated by handleFeaturesRoute returns 200 on delete and 404 when nothing was removed](apps/lore-api/src/api/routes/features/features.test.ts#L364), [`features.test.ts:292`](apps/lore-api/src/api/routes/features/features.test.ts#L377), [`features.test.ts:232`](apps/lore-api/src/api/routes/features/features.test.ts#L386))

- FR-7.5: *(added 2026-08-13)* The wizard's poll is ONE read, and it is not `GET /features/:id`. That route carries every round's `gap_result` — mockup markup plus a repo stylesheet each — which must not be re-sent every four seconds, so `/status` returns the feature row without its iterations, plus the newest round, the newest round that produced a result, and the id of the line the run graph hangs on. That line is resolved from the FIRST round's task, because from round two a resumed round mints no task of its own. `/decomposition` returns the spec-tasks the merged spec became — an empty list, not a 404, for a feature that was never decomposed: the feature exists, its tree does not yet. ([validated by returns the feature without every round's gap payload](apps/lore-api/src/api/routes/features/features.test.ts#L620), [`features.test.ts:608`](apps/lore-api/src/api/routes/features/features.test.ts#L631), [`features.test.ts:616`](apps/lore-api/src/api/routes/features/features.test.ts#L620), [`features.test.ts:634`](apps/lore-api/src/api/routes/features/features.test.ts#L679), [`features.test.ts:645`](apps/lore-api/src/api/routes/features/features.test.ts#L690), [`features.test.ts:652`](apps/lore-api/src/api/routes/features/features.test.ts#L697), [`features.test.ts:674`](apps/lore-api/src/api/routes/features/features.test.ts#L719), [`features.test.ts:688`](apps/lore-api/src/api/routes/features/features.test.ts#L733); implemented by [`features.ts:/status`](apps/lore-api/src/api/routes/features/features.ts#L277))
### FR-8: Graph Integration

The system MUST make the persistent Feature node the source of truth in the graph.

- FR-8.1: The `trace/graph` endpoint merges persistent features onto the computed spec-graph Feature nodes, joined by `(repo, path)`. ([validated by mergePersistentFeatures enriches a node sharing the path](libs/shared/src/spec-trace/merge-persistent-features.test.ts#L23))
- FR-8.2: A matching computed node is enriched with the persistent feature's id, status, and title (persistent wins). ([validated by mergePersistentFeatures enriches by path, persistent wins](libs/shared/src/spec-trace/merge-persistent-features.test.ts#L23))
- FR-8.3: A persistent draft with no spec yet is injected as a standalone Feature node. ([validated by mergePersistentFeatures injects a standalone draft node](libs/shared/src/spec-trace/merge-persistent-features.test.ts#L41))
- FR-8.4: Feature nodes are colored by lifecycle status in the D3 graph.

### FR-9: Migration Safety

The system MUST not break the UI deploy with its schema migration.

- FR-9.1: Migration `0017` creates its tables in the `lore` schema, is idempotent, and is single-transaction-safe (no concurrent index, no non-transactional DDL).
- FR-9.2: A local pre-flight applies the migration via the real apply path and proves an idempotent re-run before deploy.
- FR-9.3: A CI guard applies all migrations and re-applies the newest against an ephemeral Postgres, failing the build on any error.

### FR-10: Planning Reliability & Recovery

The system MUST self-heal planning rounds left stuck by a crash or restart.

- FR-10.1: A reaper job reconciles mid-planning features every minute: a round still `running` whose Station container/pod is gone is marked failed and the feature is restored to its last good analysis (or `draft`). The set of statuses the reaper treats as mid-planning (`draft`/`planning`/`awaiting-input`/`spec-ready`, and only those) is a single `isPlanningPhase` predicate. ([validated by decidePlanningRecovery orphans a round whose runtime is gone](libs/shared/src/project/features/planning-recovery.test.ts#L33), [`gap-result.test.ts:306`](libs/shared/src/feature-planning/gap-result.test.ts#L430), [`gap-result.test.ts:314`](libs/shared/src/feature-planning/gap-result.test.ts#L438))
- FR-10.2: Orphan detection probes the actual runtime (`StationBackend.isActive` — `docker ps` locally, the LoreTask CR on the cluster), so a dead round is recovered immediately rather than only after a timeout window; an age window is a fallback for a wedged-but-listed container.
- FR-10.3: A round that produced a `ready` result while the feature is still `planning` (a missed, non-atomic status transition) has its transition re-applied. ([validated by decidePlanningRecovery re-applies a missed transition](libs/shared/src/project/features/planning-recovery.test.ts#L86))

- FR-10.4: *(added 2026-08-11)* The runtime probe is not trusted to mean "dead" for the first two minutes of a round. A round is a task row, then an assembly line, then an Agent CR, then a pod — the CR does not exist for the first seconds, so a probe in that window says "not born yet", not "died". Round 10 was force-failed 32 seconds in and survived only because the delivered result overrode the reaper. Staleness still orphans regardless of the probe, so a wedged container that never exits is not protected by the grace window. ([validated by decidePlanningRecovery leaves a just-started round alone when no runtime is visible yet](libs/shared/src/project/features/planning-recovery.test.ts#L152), [`planning-recovery.test.ts:166`](libs/shared/src/project/features/planning-recovery.test.ts#L167), [`planning-recovery.test.ts:177`](libs/shared/src/project/features/planning-recovery.test.ts#L178), [`planning-recovery.test.ts:190`](libs/shared/src/project/features/planning-recovery.test.ts#L191); implemented by [`features-port.ts:198`](libs/shared/src/project/features/features-port.ts#L198))
- FR-10.5: *(added 2026-08-11)* A round that produced a result MUST never render as a blank page. A GapResult with an empty `sections[]` is structurally valid — `sanitizeGapResult` accepts it — and one round returned exactly that beside an 8 KB draft, so the wizard falls back to rendering `draft_spec_markdown`, and says plainly that the round produced nothing reviewable only when there is neither. ([validated by GapSections falls back to the draft spec when the round produced no sections](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/GapSections.test.tsx#L31), [`GapSections.test.tsx:21`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/GapSections.test.tsx#L21), [`GapSections.test.tsx:43`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/GapSections.test.tsx#L43), [`GapSections.test.tsx:53`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/GapSections.test.tsx#L53))
- FR-10.6: *(added 2026-08-11)* A completed round appears without a manual reload: the wizard polls its own state, but the draft renders from the server's copy of the feature, so the wizard refreshes the server render once per iteration when a round becomes ready. ([impl](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.tsx))
- FR-10.7: *(added 2026-08-18)* A round that runs on an assembly run has ONE liveness authority: the run. While the round's task has an OPEN assembly run, the reaper never orphans it — whatever a Kubernetes CR listing says, and however old the round is — because the assembly-run reaper owns that run's relaunches and timeouts, and a transiently empty CR listing during controller churn is indistinguishable from death to a point-in-time probe (on 2026-08-18 that probe executed a round whose agent had already succeeded). The direct runtime probe survives only for legacy rounds with no run row. ([validated by never orphans a running round whose assembly run is open, whatever the probe said](libs/shared/src/project/features/planning-recovery.test.ts#L211), [`planning-recovery.test.ts:226`](libs/shared/src/project/features/planning-recovery.test.ts#L226); implemented by [`features-port.ts:198`](libs/shared/src/project/features/features-port.ts#L198))
- FR-10.8: *(added 2026-08-18)* A lost round result is recovered from the run transcript, not re-run by a human. When a round is `failed` or `running` without a `gap_result` while its run's agent node SUCCEEDED, the reaper replays the artifact: the agent's last `Write` of the watch path (`result.json`) in `pipeline.agent_run_turns` holds the full GapResult, and it re-enters through the same `applyGapResult` the pod's own delivery uses — late delivery is that function's designed case. A transcript holding no artifact leaves the round failed: a genuinely failed analysis never wrote one. The failed-round sweep is bounded to rounds updated within a day, so ancient failures are not re-probed forever. ([validated by returns the artifact the agent wrote at the watch path, parsed](libs/shared/src/feature-planning/recover-gap-result.test.ts#L19), [`recover-gap-result.test.ts:34`](libs/shared/src/feature-planning/recover-gap-result.test.ts#L34), [`recover-gap-result.test.ts:46`](libs/shared/src/feature-planning/recover-gap-result.test.ts#L46), [`recover-gap-result.test.ts:63`](libs/shared/src/feature-planning/recover-gap-result.test.ts#L63), [`recover-gap-result.test.ts:72`](libs/shared/src/feature-planning/recover-gap-result.test.ts#L72), [`recover-gap-result.test.ts:105`](libs/shared/src/feature-planning/recover-gap-result.test.ts#L105); implemented by [`recover-gap-result.ts:34`](libs/shared/src/feature-planning/recover-gap-result.ts#L34), [`feature-planning-reaper.ts:218`](apps/floor/src/jobs/task/feature-planning-reaper.ts#L218))
- FR-10.9: *(added 2026-08-18)* Artifact recovery keys on the round's WORK row — the newest station run a pod actually executed; a human station runs no pod and writes no transcript. Recovery proceeds when that row SUCCEEDED, whatever the run's status: the earlier blanket exemption ("skip any `running` round on an OPEN run") hid exactly the incident shape it was built for, analyze succeeded and the walk parked on `author` with the round still reading `running` (#1302). It also proceeds when the work row is still OPEN on a CLOSED run — the pod may have written the artifact and had its node event eaten (#1298). It waits only while the work row itself is open on an open run, and recovers nothing when the round's own work FAILED, because the transcript's only artifact would be a previous round's. The transcript scan is scoped to the work row's Agent CR for the same reason: a multi-round run's transcript holds every earlier round's `result.json`, and an unscoped scan would replay one as the current round's result. ([validated by recovers with the analyze CR when the run is parked on the author and the artifact never landed](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L27), [`planning-artifact-recovery.test.ts:40`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L40), [`planning-artifact-recovery.test.ts:50`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L50), [`planning-artifact-recovery.test.ts:60`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L60), [`planning-artifact-recovery.test.ts:74`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L74), [`planning-artifact-recovery.test.ts:89`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L89), [`planning-artifact-recovery.test.ts:95`](apps/floor/src/jobs/task/planning-artifact-recovery.test.ts#L95); implemented by [`planning-artifact-recovery.ts:44`](apps/floor/src/jobs/task/planning-artifact-recovery.ts#L44))
### FR-11: Spec Decomposition (planning → implementation handoff)

The system MUST turn a merged feature spec into an implementable story/task tree
(ADR-029). Planning produces the spec; decomposition produces the work.

- FR-11.1: When a feature's spec PR merges, decomposition runs as the TAIL OF THE SAME LINE rather than as a new task. The line parks on a `merged` wait node after `push`, and merging is that node's outcome — reported through exactly the mechanism the author's accept already uses. This replaced a kick that minted a `feature-decompose` task only for a `feature-finalize` task: once finalize became a resume of the planning line the owning task became `feature-planning`, the predicate stopped matching, and no feature planned on the merged line was ever decomposed. A PR may carry other open lines (a code-review line has no `merged` node), so only a line actually parked on that node is a target, and a node that already reported is skipped so a re-delivered merge cannot advance the walk twice. ([validated by targets the merged node a pushed line is parked on](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L29), [`decompose-resume.test.ts:40`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L40), [`decompose-resume.test.ts:72`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L72), [`decompose-resume.test.ts:85`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L85), [`decompose-resume.test.ts:97`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L97), [`decompose-resume.test.ts:139`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L139), [`decompose-resume.test.ts:153`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L153), [`decompose-resume.test.ts:164`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L164), [`decompose-resume.test.ts:191`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L191); implemented by [`decompose-resume.ts:1`](libs/shared/src/project/assembly-runs/decompose-resume.ts#L1), [`merge-check.ts:331`](apps/stations/src/stations/merge-check/merge-check.ts#L331))
- FR-11.2: The decomposition agent reads the merged `specs/<slug>/spec.md` and returns a schema-validated `DecompositionResult`: an ordered list of user stories, each with a summary, acceptance criteria, and its implementable tasks (id, description, dependencies, phase, parallelizable, file hint). An invalid result fails the task — the root must be an object, `stories` an array (empty is allowed), and each story must carry a title; the parser defaults a story's missing summary/criteria/tasks, coerces a single-string `acceptance_criteria` to a list, normalizes tasks given as bare strings (minting sequential ids) or with `text`/single-string `depends_on` drift, and rejects a task with no description. ([validated by parseDecomposition accepts a valid stories payload](libs/shared/src/feature-planning/decomposition-result.test.ts#L38), [`decomposition-result.test.ts:42`](libs/shared/src/feature-planning/decomposition-result.test.ts#L42), [`decomposition-result.test.ts:48`](libs/shared/src/feature-planning/decomposition-result.test.ts#L48), [`decomposition-result.test.ts:55`](libs/shared/src/feature-planning/decomposition-result.test.ts#L55), [`decomposition-result.test.ts:59`](libs/shared/src/feature-planning/decomposition-result.test.ts#L59), [`decomposition-result.test.ts:65`](libs/shared/src/feature-planning/decomposition-result.test.ts#L65), [`decomposition-result.test.ts:78`](libs/shared/src/feature-planning/decomposition-result.test.ts#L78), [`decomposition-result.test.ts:86`](libs/shared/src/feature-planning/decomposition-result.test.ts#L86), [`decomposition-result.test.ts:109`](libs/shared/src/feature-planning/decomposition-result.test.ts#L109), [`decomposition-result.test.ts:129`](libs/shared/src/feature-planning/decomposition-result.test.ts#L129), [impl](libs/shared/src/feature-planning/decomposition-result.ts))
- FR-11.3: Each user story becomes a GitHub Issue (`User story: <title>`, labeled `lore-managed`/`user-story`, body linking the spec + feature), unless the repo's dark-factory `create_issue` policy resolves to never — then tasks-only. ([validated by storyIssueBody renders summary, acceptance criteria, tasks, and the spec link](libs/shared/src/feature-planning/decomposition-plan.test.ts#L77), [impl](apps/floor/src/jobs/task/handle-feature-decompose.ts))
- FR-11.4: Each task becomes a `spec-task` pipeline row carrying its story Issue number and `feature_id`, compatible with the existing implementation pipeline + trust gate (no new runner); in tasks-only mode (no Issue) the `story_issue` field is omitted while `feature_id` is still set. ([validated by specTaskRows links each task to its story issue and feature](libs/shared/src/feature-planning/decomposition-plan.test.ts#L32), [`decomposition-plan.test.ts:68`](libs/shared/src/feature-planning/decomposition-plan.test.ts#L68), [impl](libs/shared/src/feature-planning/decomposition-plan.ts))
- FR-11.5: The decomposition prompt and model resolve from the `feature-decompose` agent definition (editable org default, project override); the offline fallback is the shared `DECOMPOSITION_INSTRUCTIONS` constant, which documents the output contract (`stories`/`acceptance_criteria`/`depends_on`/`tasks`) and frames the job as turning the settled spec into user-story work rather than re-planning it, alongside a `DECOMPOSITION_EXAMPLE` that parses cleanly and shows a cross-task dependency. ([`decomposition-instructions.test.ts:9`](libs/shared/src/feature-planning/decomposition-instructions.test.ts#L9), [`decomposition-instructions.test.ts:20`](libs/shared/src/feature-planning/decomposition-instructions.test.ts#L20), [`decomposition-instructions.test.ts:27`](libs/shared/src/feature-planning/decomposition-instructions.test.ts#L27), [`decomposition-instructions.test.ts:34`](libs/shared/src/feature-planning/decomposition-instructions.test.ts#L34), [impl](libs/shared/src/feature-planning/decomposition-instructions.ts))
- FR-11.6: *(rewritten 2026-08-12)* Decomposition runs as an ASSEMBLY LINE — `decompose` (agent) → `issues` (station) → done — not in-process. The agent judges (which stories, which tasks, which labels) and the station only writes what the artifact says, which is the same split every other line uses and retires the last in-process execution path. The decomposition crosses between them as a declared artifact (FR6.17). `feature-decompose` therefore joins planning and finalize as a feature-lifecycle type: it runs its own line regardless of dark-factory, and it opens no per-TASK Issue, because the line files Issues itself — one per user story. Both behaviours are one named decision, since decompose was missing from both when its handler was retired. ([validated by isFeatureLifecycleType covers planning, finalize and decompose](apps/floor/src/jobs/task/worker.test.ts#L278), [`worker.test.ts:293`](apps/floor/src/jobs/task/worker.test.ts#L288))
- FR-11.8: *(added 2026-08-12)* The agent chooses labels from the repo's REAL label list (it runs `gh label list` in its own pod) and the station VALIDATES them before filing. GitHub's create-issue silently CREATES a label it does not know, so an invented `area:frontend` would not fail — it would permanently join the taxonomy. An unknown label sends the decomposition back as `changes_requested` naming every unknown label at once, so one correction fixes them all; nothing is filed in the meantime, because a half-filed decomposition cannot be re-run cleanly. A story that breaks into no tasks, and a decomposition with no stories, are rejected the same way — a story nobody can start is not an implementation plan. A repo with NO labels is not an error: the work still files under `lore-managed`/`user-story`. A missing artifact is a wiring failure, not a bad decomposition, so it FAILS rather than asking the agent to fix something it did nothing wrong about. ([validated by decideIssueWork requests changes naming a label the repo does not have](libs/shared/src/feature-planning/issue-work.test.ts#L59), [`issue-work.test.ts:77`](libs/shared/src/feature-planning/issue-work.test.ts#L77), [`issue-work.test.ts:101`](libs/shared/src/feature-planning/issue-work.test.ts#L101), [`issue-work.test.ts:110`](libs/shared/src/feature-planning/issue-work.test.ts#L110), [`issue-work.test.ts:124`](libs/shared/src/feature-planning/issue-work.test.ts#L124), [`issue-work.test.ts:39`](libs/shared/src/feature-planning/issue-work.test.ts#L39), [`issue-work.test.ts:51`](libs/shared/src/feature-planning/issue-work.test.ts#L51), [`issues.test.ts:66`](apps/stations/src/stations/issues/issues.test.ts#L66), [`issues.test.ts:88`](apps/stations/src/stations/issues/issues.test.ts#L88), [`issues.test.ts:105`](apps/stations/src/stations/issues/issues.test.ts#L105), [`issues.test.ts:124`](apps/stations/src/stations/issues/issues.test.ts#L124), [`issues.test.ts:139`](apps/stations/src/stations/issues/issues.test.ts#L139))
- FR-10.4: *(added 2026-08-12; RETIRED 2026-08-22)* When the spec analysis asks the AUTHOR a question rather than producing a change set, the finalize task FAILS carrying that objection. The line itself completes — a `changes_requested` visit is not a failed one — so without this the author presses the button, no PR appears, and nothing anywhere says why. The objection rides the line's args (FR6.17) and lands in the task's `failure_reason`, which is where the wizard already reads a diagnosis from, so it needs no new surface. Scoped to `feature-finalize`: a task of another type must never settle on a stray arg. The button says what it does — "Create the spec PR", naming the artifact the author reviews rather than the internal task type. **This mechanism is retired.** An analysis that questions the plan is now an EDGE back to the author node (specs/6-dark-factory FR6.26), so the line parks on a person instead of completing and needing a faked task failure to explain itself; `specAnalysisObjection` and the `feature-finalize` task type it was scoped to are both gone.
- FR-10.5: *(added 2026-08-12)* A round's feedback MUST survive a failure IN THE VIEW, not only in the row. It was already persisted before the pod was ever dispatched, so no pipeline failure could lose it — but nothing read it back, and the wizard clears the form on submit, so the author's words left the screen the moment the button was pressed and a failed round had no way to them. The failure block now shows what was submitted: each section's comment and direction, the answered questions, then the free-form note. A section marked `keep` with no comment is still input and survives with an empty body; whitespace is not input and is dropped; a first round, which reacts to nothing, shows nothing. ([validated by submittedFeedback recovers what the author typed for each section](apps/web-ui/src/lib/submitted-feedback.test.ts#L5), [`submitted-feedback.test.ts:28`](apps/web-ui/src/lib/submitted-feedback.test.ts#L28), [`submitted-feedback.test.ts:38`](apps/web-ui/src/lib/submitted-feedback.test.ts#L38), [`submitted-feedback.test.ts:48`](apps/web-ui/src/lib/submitted-feedback.test.ts#L48), [`submitted-feedback.test.ts:62`](apps/web-ui/src/lib/submitted-feedback.test.ts#L62), [`submitted-feedback.test.ts:70`](apps/web-ui/src/lib/submitted-feedback.test.ts#L70), [`FailureBlock.test.tsx:66`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L66), [`FailureBlock.test.tsx:92`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L92))
- FR-11.7: The feature detail view surfaces the resulting stories and the status of their tasks. ([impl](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/DecompositionView.tsx))

### FR-14: Result Delivery

A planning round's deliverable is a file the pod produces. Getting it back is a
first-class step, not an afterthought.

- FR-14.1: The round's result reaches the API by exactly one rule set, whichever way the pod delivered it — the features route (the pod POSTing directly) and the Floor's artifact-event handler both call the same `applyGapResult`. A round that read `ready` on one path and `failed` on the other would show the author a different feature depending on plumbing. A payload that does not validate marks the round failed and reports why rather than throwing, so the route can answer 400 and the Floor can log. ([validated by applyGapResult marks the round ready and stores the gap for a valid payload](libs/shared/src/feature-planning/apply-gap-result.test.ts#L28), [`apply-gap-result.test.ts:41`](libs/shared/src/feature-planning/apply-gap-result.test.ts#L41), [`apply-gap-result.test.ts:52`](libs/shared/src/feature-planning/apply-gap-result.test.ts#L52), [`apply-gap-result.test.ts:64`](libs/shared/src/feature-planning/apply-gap-result.test.ts#L64), [impl](libs/shared/src/feature-planning/apply-gap-result.ts))
- FR-14.2: A late or duplicate delivery records its round but never drags a finalized feature back into the wizard — the feature only advances while it is still mid-planning. ([validated by applyGapResult records a late result without dragging a finalized feature back into planning](libs/shared/src/feature-planning/apply-gap-result.test.ts#L77))
- FR-14.3: A failed ATTEMPT is not a failed ROUND. An attempt that produces no usable artifact — the subsystem's `missing`/`too-large`/`unreadable`, or a JSON parse error — is reported and logged, but the round stays `running`: the analyze node carries an `iteration_max` back-edge and its retry routinely produces what the first pod did not. The line's terminal settlement is the SINGLE owner of "this round failed", so the two cannot disagree. Recording it here showed the author "Planning round N failed" beside a Retry button while a retry was already in flight — and a third attempt is what that button would have started. ([validated by leaves the round running when an attempt produced no file, so a retry can rescue it](apps/floor/src/jobs/agent/planning-result.test.ts#L75), [`planning-result.test.ts:94`](apps/floor/src/jobs/agent/planning-result.test.ts#L94))
- FR-14.4: A planning node that genuinely failed — a crashed pod, a non-zero exit, a timeout — is retried once by the assembly line's existing `iteration_max` back-edge before the line fails. This cannot catch an agent that exits 0 having produced nothing: that node reports success, so the round is failed by FR-14.3's delivery check rather than retried. ([impl](libs/assembly-lines/src/assembly-lines/feature-planning.yaml))

### FR-15: Conversation Continuity

A round continues the previous round's conversation instead of being re-briefed from
scratch, so the agent keeps what it read and decided (ai-agent-subsystem#188).

- FR-15.1: A node declares what it continues with `continues: {node, key}`. `node` names the work; `key` names the THREAD — `line`, `task`, or `args.<name>` — which is what stops two features running the same definition concurrently from continuing each other, a cross-feature leak rather than merely a wrong answer. Both halves fail at LOAD, because an unresolvable reference would silently start a fresh conversation, and that is indistinguishable from one that continued and remembered nothing. The key is deliberately domain-free: the engine never learns what a feature is, and planning keys on `args.feature_id` exactly as detect lines carry `args.job_run_id`. ([validated by parseAssemblyLine accepts a node reference keyed by an arg](libs/assembly-lines/src/loader.test.ts#L979), [`loader.test.ts:988`](libs/assembly-lines/src/loader.test.ts#L990), [`loader.test.ts:1001`](libs/assembly-lines/src/loader.test.ts#L1003), [`loader.test.ts:1011`](libs/assembly-lines/src/loader.test.ts#L1013), [`loader.test.ts:1019`](libs/assembly-lines/src/loader.test.ts#L1021))
- FR-15.2: Conversations are indexed by `(kind, value, node)` and never by anything feature-shaped, so one thread's history is unreachable from another's and each node in a thread keeps its own. A run is never offered its own conversation. ([validated by ConversationsPort offers the newest saved conversation for a thread](libs/shared/src/project/conversations/conversations.test.ts#L16), [`conversations.test.ts:12`](libs/shared/src/project/conversations/conversations.test.ts#L12), [`conversations.test.ts:39`](libs/shared/src/project/conversations/conversations.test.ts#L39), [`conversations.test.ts:50`](libs/shared/src/project/conversations/conversations.test.ts#L50), [`conversations.test.ts:60`](libs/shared/src/project/conversations/conversations.test.ts#L60), [`conversations.test.ts:75`](libs/shared/src/project/conversations/conversations.test.ts#L75))
- FR-15.3: A conversation reserved at dispatch but never uploaded is never offered to a later run — resuming it would send a pod after an object that does not exist, and that fetch fails silently by design, so it would present as a fresh start rather than an error. *(amended 2026-08-21)* This is also why a relaunch reserves a FRESH pin rather than needing an idempotent reserve: a crash between the row insert and the launch leaves exactly such an orphan, which nothing will ever be offered. ([validated by ConversationsPort skips a reserved conversation whose run never uploaded](libs/shared/src/project/conversations/conversations.test.ts#L27), [`conversations.test.ts:69`](libs/shared/src/project/conversations/conversations.test.ts#L69), )
- FR-15.4: The archive moves as BYTES end to end. A transcript is gzip, and the Floor's utf-8 body helper replaces every sequence above 0x7F with U+FFFD — corruption that would surface only later, as a tar that will not extract inside a pod. ([validated by GET /api/agent-conversations returns the archive bytes unchanged](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L107), [`agent-conversations.test.ts:41`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L41))
- FR-15.5: The registry never fails a run. An unreserved id is still stored rather than lost over a bookkeeping mismatch, a missing bucket answers 202 rather than erroring, and an absent or unreadable archive answers 404 so the pod's best-effort restore degrades to a fresh conversation. Both routes require the internal token. ([validated by POST /api/agent-conversations still accepts an archive for an id nobody reserved](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L65), [`agent-conversations.test.ts:79`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L79), [`agent-conversations.test.ts:95`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L95), [`agent-conversations.test.ts:131`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L131), [`agent-conversations.test.ts:147`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L147), [`agent-conversations.test.ts:157`](apps/floor/src/delivery/http/routes/agent-conversations.test.ts#L157))
- FR-15.6: A thread key resolves against what the RUN carries, so the engine stays domain-free: `line` names this execution, `task` the task behind it, and `args.<name>` whatever value the caller put under that name. A key the run cannot satisfy is an ERROR, never an empty thread — falling back to "no conversation" would start fresh forever, and that is indistinguishable from continuity that remembered nothing. ([validated by keys a thread on the run for `line`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L11), [`conversation-thread.test.ts:18`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L18), [`conversation-thread.test.ts:25`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L25), [`conversation-thread.test.ts:32`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L32), [`conversation-thread.test.ts:41`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L42), [`conversation-thread.test.ts:47`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L51), [`conversation-thread.test.ts:53`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L57), [`resolve-conversation.test.ts:95`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L116))
- FR-15.7: A RETRY never continues. When the walk revisits a node through an `iteration_max` back-edge it re-runs the same work with the same prompt and no inherited state — a retry exists because the last attempt failed, and inheriting that attempt's context would make the rerun path-dependent rather than reproducible. ([validated by continues on a node\u2019s first execution](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L65), [`conversation-thread.test.ts:65`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L65), [`resolve-conversation.test.ts:87`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L104))
- FR-15.8: Each run FORKS: it resumes the thread's newest archived conversation and saves under a NEW id reserved before dispatch. Forking is what makes rewind possible at all — round 2's transcript still exists after rounds 3 and 4 ran, so continuing from it is just resuming a different id; resume-in-place would overwrite the very thing rewind needs. The id is reserved in advance because the pod is TOLD what to save as rather than reporting back. A node that declares no `continues` wires nothing. ([validated by continues the thread\u2019s newest conversation and saves as a new id](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L33), [`resolve-conversation.test.ts:53`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L53), [`resolve-conversation.test.ts:67`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L67), [`resolve-conversation.test.ts:76`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L92), [`resolve-conversation.test.ts:103`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L126))
- FR-15.9: The resolved conversation rides the PER-TASK recipe clone, exactly as the repo token does — which previous run to resume and which id to save as are per-run facts the shared catalog recipe cannot carry. The Floor threads the feature id into the line's args at dispatch, which is what lets planning's `args.feature_id` key resolve without the engine ever learning what a feature is. ([validated by wires resources.conversation onto the per-task clone when the run continues one](libs/shared/src/cluster/per-task-token.test.ts#L117), [`assembly-run-station-backend.test.ts:116`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L116), [`assembly-run-station-backend.test.ts:69`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L106))
- FR-15.10: A round that RESUMES the previous round's conversation sends only the author's reaction — no restatement of the draft, because the agent already holds its own last answer and re-sending it re-briefs the model on what it just said. Each comment nests under the section it lands on and each question's ASKED TEXT is quoted beside its answer: an id alone would not survive the CLI compacting the turn that asked it. Sections the author left alone are omitted — silence is not feedback, and listing every untouched section grows the turn back into the draft it exists to avoid. ([validated by nests each answered question under the section that asked it](libs/shared/src/feature-planning/planning-prompt.test.ts#L199), [`planning-prompt.test.ts:185`](libs/shared/src/feature-planning/planning-prompt.test.ts#L210), [`planning-prompt.test.ts:193`](libs/shared/src/feature-planning/planning-prompt.test.ts#L218), [`planning-prompt.test.ts:202`](libs/shared/src/feature-planning/planning-prompt.test.ts#L227), [`planning-prompt.test.ts:208`](libs/shared/src/feature-planning/planning-prompt.test.ts#L233), [`planning-prompt.test.ts:215`](libs/shared/src/feature-planning/planning-prompt.test.ts#L240), [`planning-prompt.test.ts:225`](libs/shared/src/feature-planning/planning-prompt.test.ts#L250))
- FR-15.11: Which form a round sends is decided at DISPATCH, from the resolved conversation's id alone — that id is empty for a first round, a retry, and a thread whose only prior run never uploaded its state, and every one of those needs the full composition. No second flag tracks it, because a flag set where the prompt is composed (lore-api) could not know what the Floor would later resolve. Both forms therefore ride the line's args, and a blank feedback turn falls back rather than dispatching an empty prompt. ([validated by sends only the new feedback when the run resumed a conversation](apps/floor/src/jobs/assembly-run/round-content.test.ts#L12), [`round-content.test.ts:21`](apps/floor/src/jobs/assembly-run/round-content.test.ts#L24), [`round-content.test.ts:32`](apps/floor/src/jobs/assembly-run/round-content.test.ts#L38), [`round-content.test.ts:41`](apps/floor/src/jobs/assembly-run/round-content.test.ts#L50), [`round-content.test.ts:47`](apps/floor/src/jobs/assembly-run/round-content.test.ts#L59), [`assembly-run-station-backend.test.ts:55`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L55), [`assembly-run-station-backend.test.ts:44`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L44))
- FR-15.14: The round content the dispatch chooses becomes the CR's **description**, not only its prompt. The recipe the pod runs renders `{description}`; `spec.prompt` is not what reaches the agent, so setting only the prompt handed every resumed round the full draft again — the exact re-briefing this feature exists to end, while every other signal (recipe field, reserved id, archive on disk) said continuity was working. The two fields are set from one value, because them disagreeing about what a run is working from is a bug in either direction. ([validated by dispatches the resumed round's feedback as the CR's description, not just its prompt](apps/floor/src/jobs/assembly-run/advance.test.ts#L347), [`advance.test.ts:380`](apps/floor/src/jobs/assembly-run/advance.test.ts#L381))
- FR-15.13: Every run option reaches the Station spec. The runner rebuilds that spec field by field, so an option it forgets is dropped in SILENCE — `featureId` and `roundFeedback` were both added and both lost that way, and the symptom was a run that dispatched perfectly and simply continued no conversation, which is indistinguishable from continuity that remembered nothing. *(amended 2026-08-21, #1466)* Third victim, and the reason the walk now has exactly ONE launch-spec builder: the assembly-run reaper's relaunch arm rebuilt the spec from the raw task description, dropping both `resources.conversation` and the round content — and because every launch RE-PROVISIONS the per-task recipe clone, that relaunch did not merely dispatch a thinner pod, it replaced a live pod's recipe with one that had no conversation. Both dispatch doors call `nodeLaunchSpec`, so a field added there reaches both or neither. ([validated by cluster mode forwards every run option to the Station spec](libs/shared/src/project/agents/agent-runner.test.ts#L70), )
- FR-15.12: The archive has a single-machine backend so continuity can be exercised off a cluster. A bucket wins where one is configured; `LORE_ARCHIVE_DIR` is opt-in rather than a default, because a deployment that lost its bucket config would otherwise write conversations to pod-local disk that vanishes with the pod — which reads as continuity right up until it isn't. Bytes round-trip unchanged (a gzip archive through a utf-8 hop loses every byte above 0x7F), a missing object reads null rather than throwing so the pod's best-effort restore still degrades to a fresh conversation, and a key that would escape the root is refused — conversation ids arrive over HTTP, so traversal is not something to trust upstream. ([validated by round-trips gzip bytes unchanged](libs/shared/src/project/archive/archive-file.test.ts#L18), [`archive-file.test.ts:33`](libs/shared/src/project/archive/archive-file.test.ts#L33), [`archive-file.test.ts:43`](libs/shared/src/project/archive/archive-file.test.ts#L43), [`archive-file.test.ts:51`](libs/shared/src/project/archive/archive-file.test.ts#L51), [`archive-file.test.ts:58`](libs/shared/src/project/archive/archive-file.test.ts#L58), [`archive-file.test.ts:70`](libs/shared/src/project/archive/archive-file.test.ts#L70))

### FR-16: Rewind

Planning history is a TREE, not a line: a round that went wrong can be dropped and the
next one forked from an earlier round — even the first. This is possible only because
each round saved its own transcript instead of overwriting the previous one.

- FR-16.1: `POST .../features/:id/iterations` accepts `from_iteration`; omitted, it builds on the latest round that produced a result, so today's behaviour is unchanged. A named round that the feature never had, or that produced no result, is REJECTED rather than silently ignored — continuing from a failed round means continuing from nothing, and the author would see a fresh start dressed as a rewind. ([validated by builds on the latest ready round when the author named none](libs/shared/src/project/features/features-port.test.ts#L88), [`features-port.test.ts:94`](libs/shared/src/project/features/features-port.test.ts#L95), [`features-port.test.ts:101`](libs/shared/src/project/features/features-port.test.ts#L102), [`features-port.test.ts:110`](libs/shared/src/project/features/features-port.test.ts#L111), [`features-port.test.ts:117`](libs/shared/src/project/features/features-port.test.ts#L118), [`features.test.ts:271`](apps/lore-api/src/api/routes/features/features.test.ts#L287))
- FR-16.2: The chosen round supplies BOTH halves of the new round — the draft its prompt carries and the conversation it resumes — so a rewind cannot half-happen with one round's draft and another's transcript. The new iteration records `parent_iteration`, without which the history is a list pretending to be a tree: round 5 would read as a refinement of round 4 when it descends from round 2. ([validated by rewinds to the round the author chose, carrying its draft and its task](apps/lore-api/src/api/routes/features/features.test.ts#L224))
- FR-16.3: A round is addressed by the task it ran as — what the caller already holds — and its conversation by the assembly line that reserved it. An explicit choice resolving to nothing starts FRESH rather than falling through to the newest round, because quietly resuming round 4 when the author asked for round 2 is indistinguishable from a rewind that worked. A retried round ran more than one line; the NEWEST is the attempt whose state the author actually saw, and since `listForTask` orders `created_at DESC` that attempt is its FIRST element — reading the last one resumed the discarded first attempt. ([validated by resumes the round the author chose, not the newest one](libs/shared/src/project/conversations/conversations.test.ts#L92), [`conversations.test.ts:118`](libs/shared/src/project/conversations/conversations.test.ts#L118), [`conversations.test.ts:143`](libs/shared/src/project/conversations/conversations.test.ts#L143), [`resolve-conversation.test.ts:157`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L157), [`resolve-conversation.test.ts:184`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L184), [`resolve-conversation.test.ts:211`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L211), [`resolve-conversation.test.ts:225`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L225), [`resolve-conversation.test.ts:333`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L333))
- FR-16.4: The wizard offers only rounds that produced a result, newest first, marking the newest SURVIVING one as the latest rather than the newest attempt, and naming a fork's parent in its label. It shows no picker until there is more than one round to choose between. Rewinding starts an ordinary round, so the one-round-at-a-time guard still applies. ([validated by lists the rounds newest first and marks the latest](apps/web-ui/src/lib/round-picker.test.ts#L19), [`round-picker.test.ts:31`](apps/web-ui/src/lib/round-picker.test.ts#L31), [`round-picker.test.ts:40`](apps/web-ui/src/lib/round-picker.test.ts#L40), [`round-picker.test.ts:50`](apps/web-ui/src/lib/round-picker.test.ts#L50), [`round-picker.test.ts:60`](apps/web-ui/src/lib/round-picker.test.ts#L60), [`round-picker.test.ts:68`](apps/web-ui/src/lib/round-picker.test.ts#L68), [`round-picker.test.ts:74`](apps/web-ui/src/lib/round-picker.test.ts#L74), [`round-picker.test.ts:87`](apps/web-ui/src/lib/round-picker.test.ts#L87), [`round-picker.test.ts:91`](apps/web-ui/src/lib/round-picker.test.ts#L91), [`round-picker.test.ts:95`](apps/web-ui/src/lib/round-picker.test.ts#L95), [`round-picker.test.ts:99`](apps/web-ui/src/lib/round-picker.test.ts#L99))
- FR-11.9: *(added 2026-08-13)* A spec-task the station files MUST be indistinguishable from one the `tasks.md` sync files. It carries `spec_task_id` — the name every other producer and reader of a spec-task uses (`lore_list_pipeline_tasks`, the decomposition view) — and, when the line carries one, `feature_id`. Spreading the agent's artifact instead published the agent's own vocabulary (`id`) and no feature link at all, so the detail page's `context_bundle->>'feature_id'` filter matched zero rows for every feature ever decomposed, and merge-check's spec-status flip, which reads the same key, never fired. `feature_id` is OMITTED rather than null when the line has none: the filter is a JSON text match, so a literal null would match nothing while looking set. ([validated by stamps the feature a spec-task belongs to](apps/stations/src/stations/issues/issues.test.ts#L149), [`issues.test.ts:174`](apps/stations/src/stations/issues/issues.test.ts#L174), [`issues.test.ts:195`](apps/stations/src/stations/issues/issues.test.ts#L195); implemented by [`issues.ts:101`](apps/stations/src/stations/issues/issues.ts#L101))
- FR-11.10: *(added 2026-08-13)* A feature's spec-tasks are readable through the Project facade, not by querying `pipeline.tasks` from a page. `specTasksForFeature` keys on `context_bundle->>'feature_id'` — the key the issues station stamps (FR-11.9) — scopes to the repo, and orders by `spec_task_id` so the story tree renders in the agent's intended order rather than insertion order. The in-memory double is the behavioural spec of the Pg adapter: same filter, same ordering, same empty result for a feature that was never decomposed. ([validated by specTasksForFeature keys on the context bundle's feature_id](libs/shared/src/project/tasks/task-store-memory.test.ts#L307), [`task-store-memory.test.ts:343`](libs/shared/src/project/tasks/task-store-memory.test.ts#L343), [`task-store-memory.test.ts:356`](libs/shared/src/project/tasks/task-store-memory.test.ts#L356); implemented by [`task-store-port.ts:100`](libs/shared/src/project/tasks/task-store-port.ts#L100))

### FR-12: Round Observability

A planning round is an assembly line, so the author MUST be able to watch it run
rather than infer progress from a spinner and an elapsed timer.

- FR-12.1: While a round is running, the card that announces it also renders the round's live run visualization — the definition graph, the node transcript, and the connection state — alongside the round number and the elapsed/budget timer. ([validated by RunningCard announces the round number and the elapsed / budget timer](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L46), [`RunningCard.test.tsx:60`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L63), [impl](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.tsx))
- FR-12.2 *(restated 2026-08-14: the run carries its graph)*: The graph MUST draw from the round's first moment, before the walk has recorded a single node row. That used to require `feature-planning` and `feature-finalize` to be declared graphs in a hand-transcribed builtin set inside the web UI — a copy that could only ever describe the CURRENT blueprint, never the one a given run walked, and which had not matched the definition for months. The run now carries its own clone, stamped at start (FR6.38 in `specs/6-dark-factory/spec.md`), so the first moment of a round already has the whole graph and the transcription is deleted. A run stamped before clones existed carries none and still degrades to the inferred chain, marked synthetic so edge labels are suppressed — no backfill is possible, since the blueprint a historical run used is not recoverable from the row. ([validated by toFeatureRunPayload resolves the declared feature-planning graph for a run with no visit rows](apps/web-ui/src/lib/feature-run.test.ts#L58), [`feature-run.test.ts:103`](apps/web-ui/src/lib/feature-run.test.ts#L99); implemented by [`run-graph-definition.ts:60`](apps/web-ui/src/lib/run-graph-definition.ts#L60))
- FR-12.3: The run reaches the wizard through the poll it already runs — the run the API names for the feature, its visit rows, and the resolved graph — so a round that starts after the page rendered appears on the next tick with no second fetch loop. A feature with no run row yet reports no run rather than failing the poll. ([validated by toFeatureRunPayload keeps the visit rows so the panel can colour the nodes](apps/web-ui/src/lib/feature-run.test.ts#L93), [`RunningCard.test.tsx:80`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L87), [`feature-run-fetch.test.ts:49`](apps/web-ui/src/lib/feature-run-fetch.test.ts#L49), [`feature-run-fetch.test.ts:65`](apps/web-ui/src/lib/feature-run-fetch.test.ts#L65), [`feature-run-fetch.test.ts:71`](apps/web-ui/src/lib/feature-run-fetch.test.ts#L71), [`feature-run-fetch.test.ts:77`](apps/web-ui/src/lib/feature-run-fetch.test.ts#L77), [impl](apps/web-ui/src/lib/feature-run.ts))
- FR-12.4: The local-Docker station log tail stays available alongside the visualization, since that backend records no run events. ([validated by RunningCard shows the local station log tail when one is available](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L100))
- FR-12.5 *(restated 2026-08-19: resolved by SUBJECT, not by task)*: The run the wizard draws is the newest run whose subject is this feature — `feature:<id>` — whatever blueprint it turned out to be. Resolving it through a task id and then filtering to the `feature-planning` blueprint excluded by name every other line a feature can start: a finalize run is a different task AND a different blueprint, so pressing "Create spec PR" began work the page had no way to name and the wizard went on drawing the planning run, which read as nothing having happened. Newest wins rather than newest-OPEN, so a feature whose run has finished still shows that run and its reason instead of falling back to silence. The web-ui no longer resolves a run at all — the server names it, and `runTaskIdFor`/`fetchFeatureRun` were deleted with the task-keyed lookup they served. ([validated by resolves the newest run working the feature, whatever line it is](apps/lore-api/src/api/routes/features/features.test.ts#L639), [`features.test.ts:657`](apps/lore-api/src/api/routes/features/features.test.ts#L657), [`features.test.ts:679`](apps/lore-api/src/api/routes/features/features.test.ts#L679); implemented by [`features.ts:featureRunId`](apps/lore-api/src/api/routes/features/features.ts#L151))
- FR-12.6: *(added 2026-08-13)* The wizard's poll endpoint authorizes every request: a caller needs a session and access to the repo named in the path. It had NEITHER — any signed-in user could poll any repo's feature and read its original prompt, every round's gap analysis and the draft spec, for a repo they cannot see on GitHub. The check runs BEFORE the feature is read, which is what stops a 404 being used to probe which feature ids exist in a repo the caller has no access to. ([validated by rejects a caller with no session](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L49), [`route.test.ts:55`](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L55), [`route.test.ts:61`](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L61), [`route.test.ts:68`](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L68), [`route.test.ts:79`](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L79), [`route.test.ts:88`](apps/web-ui/src/app/api/repos/[owner]/[repo]/features/[id]/route.test.ts#L88))
- FR-12.6a: *(added 2026-08-14)* A denial says WHY, in the server log. The gate answers one flat "Access denied" to the caller — correct, since telling an outsider which of three things went wrong is itself a disclosure — but it discarded GitHub's status entirely, so the operator had nothing either. The three reasons need different actions: **404** usually means the OAuth app has no access to the org rather than that the repo is missing, **401** means the session's token is stale or revoked (sign out and back in mints a new one), **403** means rate-limited or blocked by the org's app policy; a transport failure is named as such rather than reported as a refusal. The token never appears in the log, and a granted request logs nothing at all. ([validated by names the repo and GitHub's status when access is refused](apps/web-ui/src/lib/user-repo-access.test.ts#L48), [`user-repo-access.test.ts:58`](apps/web-ui/src/lib/user-repo-access.test.ts#L58), [`user-repo-access.test.ts:68`](apps/web-ui/src/lib/user-repo-access.test.ts#L68), [`user-repo-access.test.ts:80`](apps/web-ui/src/lib/user-repo-access.test.ts#L80), [`user-repo-access.test.ts:88`](apps/web-ui/src/lib/user-repo-access.test.ts#L88); implemented by [`user-repo-access.ts:11`](apps/web-ui/src/lib/user-repo-access.ts#L11))
- FR-12.7: *(added 2026-08-13)* The poll is one module and one hook, not fifty lines inside a route handler. `loadFeaturePoll` owns every read the wizard's 4s tick needs — feature, latest round, that round's task, the last round that produced an analysis, and the run — and lives under `src/lib/`, because `src/app/api/**` is excluded from coverage, so the wizard's whole data path was untested by construction. Its payload type is the ONE definition of the poll's contract: the wizard used to carry a private copy of the same shape with nothing keeping the two in step. `useFeaturePlanningPoll` owns the mount fetch, the interval and its cleanup, and a failed tick keeps the last good payload rather than blanking a page over one 500. The finalize watch reads that same payload instead of running a second interval against the same route. ([validated by returns the seed before the mount fetch resolves](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L144), [`useFeaturePlanningPoll.test.tsx:58`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L158), [`useFeaturePlanningPoll.test.tsx:74`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L174), [`useFeaturePlanningPoll.test.tsx:97`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L197), [`useFeaturePlanningPoll.test.tsx:116`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L216), [`useFeaturePlanningPoll.test.tsx:137`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L237), [`useFeaturePlanningPoll.test.tsx:156`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L256), [`feature-poll.test.ts:43`](apps/web-ui/src/lib/feature-poll.test.ts#L45), [`feature-poll.test.ts:53`](apps/web-ui/src/lib/feature-poll.test.ts#L55), [`feature-poll.test.ts:61`](apps/web-ui/src/lib/feature-poll.test.ts#L63), [`feature-poll.test.ts:71`](apps/web-ui/src/lib/feature-poll.test.ts#L73), [`feature-poll.test.ts:83`](apps/web-ui/src/lib/feature-poll.test.ts#L85), [`feature-poll.test.ts:95`](apps/web-ui/src/lib/feature-poll.test.ts#L97), [`feature-poll.test.ts:109`](apps/web-ui/src/lib/feature-poll.test.ts#L111), [`feature-poll.test.ts:120`](apps/web-ui/src/lib/feature-poll.test.ts#L122), [`feature-poll.test.ts:135`](apps/web-ui/src/lib/feature-poll.test.ts#L156); implemented by [`feature-poll.ts:1`](apps/web-ui/src/lib/feature-poll.ts#L1))

- FR-12.8: *(added 2026-08-13)* While a node runs, the card reports the two numbers that say whether to keep waiting: elapsed against THE DEADLINE THE NODE IS ACTUALLY KILLED AT, and the tokens spent so far. The budget comes from the definition's node — its `timeout_minutes` else the reaper's own default, plus the reaper's grace — because the number shown before was the feature-planning agent definition's timeout, which nothing enforces and which is one number for a line whose nodes have separate budgets: the spec phase counted against the planning round's clock. A feature that resolves no line keeps the round's budget, having no definition to read. A `wait` node has no budget at all, since it is parked on a person. The token count is summed from `pipeline.agent_run_turns` rather than `pipeline.llm_calls`: a cost row lands only when an agent run ENDS, which for a planning round is the moment this card disappears, so the authoritative table can never answer "so far". Cached prompt tokens count toward the total — they are billed, and hiding them would understate a cached run by an order of magnitude — and a run that has reported no usage shows no counter rather than a zero that reads as "nothing is happening". ([validated by counts against the WORKING NODE's kill budget, not the round's](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L155), [`RunningCard.test.tsx:171`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L171), [`RunningCard.test.tsx:184`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L184), [`RunningCard.test.tsx:202`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L202), [`RunningCard.test.tsx:215`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L215), [`RunningCard.test.tsx:230`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L230); the budget itself [`node-budget.test.ts:16`](apps/web-ui/src/lib/node-budget.test.ts#L16), [`node-budget.test.ts:25`](apps/web-ui/src/lib/node-budget.test.ts#L25), [`node-budget.test.ts:34`](apps/web-ui/src/lib/node-budget.test.ts#L34), [`node-budget.test.ts:44`](apps/web-ui/src/lib/node-budget.test.ts#L44), [`node-budget.test.ts:50`](apps/web-ui/src/lib/node-budget.test.ts#L53), [`node-budget.test.ts:54`](apps/web-ui/src/lib/node-budget.test.ts#L57), [`node-budget.test.ts:63`](apps/web-ui/src/lib/node-budget.test.ts#L66); the usage total [`run-tokens.test.ts:13`](apps/web-ui/src/lib/run-tokens.test.ts#L13), [`run-tokens.test.ts:17`](apps/web-ui/src/lib/run-tokens.test.ts#L17), [`run-tokens.test.ts:26`](apps/web-ui/src/lib/run-tokens.test.ts#L26), [`run-tokens.test.ts:39`](apps/web-ui/src/lib/run-tokens.test.ts#L39), [`run-tokens.test.ts:49`](apps/web-ui/src/lib/run-tokens.test.ts#L49), [`run-tokens.test.ts:53`](apps/web-ui/src/lib/run-tokens.test.ts#L53), [`run-tokens.test.ts:57`](apps/web-ui/src/lib/run-tokens.test.ts#L57), [`run-tokens.test.ts:61`](apps/web-ui/src/lib/run-tokens.test.ts#L61); implemented by [`node-budget.ts:1`](apps/web-ui/src/lib/node-budget.ts#L1), [`run-tokens.ts:1`](apps/web-ui/src/lib/run-tokens.ts#L1))
- FR-12.9: *(added 2026-08-13)* `DecompositionProgressCard` shows elapsed against the working node's kill deadline and the run's token total — the same two numbers `RunningCard` reports — and BOTH are shared components rather than a second copy. `ElapsedTimer` is extracted from `RunningCard.tsx` into a shared module; leaving it inline means the second caller diverges silently. The timeout it receives is `nodeBudgetMinutes(definition, nodeId)` (`src/lib/node-budget.ts`): the node's own `timeout_minutes` when the definition resolves that node, falling back to the reaper's grace-extended default otherwise — the actual kill deadline, not the round's ceiling. The card omits the timer entirely when the definition does not resolve the node; a wrong deadline is worse than no deadline. Token display comes from a `RunTokens` component and `formatTokens` helper (`src/lib/run-tokens.ts`): rendered when `run.tokens` is a positive number, absent when the field is zero or missing — the first token event may not have arrived yet, and a "~0" badge communicates nothing. `FeatureRunPayload` gains a `tokens` field carrying the total reported so far; `PlanningWizard` passes `run` (already in `data.run`) to `DecompositionProgressCard` alongside the existing `nodeId` and `since`. Colocated tests in `DecompositionProgressCard.test.tsx` cover: the elapsed timer and token badge both rendered when the run carries a definition that resolves the node and a positive token count; no timer when the definition does not resolve the node; no badge when `run.tokens` is absent.
- FR-12.10: *(added 2026-08-17)* The run GRAPH is fetched once per run, not once per poll tick. It is a clone of the blueprint, stamped at start and never edited (FR6.38 in `specs/6-dark-factory/spec.md`), so re-shipping it every four seconds for the life of a planning round is waste beside the nodes and tokens that actually change. The poll names the run whose graph it already holds (`?graph=<runId>`), the server omits THAT graph and says so, and the client folds its own back in. Naming the RUN rather than sending a bare flag is what makes a retry — a new run, a new clone — fetch its own graph instead of inheriting the previous one, and an omission answered for a different run resolves to no graph rather than a confidently wrong picture. A SYNTHETIC graph is never cached: it is inferred from the visit rows and grows as they land, so it must be re-sent every tick or the drawing stops matching the run. ([validated by names the run whose graph it holds, so the server can stop re-sending it](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L62), [`useFeaturePlanningPoll.test.tsx:84`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L84), [`useFeaturePlanningPoll.test.tsx:115`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.test.tsx#L115), [`feature-poll.test.ts:144`](apps/web-ui/src/lib/feature-poll.test.ts#L163), [`run-graph-cache.test.ts:22`](apps/web-ui/src/lib/run-graph-cache.test.ts#L22), [`run-graph-cache.test.ts:26`](apps/web-ui/src/lib/run-graph-cache.test.ts#L26), [`run-graph-cache.test.ts:30`](apps/web-ui/src/lib/run-graph-cache.test.ts#L30), [`run-graph-cache.test.ts:34`](apps/web-ui/src/lib/run-graph-cache.test.ts#L34), [`run-graph-cache.test.ts:42`](apps/web-ui/src/lib/run-graph-cache.test.ts#L42), [`run-graph-cache.test.ts:51`](apps/web-ui/src/lib/run-graph-cache.test.ts#L51), [`run-graph-cache.test.ts:63`](apps/web-ui/src/lib/run-graph-cache.test.ts#L63); implemented by [`run-graph-cache.ts:1`](apps/web-ui/src/lib/run-graph-cache.ts#L1), [`useFeaturePlanningPoll.ts:41`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/useFeaturePlanningPoll.ts#L41))

- FR-12.11: *(added 2026-08-19)* Finalize refuses to start a SECOND run for a feature something is already working, and the refusal names it: `409` carrying the in-flight run id under both spellings. `canFinalize` gates on `feature.status`, which does not move until a spec PR lands roughly eighteen minutes later, so every press inside that window minted another task, another run, another branch and another spec PR — four presses in ninety-two seconds produced three duplicate spec PRs, two of them writing different content to the same spec path. The id rides the refusal because a second press means "show me what is happening", not "you broke something", so the caller can draw the live run instead of an error. *(restated 2026-08-22: the guard is structural)* With the legacy path retired there is nothing left to guard against by counting runs — the parked node IS the permission. Reporting an outcome to the author node un-parks it, so the press that follows an accept finds nothing to report to and is refused, and the refusal still carries the run id because a duplicate press means "show me". One guard replaces two: the `findOpenBySubject` count and refine's iteration-scoped `roundInFlight` were overlapping answers to the same question, and two guards in one handler is a precedence question nobody should have to answer. Beneath both, the store's unique index is the atomic backstop for two presses that race the read, and a task whose start JOINED an existing run is settled rather than left running (specs/6-dark-factory FR6.49–FR6.50). ([validated by answers 409 naming the run already in flight, and mints no task](apps/lore-api/src/api/routes/features/features.test.ts#L772), [`features.test.ts:807`](apps/lore-api/src/api/routes/features/features.test.ts#L807), [`features.test.ts:845`](apps/lore-api/src/api/routes/features/features.test.ts#L845), [`assembly-run-station-backend.test.ts:101`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L145); implemented by [`features.ts:runAlreadyWorking`](apps/lore-api/src/api/routes/features/features.ts#L134))

### FR-13: Failure Visibility

A round that dies MUST say why. The wizard previously guessed, because nothing on
the cluster path ever recorded a cause.

- FR-13.1: A planning round is finished only when its GapResult actually landed. A line that ends without one — whatever its outcome — marks the iteration `failed` and restores the feature per the same rule the synchronous path uses, instead of leaving the round `running` and the wizard spinning forever. A round that did post its result is left untouched. ([validated by settleTaskForLine marks the planning round failed and reverts the feature to draft when the line failed](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L98), [`settle-task.test.ts:119`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L119), [`settle-task.test.ts:164`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L164), [impl](apps/floor/src/jobs/assembly-run/settle-task.ts))
- FR-13.2: The failure block reports the recorded cause — the task's `failure_reason` first, else the line's own reason — and keeps the "could not reach the model" hint only for a failure that recorded neither. ([validated by FailureBlock shows the task's failure reason instead of guessing a cause](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L9), [`FailureBlock.test.tsx:25`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L27), [`FailureBlock.test.tsx:47`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L47), [`FailureBlock.test.tsx:83`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L137), [`feature-run.test.ts:100`](apps/web-ui/src/lib/feature-run.test.ts#L115), [impl](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.tsx))
- FR-13.3: A failed round links to its run, so the full transcript is one click away; a round with no run shows no link. ([validated by FailureBlock links to the run transcript when the round has a run](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L106), [`FailureBlock.test.tsx:70`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/FailureBlock.test.tsx#L122))
- FR-13.4: A per-task GitHub token that mints empty MUST fail at the mint, naming the repo and the App variables to check. An empty token writes a present-but-useless Secret key, so the pod starts and then dies in its init container on `git clone` with GitHub's deliberately uninformative "Repository not found" — a cause visible only in pod logs. ([validated by GithubTokenMinter throws naming the repo and the App vars when the token comes back empty](apps/floor/src/jobs/station/kube-token-provisioner.test.ts#L13), [`kube-token-provisioner.test.ts:5`](apps/floor/src/jobs/station/kube-token-provisioner.test.ts#L5), [impl](apps/floor/src/jobs/station/kube-token-provisioner.ts))

### Scenario 6: A merged spec decomposes into stories + tasks

**Actor:** Developer / pipeline

**Flow:**
1. A finalized feature's spec PR is merged to `main`.
2. The merge resumes the planning line's parked `merged` wait node; the `decompose` node reads the spec and returns a story/task tree, on that same line.
3. One Issue is opened per user story; one `spec-task` row is created per task, linked to its story and feature.
4. The implementation pipeline picks up the tasks under the repo's trust gate.

**Acceptance Criteria:**
- Decomposition runs on spec-PR merge, by resuming the line's parked `merged` node rather than minting a task. ([validated by reports success to the parked merged node of the PR's line](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L139), [validated by the merge resume walks the parked line into decompose](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L125))
- The agent's `DecompositionResult` validates against the shared schema; an invalid result fails the round. ([validated by parseDecomposition accepts a valid stories payload](libs/shared/src/feature-planning/decomposition-result.test.ts#L38))
- A story Issue is created per story unless the dark-factory `create_issue` policy says never. ([validated by storyIssueBody renders the story Issue](libs/shared/src/feature-planning/decomposition-plan.test.ts#L77))
- Each task is a `spec-task` row linked to its story Issue and feature, runnable by the existing pipeline. ([validated by specTaskRows links each task to its story issue and feature](libs/shared/src/feature-planning/decomposition-plan.test.ts#L32))
- FR-13.5: *(added 2026-08-13)* The two states after the spec PR is pushed have their own views. `awaiting-merge` — the line parked on its `merged` wait node — says the machine is waiting on a PERSON, links the PR, and states that decomposition starts automatically so the author knows not to come back. `decomposing` names WHICH node is working, because they fail for different reasons: a spec too thin to break down is a question for the author, while a label the repo lacks sends the artifact back for one correction, and an attempt number above 1 says so. Both are new arms on the `featurePhaseOf` union and new cases in one switch — the seam it exists for. ([validated by reports awaiting-merge while the spec PR is open](apps/web-ui/src/lib/feature-phase.test.ts#L148), [`feature-phase.test.ts:147`](apps/web-ui/src/lib/feature-phase.test.ts#L174); implemented by [`SpecPrCard.tsx:1`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/SpecPrCard.tsx#L1), [`DecompositionProgressCard.tsx:1`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/DecompositionProgressCard.tsx#L1))
- FR-13.6: *(added 2026-08-13)* The wizard stays mounted for as long as the FEATURE'S LIFECYCLE is moving, which includes an open spec PR. An open PR is a waiting state on the same assembly line — the line is parked on `merged`, and merging it resumes the walk into decomposition — so unmounting the wizard the moment the PR was stamped made the awaiting-merge and decomposing views reachable only to a reader who happened to be watching when the line got there; opening the page fresh showed the finished view for a feature that was still working. The wizard, not the page, decides when the settled view takes over, because only the line knows; the page hands that view in, since it needs the decomposition rows the poll does not carry. A line that reports `done` while the feature is still planning-active does NOT settle the page — a legacy feature mints one line per round, and that line finishes while the author still has a decision to make. ([validated by returns true through planning and while the spec PR is open](apps/web-ui/src/app/repos/[owner]/[repo]/features/feature-status.test.ts#L47), [`feature-status.test.ts:55`](apps/web-ui/src/app/repos/[owner]/[repo]/features/feature-status.test.ts#L55), [`PlanningWizard.acceptance.test.tsx:325`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L325), [`PlanningWizard.acceptance.test.tsx:336`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L336), [`PlanningWizard.acceptance.test.tsx:325`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L343); the decomposition card counts the NODE's attempts, not the author's planning rounds [`feature-phase.test.ts:132`](apps/web-ui/src/lib/feature-phase.test.ts#L159); implemented by [`feature-status.ts:48`](apps/web-ui/src/app/repos/[owner]/[repo]/features/feature-status.ts#L48))
