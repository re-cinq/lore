# Feature Specification: Dark Factory Mode

> **Execution substrate moved (ADR-031, `specs/floor-on-ai-subsystem/`).** Dark Factory's
> policy (issue suppression, auto-merge, branch-as-state, audit) is unchanged, but the
> tasks it governs now run on the ai-agent-subsystem's `Agent` CRs, not `LoreTask` CRs —
> read the `LoreTask`-specific mechanics here in the past tense.

| Field    | Value                          |
|----------|--------------------------------|
| Feature  | Dark Factory Mode              |
| Branch   | 6-dark-factory                 |
| Status   | Shipped                        |
| Created  | 2026-04-28                     |
| Owner    | Platform Engineering           |

Dark Factory mode trims the pipeline's per-task artifact chatter — GitHub Issues, status comments, Slack messages, bot reviews — down to the few durable artifacts (the branch, the merged PR, the curated episode), suppressing issues and auto-merging low-risk changes so humans are not buried in watch-me-work theater.

## Problem Statement

Lore today defaults to maximum chatter. Every pipeline task produces a
parallel ledger of artifacts that humans must look at: a GitHub Issue
with status comments, a LoreTask custom resource with status fields, a
`pipeline.tasks` row, Slack messages on every PR open, bot review
comments on every PR, and N stage transitions across CR controller →
Job pod → watcher → review CR → review Job → review reactor.

For a single implementation task this produces ~14 distinct artifacts
of which only three are durable a year later: the branch, the merged
PR, and the curated episode. Everything else is "watch me work"
theater that the team has stopped reading — evidenced by routine
auto-generated PRs sitting open for 8+ days with green CI before being
manually triaged or closed.

The pain compounds across flow types:

1. **Implementation flow** runs four ephemeral Job pods minimum
   (impl → review → address → re-review) with state handed off between
   them via Kubernetes custom resources, GitHub Issues, comments, and
   database rows. A pod death mid-iteration loses partial work because
   state lives in three places at once.

2. **Spec / gap-fill / runbook flows** create the same machinery for
   doc-only output: an Issue, status comments, an opened PR, a bot
   review, and a human approval. Humans cannot keep up, so PRs stale.

3. **Feature-request flow** produces a spec PR that humans review,
   then later N implementation PRs they review again. A four-task
   feature is five review touchpoints for the same intent.

4. **Onboarding flow** gates value on a human merging the bot's
   onboarding PR before any context becomes available.

5. **Local runner flow** demonstrates the inverse: one process, one
   PR, no Issue, no CR, no parallel ledger. State lives in the
   worktree and the branch. This is the model.

The result is a system that is technically autonomous (the agents do
the work) but practically not (humans still touch every artifact),
and that is fragile to pod death because durable state is fragmented
across CR / DB / Issue / PR / branch.

## Vision

Lore operates as a **dark software factory**. Autonomous operation is
the default. Humans participate at exactly two points: **intent
definition** (what should the system build) and **stage-gate
validation** (was this output acceptable). Everything between is
machine-internal and produces no human-facing artifact unless an
exception requires escalation.

Three coordinated changes deliver this vision:

1. **Durable state (branch-as-state → DB-as-state, restated 2026-07).**
   Every workflow phase ends with a commit carrying structured trailers
   (`Lore-Stage:`, `Lore-Iteration:`, `Lore-Task:`) — the audit
   substrate, emitted unconditionally on all Lore-authored commits.
   Under the event-driven walk (ADR-031) the durable state is the
   `pipeline.station_runs` rows, not the git log: a Floor pod
   that dies leaves no line stranded because transitions re-derive from
   the persisted node rows (FR1.2) — no CR status sync, no parallel
   ledger.

2. **Workflow as declarative graph.** The implicit chain
   `implement → validate → push → review → address → re-review` is
   externalized into a YAML/DOT-style directed graph: nodes are
   stages (agent prompts, validation steps, gate checks), edges
   carry conditions on commit / CI / review outcomes. New flows are
   new graphs, not new code paths. The local runner and the Floor's
   event-driven walk share the same graph definition.

3. **Human gates are opt-out, not opt-in.** A per-repo `dark_factory`
   setting block flips the defaults: GitHub Issues are created only
   for exception surfaces (approval gate required, escalation needed,
   or explicit per-repo opt-in). Auto-merge is enabled for
   low-blast-radius paths on green CI + bot approval + sufficient
   trust level. Slack notifications fire for escalations and watched
   completions only. The progressive-trust system — already present
   — gates which paths qualify for auto-merge.

The feature is observable end-to-end: every task's lifecycle is
reconstructable from the persisted node rows and audit log, every
auto-merge records the trust decision in the audit log, and every
escalation produces an Issue with full context attached.

## Clarifications

**Session 2026-04-28**

- **Q1.** Assembly line on-disk format → A: Pure YAML (matches `task-types.yaml`, single source of truth, web-ui renders directly)
- **Q2.** Bot behavior on PRs outside the auto-merge path allowlist → A: Review-and-await-human (bot posts inline comments + verdict, PR sits open until a human merges; no time-based auto-merge fallback in v1)
- **Q3.** Authorization required to change `dark_factory.*` settings → A: Two-key — `enabled` toggle and `auto_merge.paths` changes require admin-scope token + CODEOWNERS approval recorded in audit log; lighter sub-settings (`notify`, `create_issue`, `review`) need only admin scope
- **Q4.** Concurrency control when two supervisors think they own the same task → A: DB row-level lease keyed on branch name; first action of any supervisor is `acquire_lease(branch_name)`; lease has a TTL that expires automatically for pod-death recovery
- **Q5.** Commit-trailer behavior in opt-out repos → A: Trailers always on regardless of `dark_factory.enabled` — strictly additive, single supervisor code path, pod-death recovery works uniformly across opt-in and opt-out repos

## Goals (Background)

1. **Cut handovers.** A successful implementation task should run
   from intent to merged PR in ≤ 1 supervised process per iteration,
   not 4+ ephemeral Job pods linked by CR / Issue / comment chains.
2. **Make pods restartable without loss.** A supervisor pod that
   dies mid-flow resumes from the last commit on the branch and loses
   no committed work.
3. **Stop the stale-PR graveyard.** Routine doc / spec / gap-fill PRs
   that pass green CI and bot review should not require human
   triage; they merge automatically on path-allowlist.
4. **Make Issues meaningful again.** A GitHub Issue in a Lore-managed
   repo should mean "a human is actually expected to do something."
   Today it means "a task happened" — which is noise.
5. **Preserve audit completeness.** Every action a Lore agent takes
   must remain traceable, even after Issues stop being the breadcrumb
   layer. Branch + PR + episode + audit_log replace Issue chatter.

## Non-Goals (this feature)

- The **Operation phase** of the BCG dark-factory model
  (auto-deployment, canary, auto-rollback, auto-incident-remediation).
  This is a follow-up feature.
- **Parallel red-team agents** (security-review, perf-review,
  spec-conformance, doc-coverage running concurrently). Today's single
  Haiku reviewer remains; fan-out is a follow-up.
- **Removal of the LoreTask CRD.** The CRD continues to spawn pods;
  what changes is that pod-internal phases use commits instead of CR
  status fields as the state machine. Eliminating the CRD entirely is
  out of scope.
- **Multi-provider model routing** (Fabro-style stylesheets). Lore
  remains Claude-Code-centric.

## User Personas

**Platform Engineer** — Operates Lore. Needs visibility into autonomous flows: which tasks
ran dark, which auto-merged, which escalated. Configures per-repo
`dark_factory` settings and the global path-allowlist. Investigates
exceptions when they fire.

**Developer / Maintainer** — Lives in a Lore-onboarded repo. Today receives ~10 bot Issue
notifications per week from routine gap-fill / spec-drift / runbook
tasks, most of which they ignore. After dark mode: receives Slack
or Issue notifications only when human action is genuinely needed.
Trust ramps up automatically as repo accumulates clean merges.

**Product Manager (non-engineer)** — Submits feature intents via Slack `/lore feature-request`. Needs to
know "is my intent being worked on" and "where is the result." Today
they watch GitHub Issues; after dark mode they watch the web-ui
pipeline page. The web-ui already exists — this feature shifts PMs
to it as the canonical surface.

**Reviewer (human)** — Today reviews bot-authored PRs (often 8 days late). After dark mode:
reviews only PRs that are *not* in the auto-merge allowlist (code
changes, infra changes, anything outside docs/specs/ADRs). Sees
fewer PRs, each carrying genuine human-decision weight.

## Background — User Scenarios & Acceptance Criteria

These walkthroughs are illustrative; the normative, testable contract lives in
the Functional Requirements below. (Some describe the original supervisor-pod
framing — superseded by the Floor's event-driven assembly-line walk per
ADR-031; the FRs carry the reconciled behaviour.)

**Scenario 1: Routine doc PR auto-merges**

**Actor:** System (no human)

**Flow:**
1. Weekly gap-detection job identifies a missing runbook section.
2. Pipeline creates a `gap-fill` task. Repo has `dark_factory.enabled = true`.
3. No GitHub Issue is created for the task.
4. Supervisor process runs the gap-fill assembly line: draft → validate → commit → push → bot-review.
5. Each phase commits with `Lore-Stage:` trailer.
6. Bot review approves; CI is green; path is in `auto_merge.paths` (e.g. `runbooks/`); repo trust ≥ docs.
7. PR auto-merges.
8. Episode + curated memory are written. Audit log records the auto-merge with policy decision.

**Acceptance Criteria:**
- No GitHub Issue exists for this task at any point.
- The branch's commit log contains, in order, `[stage:draft]`, `[stage:validate]`, `[stage:review]`, `[stage:retrospective]` trailers.
- The PR body includes a `Lore-Task: <uuid>` line and a link to the policy that justified auto-merge.
- The audit log entry names the rule applied (path-allowlist, trust level, CI status, bot-approval).
- No human action occurs between push and merge: the PR merges automatically once CI is green (auto-merge engine SLA ≤ 60s per research R6), so wall-clock-to-merge is bounded by CI duration alone.

**Scenario 2: Implementation task survives pod death**

> *(restated 2026-07 — the durability promise stands but the mechanism is historical: there is no supervisor pod and resume is DB-as-state rather than git-log, since the event-driven walk re-derives the next step by replaying `pipeline.station_runs` through the definition and the per-minute reaper converges any line a crash left open — see the restated FR1 and FR6 entries)* ([validated by `transition.test.ts:217`](libs/assembly-lines/src/transition.test.ts#L217), [`assembly-run-reaper.test.ts:39`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L39))

**Actor:** System (no human)

**Flow:**
1. A `feature-request` produces an `implementation` task on a feature spec.
2. Supervisor pod starts, walks the graph: implement → validate → push → review.
3. After `[stage:implement]` and `[stage:validate]` commits land on the branch, the pod dies (preemption or OOM).
4. A replacement supervisor pod starts for the same task.
5. The replacement reads `git log` on the branch, sees the last `Lore-Stage:` is `validate`, resumes at the next graph node (`push`).
6. Task completes normally with full audit trail intact.

**Acceptance Criteria:**
- The replacement pod re-uses the existing branch (does not create a duplicate or rebase the history).
- No phase that was already committed is re-executed.
- The final PR carries the unbroken commit chain across both pods.
- `pipeline.tasks.status` and the branch state agree at termination — neither is the source of truth alone.

**Scenario 3: Code change still requires human review**

**Actor:** Reviewer (human)

**Flow:**
1. A `general` task makes a non-trivial code change in `agent/src/`.
2. Path is *not* in `auto_merge.paths`; the dark-factory policy rejects auto-merge for this path.
3. Supervisor walks the graph normally and pushes commits with stage trailers.
4. PR opens, bot review runs, posts comments, marks `APPROVED` or `CHANGES_REQUESTED`.
5. PR remains open awaiting human review.
6. Human reviews and merges (or comments, triggering review-reactor on the same branch within the same supervised flow — no new CR, no new pod hand-off).

**Acceptance Criteria:**
- The PR exists and is *not* auto-merged.
- The bot review comments are present.
- If the human comments, the response is committed by the same supervisor process or a webhook-triggered continuation, and resumes from the branch state — not from a fresh CR / Job pair.
- Final merge is done by the human.

**Scenario 4: Approval gate produces an Issue**

**Actor:** Approver (human), System

**Flow:**
1. Repo has `dark_factory.create_issue: on_gate` (the default when dark mode is on).
2. Task type has `approval_required: true` (e.g. infra change, sensitive repo).
3. System creates a GitHub Issue with full context, awaits the `approved` label.
4. No supervisor work begins until the label appears.
5. Human reviews the Issue and applies the label.
6. Supervisor begins, runs the graph normally to PR, follows the path-allowlist auto-merge rules from there.

**Acceptance Criteria:**
- An Issue exists, with the task description, links to relevant context, and a clear instruction on how to approve.
- No commits are made until the Issue is labeled.
- After approval, the flow proceeds without further human input until the next gate (or merge, if auto-merge applies).

**Scenario 5: Escalation produces an Issue with full context**

**Actor:** Maintainer (human)

**Flow:**
1. An `implementation` task fails validation twice in a row.
2. Per existing policy, the supervisor marks the task `needs-human-help`.
3. With dark mode, no Issue existed at task start. On escalation, an Issue is created on the fly with: task description, the branch link, the failing validation output, the supervisor's diagnostic, and links to the contributing facts and memories.
4. Notification fires (Slack `escalation` channel).
5. Human picks up the Issue, fixes by hand or applies guidance, can resume the task or close.

**Acceptance Criteria:**
- The escalation Issue contains everything needed to act without re-deriving the context.
- The Issue links to the branch (which carries the partial work).
- A `escalation` notification reaches the configured Slack channel.
- The task can be resumed from the current commit if the human pushes a fix to the branch.

**Scenario 6: Repo opts out of dark mode**

**Actor:** Platform Engineer

**Flow:**
1. A repo's settings have `dark_factory.enabled = false` (the migration default).
2. All flows behave exactly as today: Issue per task, status comments, no auto-merge, every PR awaits human review.

**Acceptance Criteria:**
- Repos with dark mode off see no behavior change from before this feature.
- Migration is non-destructive: enabling dark mode is a settings update, no schema migration required for opt-in repos.

**Scenario 7: PR-to-task cross-reference works without Issues**

**Actor:** Auditor / Maintainer

**Flow:**
1. Looking at a PR (auto-merged or otherwise), a maintainer wants to find the originating task.
2. PR body contains `Lore-Task: <uuid>`.
3. Web-ui task page resolves the UUID to the full task record, branch, commit log with stage breakdown, and the curated retrospective.

**Acceptance Criteria:**
- Every Lore-authored PR has a `Lore-Task: <uuid>` in its body and on the final commit's trailer.
- Web-ui resolves the UUID in one click and displays branch + commits + episode side-by-side.
- The reverse direction (task page → PR) also resolves. ([validated by `TaskDetailView.test.tsx:220`](apps/web-ui/src/app/tasks/[id]/TaskDetailView.test.tsx#L220))

## Functional Requirements

### FR1 — Branch-as-state checkpoints

- **FR1.1** Every workflow phase MUST end with a git commit containing a structured trailer block including at minimum `Lore-Stage:`, `Lore-Iteration:`, and `Lore-Task:`. Trailers are emitted unconditionally on every Lore-authored commit, regardless of the repo's `dark_factory.enabled` setting; they are the audit substrate for both dark-mode and opt-out repos. ([validated by `commit-trailers.test.ts:11`](libs/shared/src/commit-trailers.test.ts#L11), [`commit-trailers.test.ts:23`](libs/shared/src/commit-trailers.test.ts#L23); implemented by [`commit-trailers.ts:30`](libs/shared/src/commit-trailers.ts#L30))
- **FR1.1a** The structured trailer block MUST be machine-readable: `parseTrailers` round-trips a formatted block with or without extras, reads a trailer paragraph appended to a multi-paragraph commit body, tolerates CRLF line endings and trailing whitespace, and returns null when no trailer paragraph is present, a required key is missing, `Lore-Iteration` is non-numeric, the final paragraph mixes trailer and non-trailer lines, or the input is empty. ([validated by `commit-trailers.test.ts:84`](libs/shared/src/commit-trailers.test.ts#L84), [`commit-trailers.test.ts:146`](libs/shared/src/commit-trailers.test.ts#L146), [`commit-trailers.test.ts:201`](libs/shared/src/commit-trailers.test.ts#L201), [`commit-trailers.test.ts:233`](libs/shared/src/commit-trailers.test.ts#L233), [`commit-trailers.test.ts:157`](libs/shared/src/commit-trailers.test.ts#L157), [`commit-trailers.test.ts:212`](libs/shared/src/commit-trailers.test.ts#L212), [`commit-trailers.test.ts:223`](libs/shared/src/commit-trailers.test.ts#L223), [`commit-trailers.test.ts:175`](libs/shared/src/commit-trailers.test.ts#L175), [`commit-trailers.test.ts:179`](libs/shared/src/commit-trailers.test.ts#L179), [`commit-trailers.test.ts:183`](libs/shared/src/commit-trailers.test.ts#L183), [`commit-trailers.test.ts:189`](libs/shared/src/commit-trailers.test.ts#L189), [`commit-trailers.test.ts:196`](libs/shared/src/commit-trailers.test.ts#L196); implemented by [`commit-trailers.ts:65`](libs/shared/src/commit-trailers.ts#L65))
- **FR1.1b** The commit-trailers module also carries a spec→test provenance trailer: `formatValidatesTrailer` renders a `ProvenanceRef` as `Lore-Validates: <specPath>#<ordinal> -> <target>`, and `parseValidatesTrailers` reads each `Lore-Validates` line back into a `ProvenanceRef` with a numeric ordinal, round-tripping format→parse. ([validated by returns one ref with numeric ordinal for a single Lore-Validates line](libs/shared/src/commit-trailers.test.ts#L244), [renders specs/foo/spec.md#7 -> test/x.test.ts and round-trips through parseValidatesTrailers](libs/shared/src/commit-trailers.test.ts#L259); implemented by [`commit-trailers.ts`](libs/shared/src/commit-trailers.ts))
- **FR1.2** *(restated 2026-07: DB-as-state)* The next assembly line node MUST be derivable from persisted state alone, without an in-memory walker: the event-driven walk replays `pipeline.station_runs` through the definition (`nextTransition`, FR6.9). The original git-log resume described a fiction in production — the Floor's stage commits were local-only and never pushed. ([validated by `transition.test.ts:217`](libs/assembly-lines/src/transition.test.ts#L217), [`transition.test.ts:117`](libs/assembly-lines/src/transition.test.ts#L117), [`transition.test.ts:122`](libs/assembly-lines/src/transition.test.ts#L122), [`transition.test.ts:201`](libs/assembly-lines/src/transition.test.ts#L201), [`transition.test.ts:209`](libs/assembly-lines/src/transition.test.ts#L209), [`transition.test.ts:223`](libs/assembly-lines/src/transition.test.ts#L223), [`transition.test.ts:237`](libs/assembly-lines/src/transition.test.ts#L237), [`transition.test.ts:247`](libs/assembly-lines/src/transition.test.ts#L247), [`transition.test.ts:264`](libs/assembly-lines/src/transition.test.ts#L264), [`transition.test.ts:276`](libs/assembly-lines/src/transition.test.ts#L276), [`transition.test.ts:285`](libs/assembly-lines/src/transition.test.ts#L285), [`transition.test.ts:328`](libs/assembly-lines/src/transition.test.ts#L328); implemented by [`transition.ts:44`](libs/assembly-lines/src/transition.ts#L44), [a revisit numbers past the node's highest recorded iteration](libs/assembly-lines/src/transition.test.ts#L470), [`transition.test.ts:484`](libs/assembly-lines/src/transition.test.ts#L484))
- **FR1.3** *(restated 2026-07: DB-as-state)* A phase that produces no file changes still checkpoints and advances the walk — the checkpoint is the `pipeline.station_runs` row recording the node's outcome, not an empty git commit (the empty-commit mechanism retired with the in-process executor, ADR-031). ([validated by `advance.test.ts:543`](apps/floor/src/jobs/assembly-run/advance.test.ts#L543))
- **FR1.4** *(restated 2026-07: DB-as-state)* Recorded walk history is immutable: a node's outcome is written once and replayed, never rewritten — the UNIQUE `(assembly_line_id, node_id, iteration)` constraint and first-writer-wins finish (FR1.6/FR6.9) make an already-recorded node non-re-executable. The original guard was against agents rewriting on-branch stage-trailer history; the walk now pushes no stage commits, so node-row immutability is the invariant that carries the intent. ([validated by `transition.test.ts:209`](libs/assembly-lines/src/transition.test.ts#L209), [`advance.test.ts:543`](apps/floor/src/jobs/assembly-run/advance.test.ts#L543))
- **FR1.5** The `Lore-Task: <uuid>` trailer MUST also appear in the final PR body, replacing the today's `Refs #<issue>` cross-reference. `prFooter` emits `Refs #N` before `Lore-Task` only when an issue number is present, treating a null, undefined, or `0` issue number as no issue. ([validated by `pr-body.test.ts:11`](libs/shared/src/pr-body.test.ts#L11), [`pr-body.test.ts:5`](libs/shared/src/pr-body.test.ts#L5), [`pr-body.test.ts:17`](libs/shared/src/pr-body.test.ts#L17), [`pr-body.test.ts:21`](libs/shared/src/pr-body.test.ts#L21); implemented by [`pr-body.ts:10`](libs/shared/src/pr-body.ts#L10))
- **FR1.6** *(re-scoped 2026-07)* Concurrency control on the assembly-line path is structural — UNIQUE `(assembly_line_id, node_id, iteration)`, CAS node outcomes, first-writer-wins row finish, per-CR event dedupe (FR6.9) — plus a branch-keyed overlap guard: a second not-yet-started run on the same repo+branch finishes `lease_held`, deferring to the deterministically-chosen oldest open run — except for definitions whose branch is a shared workspace rather than a work identity (`comment-triage`, `code-review-reply`: distinct human comments ride one PR head branch, so guarding them would silently drop a comment; they run concurrently instead). There is no walker process left to lease. The branch-name lease backend (`lease-backends.ts`) and lease reaper remain for other consumers (the local runner's worktree mode); the reaper writes one `lease_expired` audit entry per reaped lease (branch name + previous holder, expiry ISO-stringified from a Date and passed through as a string), reporting the reaped count and writing nothing when none are past the grace cutoff. ([validated by `advance.test.ts:747`](apps/floor/src/jobs/assembly-run/advance.test.ts#L747), [`lease-reaper.test.ts:22`](apps/floor/src/main-loop/lease/lease-reaper.test.ts#L22), [`lease-reaper.test.ts:46`](apps/floor/src/main-loop/lease/lease-reaper.test.ts#L46), [`lease-reaper.test.ts:54`](apps/floor/src/main-loop/lease/lease-reaper.test.ts#L54), [`lease-reaper.test.ts:66`](apps/floor/src/main-loop/lease/lease-reaper.test.ts#L66), [`assembly-runs.test.ts:1729`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1761), [`assembly-runs.test.ts:1741`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1773); implemented by [`advance.ts:143`](apps/floor/src/jobs/assembly-run/advance.ts#L143))
- **FR1.6a** The branch-keyed lease backend (Db + File + in-memory double) MUST implement one contract: `acquire` returns `acquired:true` on an empty slot or after a prior lease has expired (reporting `tookOverFrom` on takeover) and `acquired:false` with the current holder while a lease is still valid; a default TTL of 600s applies when unspecified; `refresh` extends the expiry only for the current holder (false for a non-holder or a missing record) and preserves the existing phase via COALESCE when the phase is omitted; `release` removes the record only for the holder (false, record intact, otherwise); `reapExpired(cutoff)` deletes and returns only leases past the cutoff (empty array otherwise); branch names containing slashes are encoded correctly. ([validated by `lease-backends.test.ts:14`](libs/shared/src/project/leases/lease-backends.test.ts#L14), [`lease-backends.test.ts:32`](libs/shared/src/project/leases/lease-backends.test.ts#L32), [`lease-backends.test.ts:45`](libs/shared/src/project/leases/lease-backends.test.ts#L45), [`lease-backends.test.ts:61`](libs/shared/src/project/leases/lease-backends.test.ts#L61), [`lease-backends.test.ts:72`](libs/shared/src/project/leases/lease-backends.test.ts#L72), [`lease-backends.test.ts:87`](libs/shared/src/project/leases/lease-backends.test.ts#L87), [`lease-backends.test.ts:94`](libs/shared/src/project/leases/lease-backends.test.ts#L94), [`lease-backends.test.ts:104`](libs/shared/src/project/leases/lease-backends.test.ts#L104), [`lease-backends.test.ts:113`](libs/shared/src/project/leases/lease-backends.test.ts#L113), [`lease-backends.test.ts:122`](libs/shared/src/project/leases/lease-backends.test.ts#L122), [`lease-backends.test.ts:142`](libs/shared/src/project/leases/lease-backends.test.ts#L142), [`lease-backends.test.ts:164`](libs/shared/src/project/leases/lease-backends.test.ts#L164), [`lease-backends.test.ts:179`](libs/shared/src/project/leases/lease-backends.test.ts#L179), [`lease-backends.test.ts:188`](libs/shared/src/project/leases/lease-backends.test.ts#L188), [`lease-backends.test.ts:197`](libs/shared/src/project/leases/lease-backends.test.ts#L197), [`lease-backends.test.ts:206`](libs/shared/src/project/leases/lease-backends.test.ts#L206), [`lease-backends.test.ts:215`](libs/shared/src/project/leases/lease-backends.test.ts#L215), [`lease-backends.test.ts:221`](libs/shared/src/project/leases/lease-backends.test.ts#L221), [`lease-backends.test.ts:231`](libs/shared/src/project/leases/lease-backends.test.ts#L231), [`lease-backends.test.ts:241`](libs/shared/src/project/leases/lease-backends.test.ts#L241), [`lease-backends.test.ts:255`](libs/shared/src/project/leases/lease-backends.test.ts#L255), [`lease-backends.test.ts:275`](libs/shared/src/project/leases/lease-backends.test.ts#L275), [`lease-backends.test.ts:287`](libs/shared/src/project/leases/lease-backends.test.ts#L287); implemented by [`lease-backends.ts`](libs/shared/src/project/leases/lease-backends.ts))

### FR2 — Assembly line

- A definition's schema is STRICT: an undeclared key fails the load instead of being discarded. The schemas accepted anything and kept what they recognised, so `timeoutMinutes:` (the field is `timeout_minutes`) meant a node with no timeout, `prompt-ref:` a node with no prompt, and a dropped `iterationMax` left a back-edge unbounded — the exact runaway the cycle check exists to refuse. In every case the author's instruction was discarded in silence, with nothing anywhere to read it back. Strictness adds no field, so it does not move `definitionHash`. Because the schema and the shipped recipes must now agree, the builtin loader is held to the FILE COUNT rather than to "the catalog resolved": a per-file failure that was swallowed would leave the map short, and a test that only looks up one definition would not notice. ([validated by rejects a mistyped node key instead of discarding it](libs/assembly-lines/src/loader.test.ts#L69), [`loader.test.ts:86`](libs/assembly-lines/src/loader.test.ts#L86), [`loader.test.ts:103`](libs/assembly-lines/src/loader.test.ts#L103), [`builtin-assembly-lines.test.ts:21`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L21); implemented by [`loader.ts:43`](libs/assembly-lines/src/loader.ts#L43))

- A node's `validator` field is GONE, end to end. It was declared in the schema, set to `all` by three recipes, threaded through the Floor into a station's params and passed to the validate handler — which named the parameter `_node` and never read it. The handler detects the repo's toolchain and runs its quick checks, so `all` described the only behaviour there was; implementing selection would have been inventing a feature to justify a field rather than the reverse. Removing it changes `definitionHash`, so forks of runs started before this refuse to resume — the deliberate over-refusal the hash already documents. ([validated by carries the knobs a station pod receives as params](libs/assembly-lines/src/snapshot-graph.test.ts#L93))

- **FR2.1** Assembly line definitions MUST live as YAML files outside of TypeScript code, in a directory parallel to `scripts/task-types.yaml`. No alternate formats (DOT, JSON, custom DSL) are introduced; web-ui renders the graph from YAML directly. ([validated by `loader.test.ts:39`](libs/assembly-lines/src/loader.test.ts#L38), [`loader.test.ts:47`](libs/assembly-lines/src/loader.test.ts#L47), [`loader.test.ts:53`](libs/assembly-lines/src/loader.test.ts#L53), [`loader.test.ts:125`](libs/assembly-lines/src/loader.test.ts#L125), [`loader.test.ts:141`](libs/assembly-lines/src/loader.test.ts#L141), [`loader.test.ts:160`](libs/assembly-lines/src/loader.test.ts#L160), [`loader.test.ts:183`](libs/assembly-lines/src/loader.test.ts#L183), [`loader.test.ts:590`](libs/assembly-lines/src/loader.test.ts#L590); implemented by [`loader.ts:128`](libs/assembly-lines/src/loader.ts#L128))
- **FR2.2** An assembly line definition MUST express: nodes (typed: agent stage, validation, gate, retrospective), edges (with conditions on commit / CI / review outcomes), and entry/exit nodes. ([validated by `loader.test.ts:395`](libs/assembly-lines/src/loader.test.ts#L395), [`loader.test.ts:424`](libs/assembly-lines/src/loader.test.ts#L424), [`loader.test.ts:456`](libs/assembly-lines/src/loader.test.ts#L456), [`loader.test.ts:531`](libs/assembly-lines/src/loader.test.ts#L531), [`loader.test.ts:569`](libs/assembly-lines/src/loader.test.ts#L569); implemented by [`loader.ts:54`](libs/assembly-lines/src/loader.ts#L54))
- **FR2.3** *(restated 2026-07: shared definition, not shared executor)* The assembly line definition is a single source of truth: the same YAML (loader + builtin definitions) is consumed by both the Floor's event-driven walk (`nextTransition` over the parsed graph) and, aspirationally, the local runner. The in-process `executeAssemblyLine` that once interpreted it Floor-side is retired; there is no "GKE supervisor" process anymore. ([validated by `loader.test.ts:611`](libs/assembly-lines/src/loader.test.ts#L611), [`boundaries.test.ts:35`](libs/assembly-lines/src/boundaries.test.ts#L35), [`boundaries.test.ts:39`](libs/assembly-lines/src/boundaries.test.ts#L39), [`boundaries.test.ts:43`](libs/assembly-lines/src/boundaries.test.ts#L43), [`builtin-assembly-lines.test.ts:11`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L11), [`builtin-assembly-lines.test.ts:15`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L15), [`builtin-assembly-lines.test.ts:68`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L68), [`builtin-assembly-lines.test.ts:81`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L81); implemented by [`loader.ts:128`](libs/assembly-lines/src/loader.ts#L128), [`transition.ts:44`](libs/assembly-lines/src/transition.ts#L44))
- **FR2.4** Existing flows (implementation, gap-fill, runbook, review, feature-request, onboard, general) MUST be migratable to graph definitions without losing current behavior. ([validated by `loader.test.ts:683`](libs/assembly-lines/src/loader.test.ts#L683), [`loader.test.ts:635`](libs/assembly-lines/src/loader.test.ts#L635), [`loader.test.ts:652`](libs/assembly-lines/src/loader.test.ts#L652), [`loader.test.ts:662`](libs/assembly-lines/src/loader.test.ts#L662), [`loader.test.ts:692`](libs/assembly-lines/src/loader.test.ts#L692), [`loader.test.ts:703`](libs/assembly-lines/src/loader.test.ts#L703); implemented by [`loader.ts:138`](libs/assembly-lines/src/loader.ts#L138))
- **FR2.5** Adding a new flow MUST require only a new graph definition (YAML) plus any agent prompts it references — no engine code change; every bundled definition loads and walks through the one shared kernel. ([validated by `loader.test.ts:611`](libs/assembly-lines/src/loader.test.ts#L611), [`transition.test.ts:328`](libs/assembly-lines/src/transition.test.ts#L328))
- **FR2.6** Non-agent node types MUST execute deterministically through a command relay: the relay runs a command in the configured workdir and returns its stdout, stderr and exit code, running sequential commands in order on the same relay; the `validate` node runs the repo's detected lint/typecheck check through the relay — success when no tooling is detected or the check passes, `failed` naming the failing step otherwise; the `github_action` node maps each CI conclusion to a node outcome. ([validated by `relay-executor.test.ts:44`](libs/assembly-lines/src/relay/relay-executor.test.ts#L44), [`relay-executor.test.ts:54`](libs/assembly-lines/src/relay/relay-executor.test.ts#L54), [`relay-executor.test.ts:62`](libs/assembly-lines/src/relay/relay-executor.test.ts#L62), [`relay-executor.test.ts:70`](libs/assembly-lines/src/relay/relay-executor.test.ts#L70), [`validate-handler.test.ts:51`](libs/assembly-lines/src/validate-handler.test.ts#L51), [`validate-handler.test.ts:59`](libs/assembly-lines/src/validate-handler.test.ts#L59), [`validate-handler.test.ts:67`](libs/assembly-lines/src/validate-handler.test.ts#L67), [`validate-handler.test.ts:108`](libs/assembly-lines/src/validate-handler.test.ts#L108), [`github-action-handler.test.ts:5`](libs/assembly-lines/src/github-action-handler.test.ts#L5))
- **FR2.7** *(added 2026-07-23, #946)* The loader MUST reject, at load time, a definition in which any non-exit node lacks an edge (exact-outcome or `always`) for an outcome its node type can produce — every type produces `success` and `failed` (infrastructure failure: CR phase Failed, station timeout), and agent nodes additionally `changes_requested` (LORE_NODE_RESULT / REVIEW_RESULT verdict lines). The error names the node, the missing outcome(s), and the definition; an uncovered outcome must never surface as `nextTransition`'s runtime no-edge walk crash. ([validated by `loader.test.ts:569`](libs/assembly-lines/src/loader.test.ts#L569), [`loader.test.ts:590`](libs/assembly-lines/src/loader.test.ts#L590), [`loader.test.ts:774`](libs/assembly-lines/src/loader.test.ts#L774), [`loader.test.ts:797`](libs/assembly-lines/src/loader.test.ts#L797), [`loader.test.ts:827`](libs/assembly-lines/src/loader.test.ts#L827), [`loader.test.ts:848`](libs/assembly-lines/src/loader.test.ts#L848), [`loader.test.ts:887`](libs/assembly-lines/src/loader.test.ts#L887), [`loader.test.ts:894`](libs/assembly-lines/src/loader.test.ts#L894), [`loader.test.ts:901`](libs/assembly-lines/src/loader.test.ts#L901); implemented by [`loader.ts:91`](libs/assembly-lines/src/loader.ts#L91))

### FR3 — Opt-out human gates

- **FR3.1** Per-repo `settings.dark_factory.enabled` boolean (default `false` at migration time) MUST gate all dark-factory behavior changes. ([validated by `dark-factory-settings.test.ts:72`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L72), [`dark-factory-settings.test.ts:81`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L81), [`dark-factory-settings.test.ts:92`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L92), [`dark-factory-resolve.test.ts:8`](apps/web-ui/src/lib/dark-factory-resolve.test.ts#L8), [`dark-factory-resolve.test.ts:17`](apps/web-ui/src/lib/dark-factory-resolve.test.ts#L17), [`dark-factory-resolve.test.ts:29`](apps/web-ui/src/lib/dark-factory-resolve.test.ts#L29), [`dark-factory-resolve.test.ts:5`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L5), [`dark-factory-resolve.test.ts:14`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L14), [`dark-factory-resolve.test.ts:20`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L20), [`dark-factory-resolve.test.ts:32`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L32), [`dark-factory-resolve.test.ts:45`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L45), [`settings-pg.test.ts:36`](libs/shared/src/project/settings/settings-pg.test.ts#L36), [`settings-pg.test.ts:49`](libs/shared/src/project/settings/settings-pg.test.ts#L49), [`settings-pg.test.ts:57`](libs/shared/src/project/settings/settings-pg.test.ts#L57), [`settings-pg.test.ts:63`](libs/shared/src/project/settings/settings-pg.test.ts#L63); implemented by [`dark-factory-settings.ts:63`](libs/shared/src/dark-factory-settings.ts#L63), [`df-resolve-parity`](apps/web-ui/src/lib/dark-factory-resolve.parity.test.ts#L50), [`df-resolve-parity-defaults`](apps/web-ui/src/lib/dark-factory-resolve.parity.test.ts#L54))
- **FR3.1a** The `dark_factory` settings schema MUST validate input: it accepts an empty patch and a complete settings doc, and rejects unknown `create_issue` values, unknown trust levels, and more than 32 `auto_merge.paths`. ([validated by `dark-factory-settings.test.ts:11`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L11), [`dark-factory-settings.test.ts:15`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L15), [`dark-factory-settings.test.ts:32`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L32), [`dark-factory-settings.test.ts:38`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L38), [`dark-factory-settings.test.ts:44`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L44))
- **FR3.1b** The settings persistence port MUST resolve a repo's settings through the real resolver, read and overwrite the raw settings JSONB bound to the repo, bind the repo when setting a GitHub variable (delegating the write to the repo-config writer), and list only onboarded repos with their ingest stamp. ([validated by `settings.test.ts:13`](libs/shared/src/project/settings/settings.test.ts#L13), [`settings.test.ts:27`](libs/shared/src/project/settings/settings.test.ts#L27), [`settings.test.ts:38`](libs/shared/src/project/settings/settings.test.ts#L38), [`settings.test.ts:55`](libs/shared/src/project/settings/settings.test.ts#L55), [`settings-pg.test.ts:74`](libs/shared/src/project/settings/settings-pg.test.ts#L74))
- **FR3.2** Sub-setting `create_issue` MUST support `never | on_gate | always`. Default when dark-mode-on: `on_gate`. ([validated by `dark-factory.test.ts:51`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L51), [`dark-factory.test.ts:11`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L11), [`dark-factory.test.ts:21`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L21), [`dark-factory.test.ts:31`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L31), [`dark-factory.test.ts:41`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L41), [`dark-factory.test.ts:61`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L61), [`dark-factory.test.ts:71`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L71), [`dark-factory.test.ts:94`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L94); implemented by [`dark-factory.ts:52`](apps/floor/src/jobs/dark-factory/dark-factory.ts#L52))
- **FR3.3** Sub-setting `auto_merge` MUST express path allowlist, minimum repo trust level, CI requirement, and bot-approval requirement. Default paths: `specs/`, `adrs/`, `*.md`, `CLAUDE.md`, `.claude/`. Default min trust: `docs`. The bot-approval requirement is satisfied by the code-review line's formal verdict and reads the bot's **latest** review decision, so the per-push re-check's later `REQUEST_CHANGES` overrides an earlier `APPROVE` (and vice-versa). ([validated by `pr-policy.test.ts:98`](apps/floor/src/jobs/merge/pr-policy.test.ts#L98), [`pr-policy.test.ts:88`](apps/floor/src/jobs/merge/pr-policy.test.ts#L88), [`pr-policy.test.ts:125`](apps/floor/src/jobs/merge/pr-policy.test.ts#L125), [`pr-policy.test.ts:137`](apps/floor/src/jobs/merge/pr-policy.test.ts#L137), [`pr-policy.test.ts:147`](apps/floor/src/jobs/merge/pr-policy.test.ts#L147), [`pr-policy.test.ts:162`](apps/floor/src/jobs/merge/pr-policy.test.ts#L162), [`pr-policy.test.ts:174`](apps/floor/src/jobs/merge/pr-policy.test.ts#L174), [`pr-policy.test.ts:186`](apps/floor/src/jobs/merge/pr-policy.test.ts#L186), [`pr-policy.test.ts:198`](apps/floor/src/jobs/merge/pr-policy.test.ts#L198), [`pr-policy.test.ts:213`](apps/floor/src/jobs/merge/pr-policy.test.ts#L213), [`pr-policy.test.ts:228`](apps/floor/src/jobs/merge/pr-policy.test.ts#L228), [`pr-policy.test.ts:243`](apps/floor/src/jobs/merge/pr-policy.test.ts#L243); implemented by [`auto-merge.ts:60`](apps/floor/src/jobs/merge/auto-merge.ts#L64), [`path-match.ts:18`](libs/shared/src/path-match.ts#L18))
- **FR3.3a** The `auto_merge.paths` gate is all-or-nothing: `allPathsMatch()` admits a PR only when **every** changed path matches at least one allowlist glob — a single non-matching path in a mixed PR rejects auto-merge. Matching covers dotfile prefixes (`.claude/rules/`) and deep `specs/**` nesting, treats an empty changed-set as vacuously allowed, and treats an empty allowlist as matching nothing; the companion pattern-lister names every glob a path matched for the audit rule trace (FR3.7). ([validated by `path-match.test.ts:13`](libs/shared/src/path-match.test.ts#L13), [`path-match.test.ts:22`](libs/shared/src/path-match.test.ts#L22), [`path-match.test.ts:31`](libs/shared/src/path-match.test.ts#L31), [`path-match.test.ts:40`](libs/shared/src/path-match.test.ts#L40), [`path-match.test.ts:44`](libs/shared/src/path-match.test.ts#L44), [`path-match.test.ts:48`](libs/shared/src/path-match.test.ts#L48), [`path-match.test.ts:54`](libs/shared/src/path-match.test.ts#L54), [`path-match.test.ts:58`](libs/shared/src/path-match.test.ts#L58), [`path-match.test.ts:66`](libs/shared/src/path-match.test.ts#L66), [`path-match.test.ts:73`](libs/shared/src/path-match.test.ts#L73); implemented by [`path-match.ts:18`](libs/shared/src/path-match.ts#L18))
- **FR3.3b** The auto-merge decision engine (`evaluateAutoMerge`) MUST squash-merge only when every gate passes and otherwise defer with a specific reason — `dark_mode_off` (overrides everything), `no_changes` (empty PR, checked before the path allowlist), `human_review`, `ci_failed` (only when `require_green_ci`), `bot_changes_requested`, `path_outside_allowlist`, `trust_too_low` (including a repo with no trust set) — and it reports CI/bot-review status in the decision inputs. The trigger wrapper (`evaluateAndMerge`) resolves the PR via the facade-backed policy lookup (no direct octokit) and no-ops without writing an audit row for an orphaned task (no `target_repo`), a `dark_factory.enabled:false` or null-settings repo, or a not-yet-created PR, invoking the merge only when dark mode is on and a PR exists, and propagating merge errors to the caller. ([validated by `auto-merge.test.ts:32`](apps/floor/src/jobs/merge/auto-merge.test.ts#L32), [`auto-merge.test.ts:41`](apps/floor/src/jobs/merge/auto-merge.test.ts#L41), [`auto-merge.test.ts:47`](apps/floor/src/jobs/merge/auto-merge.test.ts#L47), [`auto-merge.test.ts:53`](apps/floor/src/jobs/merge/auto-merge.test.ts#L53), [`auto-merge.test.ts:59`](apps/floor/src/jobs/merge/auto-merge.test.ts#L59), [`auto-merge.test.ts:65`](apps/floor/src/jobs/merge/auto-merge.test.ts#L65), [`auto-merge.test.ts:71`](apps/floor/src/jobs/merge/auto-merge.test.ts#L71), [`auto-merge.test.ts:82`](apps/floor/src/jobs/merge/auto-merge.test.ts#L82), [`auto-merge.test.ts:88`](apps/floor/src/jobs/merge/auto-merge.test.ts#L88), [`auto-merge.test.ts:96`](apps/floor/src/jobs/merge/auto-merge.test.ts#L96), [`auto-merge.test.ts:107`](apps/floor/src/jobs/merge/auto-merge.test.ts#L107), [`auto-merge.test.ts:126`](apps/floor/src/jobs/merge/auto-merge.test.ts#L126), [`auto-merge.test.ts:139`](apps/floor/src/jobs/merge/auto-merge.test.ts#L139), [`auto-merge-trigger.test.ts:52`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L52), [`auto-merge-trigger.test.ts:60`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L60), [`auto-merge-trigger.test.ts:69`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L69), [`auto-merge-trigger.test.ts:77`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L77), [`auto-merge-trigger.test.ts:88`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L88), [`auto-merge-trigger.test.ts:135`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L135), [`auto-merge-trigger.test.ts:145`](apps/floor/src/jobs/merge/auto-merge-trigger.test.ts#L145); implemented by [`auto-merge.ts:60`](apps/floor/src/jobs/merge/auto-merge.ts#L64))
- **FR3.4** Sub-setting `review` MUST support `trust_based | always | never`. Default when dark-mode-on: `trust_based`. When `trust_based` is active and a PR's changed paths are *outside* the configured `auto_merge.paths` allowlist, the bot MUST post its inline review comments and verdict and then stop; the PR remains open awaiting human merge. Time-based "no-objection" auto-merge is explicitly out of scope for v1. ([validated by `dark-factory.test.ts:182`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L182), [`dark-factory.test.ts:104`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L104), [`dark-factory.test.ts:111`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L111), [`dark-factory.test.ts:128`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L128), [`dark-factory.test.ts:137`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L137), [`dark-factory.test.ts:146`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L146), [`dark-factory.test.ts:155`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L155), [`dark-factory.test.ts:164`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L164), [`dark-factory.test.ts:173`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L173); implemented by [`dark-factory.ts:100`](apps/floor/src/jobs/dark-factory/dark-factory.ts#L101))
- **FR3.5** Sub-setting `notify` MUST support a list of channels: `escalation`, `watched`, `all`. Default when dark-mode-on: `[escalation]`. ([validated by `notify.test.ts:22`](libs/shared/src/project/notify/notify.test.ts#L22), [`notify.test.ts:35`](libs/shared/src/project/notify/notify.test.ts#L35), [`notify-decision.test.ts:10`](libs/shared/src/project/notify/notify-decision.test.ts#L10), [`notify-decision.test.ts:17`](libs/shared/src/project/notify/notify-decision.test.ts#L17), [`notify-decision.test.ts:24`](libs/shared/src/project/notify/notify-decision.test.ts#L24), [`notify-decision.test.ts:35`](libs/shared/src/project/notify/notify-decision.test.ts#L35), [`notify-slack.test.ts:10`](libs/shared/src/project/notify/notify-slack.test.ts#L10), [`notify-slack.test.ts:24`](libs/shared/src/project/notify/notify-slack.test.ts#L24); implemented by [`notify-decision.ts:13`](libs/shared/src/project/notify/notify-decision.ts#L13))
- **FR3.6** Per-task overrides at creation time MUST be able to force `human_review: required` or `with_issue: true` for a single task without changing repo settings. (The `notify: completion` per-task override listed in the original draft was not built in v1 — per-task notify control is deferred; repo-level `notify` channels remain the mechanism.) ([validated by `dark-factory.test.ts:11`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L11), [`dark-factory.test.ts:128`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L128))
- **FR3.7** Auto-merge decisions MUST be recorded in the audit log with the rule that justified them (path matched, trust level, CI status, bot-approval). ([validated by `audit.test.ts:6`](apps/floor/src/jobs/lib/audit.test.ts#L6), [`auto-merge.test.ts:119`](apps/floor/src/jobs/merge/auto-merge.test.ts#L113), [`audit.test.ts:23`](libs/shared/src/project/audit/audit.test.ts#L23), [`audit.test.ts:44`](libs/shared/src/project/audit/audit.test.ts#L44), [`audit-memory.test.ts:5`](libs/shared/src/project/audit/audit-memory.test.ts#L5); implemented by [`auto-merge.ts:60`](apps/floor/src/jobs/merge/auto-merge.ts#L64), [`audit.ts:9`](apps/floor/src/jobs/lib/audit.ts#L9))
- **FR3.8** Issues created on escalation MUST contain: task description, branch link, failing phase output (if any), diagnostic from the supervisor, and links to contributing facts/memories. ([validated by includes branch link, commit log link, diagnostic](libs/shared/src/escalation/escalation-body.test.ts#L5), [`escalation-body.test.ts:25`](libs/shared/src/escalation/escalation-body.test.ts#L25); [`infra-failure.test.ts:23`](apps/floor/src/jobs/platform/infra-failure.test.ts#L23), [`k8s-pod-failure.test.ts:23`](libs/shared/src/k8s-pod-failure.test.ts#L23); implemented by [`escalation-body.ts:11`](libs/shared/src/escalation/escalation-body.ts#L11))

- **FR3.9** Authorization on `dark_factory.*` settings MUST be tiered. Privileged changes — toggling `dark_factory.enabled` and modifying `dark_factory.auto_merge.paths` — MUST require both an admin-scope API token and a CODEOWNERS approval recorded in the audit log (a labeled PR against the settings, or an equivalent ceremony surfaced via the web-ui). Lighter sub-settings (`notify`, `create_issue`, `review`, `auto_merge.min_trust`, `auto_merge.require_*`) MAY be changed with admin scope alone. Every mutation, regardless of tier, MUST write an audit_log entry naming the actor, the previous value, the new value, and the authorization path used. ([validated by `dark-factory.test.ts:250`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L250), [`dark-factory-settings.test.ts:108`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L108), [`dark-factory-settings.test.ts:113`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L113), [`dark-factory-settings.test.ts:119`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L119), [`dark-factory-settings.test.ts:128`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L128), [`dark-factory-settings.test.ts:137`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L137), [`dark-factory-settings.test.ts:147`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L147), [`dark-factory-settings.test.ts:206`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L206), [`dark-factory-settings.test.ts:215`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L215), [`dark-factory.test.ts:178`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L178), [`dark-factory.test.ts:197`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L197), [`dark-factory.test.ts:239`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L239), [`dark-factory.test.ts:258`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L258), [`dark-factory.test.ts:351`](apps/lore-api/src/api/routes/dark-factory/dark-factory.test.ts#L351); implemented by [`dark-factory-authz.ts:69`](apps/lore-api/src/features/dark-factory/dark-factory-authz.ts#L73), [`dark-factory-settings.ts:63`](apps/lore-api/src/features/dark-factory/dark-factory-settings.ts#L63), [`escalation-step.test.ts:100`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L100), [`escalation-step.test.ts:122`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L122))

- **FR3.9a** The execution image is a two-key security-boundary field. It MUST resolve to the platform default when no execution settings are present (or settings is null), to the per-repo `dark_factory.execution.image` when set, and to a per-task-type override in preference to the per-repo image — applying that override only to its own task type. The schema accepts an execution image (per-repo and per-task-type) and rejects an empty one; `twoKeyFieldsTouched` flags `execution.image` and a per-task-type `task_overrides` execution image while not flagging non-execution `task_overrides` fields. ([validated by `dark-factory-settings.test.ts:8`](libs/shared/src/dark-factory-settings.test.ts#L8), [`dark-factory-settings.test.ts:14`](libs/shared/src/dark-factory-settings.test.ts#L14), [`dark-factory-settings.test.ts:20`](libs/shared/src/dark-factory-settings.test.ts#L20), [`dark-factory-settings.test.ts:28`](libs/shared/src/dark-factory-settings.test.ts#L28), [`dark-factory-settings.test.ts:41`](libs/shared/src/dark-factory-settings.test.ts#L41), [`dark-factory-settings.test.ts:52`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L52), [`dark-factory-settings.test.ts:58`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L58), [`dark-factory-settings.test.ts:64`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L64), [`dark-factory-settings.test.ts:158`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L158), [`dark-factory-settings.test.ts:164`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L164), [`dark-factory-settings.test.ts:173`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L173), [`dark-factory-settings.test.ts:184`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L184), [`dark-factory-settings.test.ts:198`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L198); implemented by [`dark-factory-settings.ts:63`](libs/shared/src/dark-factory-settings.ts#L63))

- **FR3.10** *(added 2026-08-25)* Escalation runs as an assembly line, not as a function call. Its steps are recorded visits, so filing the Issue and telling the humans are each visible and each retried by the engine rather than by bespoke backoff. Both steps route BOTH outcomes forward: a failure to reach the Issue surface is precisely when the notification must still go, carrying the whole diagnostic — what used to be a catch block reaching for an audit-only fallback is now an edge. The line is keyed by the TASK, because more than one thing can notice the same failure and two noticers must produce one Issue, not two at the same person; and it is capped, because a task that stays failed is noticed again on every sweep and an uncapped start would tell the human again every time. ([validated by notifies even when filing the issue failed, which is when it matters most](libs/assembly-lines/src/escalation-line.test.ts#L26), [`escalation-line.test.ts:21`](libs/assembly-lines/src/escalation-line.test.ts#L21), [`escalation-line.test.ts:30`](libs/assembly-lines/src/escalation-line.test.ts#L30), [`escalation-line.test.ts:37`](libs/assembly-lines/src/escalation-line.test.ts#L37), [`escalation-step.test.ts:27`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L27), [`escalation-step.test.ts:36`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L36), [`escalation-step.test.ts:51`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L51), [`escalation-step.test.ts:74`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L74), [`start-escalation-line.test.ts:18`](libs/shared/src/escalation/start-escalation-line.test.ts#L18), [`start-escalation-line.test.ts:45`](libs/shared/src/escalation/start-escalation-line.test.ts#L45), [`start-escalation-line.test.ts:55`](libs/shared/src/escalation/start-escalation-line.test.ts#L55), [`escalation-step.test.ts:137`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L137), [`escalation-step.test.ts:153`](apps/stations/src/stations/escalation-step/escalation-step.test.ts#L153))

### FR4 — Migration and compatibility

- **FR4.1** All existing repos MUST default to `dark_factory.enabled = false` at migration; behavior is identical to pre-feature. ([validated by `dark-factory-settings.test.ts:72`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L72), [`dark-factory.test.ts:94`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L94))
- **FR4.2** Enabling or reverting dark mode is a `settings.dark_factory` JSONB write — the config lives on the `lore.repos.settings` column and resolves through `resolveDarkFactorySettings`, so opting in needs no dedicated table and no schema migration. ([validated by `settings-pg.test.ts:36`](libs/shared/src/project/settings/settings-pg.test.ts#L36), [`dark-factory-settings.test.ts:72`](apps/lore-api/src/features/dark-factory/dark-factory-settings.test.ts#L72))
- **FR4.3** A repo can revert to `dark_factory.enabled = false` at any time; subsequent tasks behave as today. ([validated by `dark-factory.test.ts:94`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L94), [`dark-factory.test.ts:104`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L104))
- **FR4.4** A task's flow is resolved from the repo's current `dark_factory.enabled` at decision time, so dark mode applies only to tasks that decide after enablement and any in-flight task completes on its original (legacy) flow. ([validated by `dark-factory.test.ts:94`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L94), [`dark-factory.test.ts:104`](apps/floor/src/jobs/dark-factory/dark-factory.test.ts#L104), [`dark-factory-resolve.test.ts:5`](apps/floor/src/jobs/dark-factory/dark-factory-resolve.test.ts#L5))

### FR5 — Observability

- **FR5.1** Every auto-merge decision is emitted as a `lore.auto_merge.decision` OTEL span carrying its rule trace — the decision outcome, path-match count, trust level, CI status, and bot-review state (this replaced the retired per-phase span-linked-to-commit-SHA model, ADR-031). ([validated by `auto-merge-span.test.ts:53`](apps/floor/src/jobs/merge/auto-merge-span.test.ts#L53), [`auto-merge-span.test.ts:63`](apps/floor/src/jobs/merge/auto-merge-span.test.ts#L63))
- **FR5.1a** Agent and station NDJSON telemetry is projected into `pipeline.llm_calls` — model, input/output tokens, cost, and duration per task and Agent CR — for cost observability. ([validated by `agent-events.test.ts:16`](apps/floor/src/jobs/agent/agent-events.test.ts#L16), [`agent-events.test.ts:229`](apps/floor/src/jobs/agent/agent-events.test.ts#L229))
- **FR5.1b** A terminal `result` event that reports its usage under `stats.*` instead of `usage.*` (the ai-agent-subsystem's `gemini` vendor, confirmed against a real run — it has no `total_cost_usd` and nests tokens differently than Claude Code's shape) still projects to a `pipeline.llm_calls` row: tokens are read from either parent key, and a model with no self-reported cost is priced from its own tokens rather than logged at zero. ([validated by `agent-events.test.ts:41`](apps/floor/src/jobs/agent/agent-events.test.ts#L41), [`agent-events.test.ts:72`](apps/floor/src/jobs/agent/agent-events.test.ts#L72), [`agent-events.test.ts:95`](apps/floor/src/jobs/agent/agent-events.test.ts#L95))

- **FR5.2** A repo dashboard view (web-ui) MUST surface: tasks run dark this week, tasks auto-merged, tasks escalated, current trust level, current `dark_factory` settings. ([validated by `RepoOverviewView.test.tsx:129`](apps/web-ui/src/app/repos/[owner]/[repo]/RepoOverviewView.test.tsx#L129), [`DarkFactoryConsoleView.test.tsx:26`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/DarkFactoryConsoleView.test.tsx#L26), [`DarkFactoryConsoleView.test.tsx:33`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/DarkFactoryConsoleView.test.tsx#L33), [`DarkFactoryConsoleView.test.tsx:46`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/DarkFactoryConsoleView.test.tsx#L46), [`DarkFactoryView.test.tsx:32`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/settings/DarkFactoryView.test.tsx#L32), [`DarkFactoryView.test.tsx:56`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/settings/DarkFactoryView.test.tsx#L56), [`DarkFactoryView.test.tsx:66`](apps/web-ui/src/app/repos/[owner]/[repo]/dark-factory/settings/DarkFactoryView.test.tsx#L66), [`AssemblyRunListView.test.tsx:26`](apps/web-ui/src/app/assembly-runs/AssemblyRunListView.test.tsx#L26), [`AssemblyRunListView.test.tsx:42`](apps/web-ui/src/app/assembly-runs/AssemblyRunListView.test.tsx#L42), [`AssemblyRunsTable.test.tsx:35`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L35), [`AssemblyRunsTable.test.tsx:41`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L41), [`AssemblyRunsTable.test.tsx:62`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L62), [`AssemblyRunsTable.test.tsx:83`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L83), [`AssemblyRunsTable.test.tsx:180`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L180), [`AssemblyRunsTable.test.tsx:202`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L202), [`AssemblyRunsTable.test.tsx:208`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L208))
- **FR5.3** The web-ui MUST resolve `Lore-Task: <uuid>` from a PR URL and render the branch's per-node timeline on the run detail page (the task page routes there via its runs list, #1608). ([validated by `AssemblyRunView.test.tsx:28`](apps/web-ui/src/app/assembly-runs/[id]/AssemblyRunView.test.tsx#L28), [`AssemblyRunView.test.tsx:41`](apps/web-ui/src/app/assembly-runs/[id]/AssemblyRunView.test.tsx#L41), [`AssemblyRunView.test.tsx:51`](apps/web-ui/src/app/assembly-runs/[id]/AssemblyRunView.test.tsx#L51), [`RunNodeDetail.test.tsx:136`](apps/web-ui/src/app/assembly-runs/[id]/RunNodeDetail.test.tsx#L136), [`AssemblyRunView.test.tsx:67`](apps/web-ui/src/app/assembly-runs/[id]/AssemblyRunView.test.tsx#L67), [`NodeLogPanel.test.tsx:56`](apps/web-ui/src/app/assembly-runs/[id]/NodeLogPanel.test.tsx#L56), [`node-pod-logs-presenter.test.ts:20`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L20), [`node-pod-logs-presenter.test.ts:28`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L28), [`node-pod-logs-presenter.test.ts:32`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L32), [`node-pod-logs-presenter.test.ts:36`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L36), [`node-pod-logs-presenter.test.ts:40`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L40), [`node-pod-logs-presenter.test.ts:48`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L48), [`node-pod-logs-presenter.test.ts:52`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L52), [`node-pod-logs-presenter.test.ts:56`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L56), [`node-pod-logs-presenter.test.ts:60`](apps/web-ui/src/app/assembly-runs/[id]/node-pod-logs-presenter.test.ts#L60); implemented by [`task-timeline.ts:62`](apps/lore-api/src/api/routes/tasks/task-timeline.ts#L66))

- **FR5.4** The web-ui MUST provide the intent-definition surface: a create-task form exposing the four task types, a target-repo selector (a dropdown option per onboarded repo, falling back to a free-text input defaulting to `re-cinq/lore` when no repos exist), an immediate-priority checkbox carrying value `immediate`, a required description textarea, and the heading + Create Task submit wired to the injected create-task action. ([validated by `AssemblyRunCreateView.test.tsx:9`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L9), [`AssemblyRunCreateView.test.tsx:21`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L21), [`AssemblyRunCreateView.test.tsx:63`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L63), [`AssemblyRunCreateView.test.tsx:76`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L76), [`AssemblyRunCreateView.test.tsx:90`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L90), [`AssemblyRunCreateView.test.tsx:103`](apps/web-ui/src/app/assembly-runs/create/AssemblyRunCreateView.test.tsx#L103))

### FR6 — Assembly line identity

- **FR6.1** Every assembly line execution MUST have a first-class per-attempt identity: a `pipeline.assembly_runs` row with a fresh uuid, distinct across retries and resumes of the same task. The task id remains stable across attempts; the assemblyLineId does not. ([validated by `assembly-lines.test.ts:247`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L255); implemented by [`0025_assembly_lines.sql`](infra/terraform/modules/gke-mcp/lore-platform/charts/ui-helm/migrations/0025_assembly_lines.sql))
- **FR6.1a** The `pipeline.assembly_runs` row supports the full attempt lifecycle (Pg adapter and in-memory double sharing one behavior): `markRunning` stamps `running` + `started_at` guarded against terminal rows (throwing on unknown ids), `finish` stamps `finished`/`failed` with the outcome, reason (on `error`), and `finished_at`; `getById` maps the row to an `AssemblyLineRecord` (null args → empty object) and returns null for unknown ids; `listForTask` returns only that task's rows newest-first; `findOpenByPr` matches repo + `args.pr_number` on open statuses only (excluding finished rows and other PRs) and `finishOpenByPr` closes only those and returns a `ClosedRunRef` per line it closed (the refs, not a count: closing here bypasses `finishLine`, so this is the only moment that knows which lines lost their token reclaim) — the PR-keyed lookup that lets code-review reuse an open line. ([validated by `assembly-lines.test.ts:73`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L88), [`assembly-line-reads.test.ts:85`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L100), [`assembly-line-reads.test.ts:106`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L121), [`assembly-line-reads.test.ts:137`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L137), [`assembly-line-reads.test.ts:177`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L177), [`assembly-line-reads.test.ts:183`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L183), [`assembly-line-reads.test.ts:207`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L207), [`assembly-line-reads.test.ts:217`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L217), [`assembly-line-reads.test.ts:228`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L228), [`assembly-line-reads.test.ts:264`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L272), [`assembly-line-reads.test.ts:264`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L272), [`assembly-line-reads.test.ts:550`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L558), [`assembly-line-reads.test.ts:613`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L621), [`assembly-line-reads.test.ts:613`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L621), [`assembly-line-reads.test.ts:635`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L643), [`assembly-line-reads.test.ts:663`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L671), [`assembly-line-reads.test.ts:691`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L699), [`assembly-line-reads.test.ts:790`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L800), [`assembly-line-reads.test.ts:790`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L800), [`assembly-line-reads.test.ts:1033`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1045); implemented by [`assembly-lines-pg.ts:26`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L26))
- **FR6.2** `project.assemblyRuns.start(definitionName, opts)` MUST mint the assemblyLineId, persist the row (status `queued`), and insert the `assembly_run.start` event (source `internal`, dedupe key `assembly_run.start:<assemblyLineId>`, denormalized `repo` column) in one atomic statement, returning the id immediately — execution is picked up by the Floor event loop, never awaited by the caller. ([validated by `assembly-lines.test.ts:27`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L27), [`project.test.ts:73`](libs/shared/src/project/lib/project.test.ts#L73), [`assembly-line-reads.test.ts:57`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L57), [`assembly-line-reads.test.ts:765`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L775), [`assembly-run-station-backend.test.ts:18`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L18), [`assembly-run-station-backend.test.ts:111`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L111); implemented by [`assembly-lines-pg.ts:26`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L26))
- **FR6.3** Every node execution MUST be associated with its assemblyLineId: a `pipeline.station_runs` row records the node id, iteration, outcome, Agent CR name, and stage-commit sha. ([validated by `assembly-lines.test.ts:567`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L575); implemented by [`assembly-lines-pg.ts:180`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L180))
- **FR6.4** *(re-scoped 2026-07)* Lore-authored commits (agent pods) carry the `Lore-Assembly-Run: <id>` trailer alongside `Lore-Task`; both are optional on parse (task-less lines omit `Lore-Task`). Floor-side stage commits retired with the in-process walk — the per-node record is the `pipeline.station_runs` row (FR6.3), not a commit. `Lore-Assembly-Run` is emitted after the required keys only when an assemblyLineId is set and is omitted otherwise, and it round-trips through `parseTrailers` as a first-class field (not an extra), with pre-existing task-less blocks parsing back to an empty `taskId`. ([validated by `commit-trailers.test.ts:68`](libs/shared/src/commit-trailers.test.ts#L68), [`commit-trailers.test.ts:44`](libs/shared/src/commit-trailers.test.ts#L44), [`commit-trailers.test.ts:58`](libs/shared/src/commit-trailers.test.ts#L58), [`commit-trailers.test.ts:91`](libs/shared/src/commit-trailers.test.ts#L91), [`commit-trailers.test.ts:119`](libs/shared/src/commit-trailers.test.ts#L119), [`commit-trailers.test.ts:132`](libs/shared/src/commit-trailers.test.ts#L132); implemented by [`commit-trailers.ts:34`](libs/shared/src/commit-trailers.ts#L34))
- **FR6.5** Agent CR names MUST key on the assemblyLineId, not the task id — `<assemblyLineId:12>-<nodeId>` (a revisited node appends `-<iteration>`; the 12-hex prefix is also the telemetry correlation key, #907) — so two attempts of one task never collide on a CR. The CR spec keeps the `taskId` field (the watcher/reaper probe by task-id label). ([validated by `floor-assembly-run.test.ts:29`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L29), [`floor-assembly-run.test.ts:56`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L56), [`floor-assembly-run.test.ts:62`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L62), [`floor-assembly-run.test.ts:77`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L77), [`floor-assembly-run.test.ts:138`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L138); implemented by [`floor-assembly-run.ts:32`](apps/floor/src/jobs/assembly-run/floor-assembly-run.ts#L32))
- **FR6.6** *(restated 2026-07)* Every node execution — including back-edge re-iterations — MUST be recorded in `pipeline.station_runs`: `ensureNodeStart` before the CR launches (with the CR name), `finishNodeOnce` at its terminal outcome. The rows ARE the walk state (FR6.9), not an observability side-channel. ([validated by `assembly-lines.test.ts:835`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L845), [`advance.test.ts:313`](apps/floor/src/jobs/assembly-run/advance.test.ts#L313), [`assembly-line-reads.test.ts:583`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L591), [`assembly-line-reads.test.ts:812`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L822), [`assembly-line-reads.test.ts:856`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L868), [`assembly-line-reads.test.ts:882`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L894), [`assembly-line-reads.test.ts:901`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L913), [`assembly-line-reads.test.ts:920`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L932), [`assembly-line-reads.test.ts:947`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L959), [`assembly-line-reads.test.ts:1018`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1030), [`assembly-line-reads.test.ts:962`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L974), [`assembly-line-reads.test.ts:994`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1006), [`assembly-line-reads.test.ts:1006`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1018), [`assembly-line-reads.test.ts:1033`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1045); implemented by [`assembly-lines-pg.ts:169`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L169), [`advance.ts:221`](apps/floor/src/jobs/assembly-run/advance.ts#L245))

- **FR6.7** *(rewritten 2026-07: event-driven walk)* The `assembly_run.start` handler MUST be the sole executor entry: it validates the definition (a task-less row with an unknown definition closes as failed and resolves — configuration errors never retry), marks the row running, and routes by definition shape (detect-shaped definitions to the repo-less detect runner; single-CR rows — task-backed, no builtin definition — mark running and return, per FR6.8; every other definition launches the ENTRY node's Agent CR and returns). It neither clones, nor supervises, nor backgrounds a walk — the walk advances per FR6.9, so a Floor restart loses nothing. A launch failure propagates so the event loop retries (the advance path is idempotent end to end). ([validated by `start-event-handler.test.ts:125`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L125), [`start-event-handler.test.ts:149`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L149), [`start-event-handler.test.ts:164`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L164), [`start-event-handler.test.ts:176`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L176), [`start-event-handler.test.ts:137`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L137), [`start-event-handler.test.ts:229`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L229); implemented by [`start-event-handler.ts:39`](apps/floor/src/jobs/assembly-run/start-event-handler.ts#L39), [`advance.ts:110`](apps/floor/src/jobs/assembly-run/advance.ts#L110))

- **FR6.8** Every Agent-CR task execution MUST have a run row — single-CR task types (onboard / review / runbook: no builtin assembly line) also create a `pipeline.assembly_runs` row at launch (`task_id` set), so the run table is the total execution history. `shouldUseAssemblyLine` routes an assembly-line-having task type (implementation/general/gap-fill) to the line backend and the rest to the single-Agent path; the assembly-line branch does not double-create a row (its `start()` lives in the line backend), and `isActive` probes the cluster's CRs by task-id label, which finds a single Agent and a line's per-node Agents alike. ([validated by routes assemblyLine-having task types to the assembly line, others to single-Agent](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L94), [`agent-cr-station-backend.test.ts:74`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L77), [`agent-cr-station-backend.test.ts:79`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L84), [`agent-cr-station-backend.test.ts:201`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L206), [`agent-cr-station-backend.test.ts:209`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L214), [`start-event-handler.test.ts:219`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L219))
- **FR6.8a** *(2026-08-29)* A single-CR task is DISPATCHED the same way a node is: its one visit is written as a `queued` `pipeline.station_runs` row tagged `node:agent` (plus the repo's `station_default_tags`) and armed with the complete `LoreTaskSpec`, for a cluster-agent to claim — the Floor pushes no CR. The row records the CR name the spec carries, so the watch's terminal report and the reconcile pass correlate by one value; it records what the visit was dispatched with, since the CR that held the prompt is pruned; a crash-recovery re-dispatch converges on the one row rather than minting a second; and a row another cluster already claimed is not re-armed. ([validated by enqueues a single-CR task as a claimable row instead of pushing a CR](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L108), [`agent-cr-station-backend.test.ts:121`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L126), [`agent-cr-station-backend.test.ts:145`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L150), [`agent-cr-station-backend.test.ts:160`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L165), [`agent-cr-station-backend.test.ts:169`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L174), [`agent-cr-station-backend.test.ts:184`](apps/floor/src/jobs/station/agent-cr-station-backend.test.ts#L189))
- **FR6.8b** *(2026-08-29)* The agent-watcher closes a single-CR run from the task's post-handler status (`pr-created`/`review` → `pr_created`, `failed`/`needs-human-help` → `failed`, `completed` → `completed`), falling back to the reported phase for an un-advanced task (a `Failed` run whose task is still `running` closes `failed`, not `completed`), and closes its open station visit in the station vocabulary rather than the run's. Token reclaim keys on the task type's routing, not on row existence. Because the visit is now claimable, the reaper's definition-less arm bounds it: queued past the wait fails terminally naming the unmatched `required_tags`, and a claim held by an offline cluster is requeued with a `cluster_agent_offline` audit entry — while a claimed visit's execution timeout stays the watcher's, which hears the terminal event. ([validated by closes the single-CR run's open station row, not just the run](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L506), [`assembly-run-reaper.test.ts:472`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L472), [`assembly-run-reaper.test.ts:487`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L487), [`assembly-run-reaper.test.ts:530`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L530), [`assembly-run-reaper.test.ts:566`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L566), [`assembly-run-reaper.test.ts:1147`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L1147), [`agent-watcher-logic.test.ts:90`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L90), [`agent-watcher-logic.test.ts:270`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L270); implemented by [`agent-cr-station-backend.ts:63`](apps/floor/src/jobs/station/agent-cr-station-backend.ts#L63), [`agent-watcher.ts:232`](apps/floor/src/jobs/watcher/agent-watcher.ts#L232))

- **FR6.9** The walk MUST be event-driven and idempotent: node CRs carry `lore.re-cinq.com/assembly-run-id` (full uuid; pre-flip CRs carry the legacy `assembly-line-id` spelling, still read) + `node-id` + `iteration` labels; their terminal phases produce `kubernetes.agent_node.{succeeded,failed}` events deduped PER CR (`k8s:<crName>:<phase>` — all node CRs of one line share the synthetic task-id label, so a task-keyed dedupe would swallow every node after the first). CR names embed the iteration on revisits (`<assemblyLineId:12>-<nodeId>`, `-<iteration>` appended when iteration > 1) so a revisited node runs a fresh pod rather than 409-reusing the prior iteration's terminal CR. The transition handler parses the outcome (LORE_NODE_RESULT → REVIEW_RESULT → phase precedence), records it via compare-and-set, and re-derives the next step purely from the persisted node rows (`nextTransition` replays the executor's exact edge/iteration_max routing). Duplicate and concurrent transitions MUST converge structurally: UNIQUE `(assembly_line_id, node_id, iteration)`, CAS node outcome, first-writer-wins row finish, 409-idempotent CR creates. The single-agent watcher path MUST never process labeled node CRs. *(restated 2026-08-21, #1469)* A `LORE_NODE_RESULT` marker that is PRESENT but says nothing usable resolves `failed` carrying the malformed line, never the `success` default — that default exists for an agent which prints NO marker, and an agent that spoke and was misheard is the opposite case: a station's `changes_requested` became a silent success and the walk skipped the human decision point its edge exists for. The legacy bare-word form parses as valid, because a deployed recipe taught it; only LINE-START markers count and the LAST one wins, so an agent may quote or explain its own contract without deciding the node. ([validated by `k8s-map.test.ts:23`](libs/shared/src/project/events/k8s-map.test.ts#L59), [`node-event-handler.test.ts:146`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L146), [`advance.test.ts:418`](apps/floor/src/jobs/assembly-run/advance.test.ts#L418), [`transition.test.ts:292`](libs/assembly-lines/src/transition.test.ts#L292), [`advance.test.ts:430`](apps/floor/src/jobs/assembly-run/advance.test.ts#L430), [`advance.test.ts:442`](apps/floor/src/jobs/assembly-run/advance.test.ts#L442), [`advance.test.ts:460`](apps/floor/src/jobs/assembly-run/advance.test.ts#L460), [`advance.test.ts:505`](apps/floor/src/jobs/assembly-run/advance.test.ts#L505), [`advance.test.ts:543`](apps/floor/src/jobs/assembly-run/advance.test.ts#L543), [`advance.test.ts:567`](apps/floor/src/jobs/assembly-run/advance.test.ts#L567), [`advance.test.ts:771`](apps/floor/src/jobs/assembly-run/advance.test.ts#L771), [`advance.test.ts:950`](apps/floor/src/jobs/assembly-run/advance.test.ts#L950), [`advance.test.ts:857`](apps/floor/src/jobs/assembly-run/advance.test.ts#L857), [`advance.test.ts:1101`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1101), [`node-event-handler.test.ts:306`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L306), [`node-event-handler.test.ts:377`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L377), [`node-event-handler.test.ts:387`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L387), [`node-event-handler.test.ts:405`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L405), [`node-outcome.test.ts:35`](libs/assembly-lines/src/node-outcome.test.ts#L34), [`node-outcome.test.ts:42`](libs/assembly-lines/src/node-outcome.test.ts#L41), [`node-outcome.test.ts:49`](libs/assembly-lines/src/node-outcome.test.ts#L48), [`node-outcome.test.ts:62`](libs/assembly-lines/src/node-outcome.test.ts#L61), [`node-outcome.test.ts:94`](libs/assembly-lines/src/node-outcome.test.ts#L94), [`node-outcome.test.ts:111`](libs/assembly-lines/src/node-outcome.test.ts#L111), [`node-outcome.test.ts:205`](libs/assembly-lines/src/node-outcome.test.ts#L205), [`node-outcome.test.ts:212`](libs/assembly-lines/src/node-outcome.test.ts#L212), [`node-outcome.test.ts:225`](libs/assembly-lines/src/node-outcome.test.ts#L225), [`node-outcome.test.ts:231`](libs/assembly-lines/src/node-outcome.test.ts#L231), [`node-outcome.test.ts:242`](libs/assembly-lines/src/node-outcome.test.ts#L242), [`node-outcome.test.ts:252`](libs/assembly-lines/src/node-outcome.test.ts#L252), [`node-outcome.test.ts:262`](libs/assembly-lines/src/node-outcome.test.ts#L262), [`node-outcome.test.ts:272`](libs/assembly-lines/src/node-outcome.test.ts#L272); implemented by [`advance.ts:110`](apps/floor/src/jobs/assembly-run/advance.ts#L110), [`node-event-handler.ts:24`](apps/floor/src/jobs/assembly-run/node-event-handler.ts#L24), [`k8s-map.ts:37`](libs/shared/src/project/events/k8s-map.ts#L37), [`advance.test.ts:1778`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1778))

- **FR6.10** A per-minute reaper (`cron.assembly_line_reaper.tick`) MUST converge every open line — it is the ONLY recovery for a dead-lettered transition (dedupe rows make reconcile re-emits permanent no-ops): an open node whose CR is terminal resolves its real outcome; a rowed-but-unlaunched node relaunches (409 no-op if the CR exists) — *(amended 2026-08-21, #1466)* but ONLY on true absence and only past a two-minute startup grace: the read boundary answers `Pending` for a CR that exists without a status, because a born-but-unstamped CR read as absence made the reaper relaunch over a LIVE pod every tick, and the row is minted BEFORE the CR create so a tick can legitimately land between them. A relaunch builds its spec with the SAME builder the first dispatch uses (`nodeLaunchSpec`); a second field-by-field builder is how the conversation and the round content went missing on this door; a node past `timeout_minutes` (+2 min buffer, default 62) fails as a timeout and transitions; a row queued >30 min fails with a reason; a running row with no open node re-advances (the replay converges); a single-CR (definition-less) row whose backing task is terminal is closed from that task's status. Every open line either progresses or terminally fails — bounded. ([validated by waits out the startup grace before relaunching a missing CR](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L241), [`assembly-run-reaper.test.ts:235`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L235), [`agent-node-status.test.ts:6`](libs/shared/src/cluster/agent-node-status.test.ts#L6), [`agent-node-status.test.ts:12`](libs/shared/src/cluster/agent-node-status.test.ts#L12), [`assembly-run-reaper.test.ts:39`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L39), [`assembly-run-reaper.test.ts:334`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L334), [`assembly-run-reaper.test.ts:104`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L104), [`assembly-run-reaper.test.ts:116`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L116), [`assembly-run-reaper.test.ts:132`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L132), [`assembly-run-reaper.test.ts:245`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L245), [`assembly-run-reaper.test.ts:404`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L404), [`assembly-run-reaper.test.ts:456`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L456), [`assembly-run-reaper.test.ts:472`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L472), [`assembly-run-reaper.test.ts:487`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L487); implemented by [`assembly-run-reaper.ts:76`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.ts#L76))
- **FR6.10a** *(added 2026-09-03)* Closing a run MUST close every visit still open under it, in the same statement that closes the run. FR6.10's reaper converges OPEN runs only (`listOpen`), so a visit left open when its run went terminal was swept by nothing and stayed open forever — 86 such rows had accumulated since 2026-08-21, and because the spend page prices a visit with no `finished_at` at up to its two-hour cap, each one billed phantom pod-hours on every page load. A visit that DID report keeps its own outcome; a stranded one is recorded `failed`/`unknown` saying it never reported, so it reads as the failure it was rather than as an eternal in-flight row. ([validated by [`assembly-runs.contract.test.ts:435`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L373))
- **FR6.11** An Agent's `status.output` is an NDJSON event stream whose terminal line carries the agent text inside a JSON string field (`{"type":"result","result":"…"}`), so newlines and any embedded JSON arrive escaped. The Floor MUST unwrap that envelope once, at the read boundary, before any text parser runs — a parser scanning for a single-line marker survives the escaping while one needing a real newline (the ```REVIEW_FINDINGS block) silently matches nothing, which yields a node that records `changes_requested` while the PR receives no review at all. Unwrapping MUST be idempotent so already-plain and legacy output pass through unchanged, and MUST (transitionally, until no pre-cutover CRs remain) also peel the ai-agent-subsystem's `{"source": {...}, "event": <line>}` attribution envelope that older runs stamped onto every stdout line — the result object rides at `.event` there. The duties a terminal node owes — post the review, record the outcome and advance, publish the PR check — MUST be shared by the event path and the reaper (FR6.10), and the review MUST be posted BEFORE the state transition, since both paths ignore a non-running row and a post attempted after it can never be repaired by a retry. A review that reaches a verdict but posts nothing, and a check that fails to publish, MUST be audited (`review_findings_unparsed` / `review_post_failed` / `pr_check_publish_failed`) rather than warned away — each is indistinguishable from a clean review at the PR. ([validated by `agent-output.test.ts:28`](libs/assembly-lines/src/agent-output.test.ts#L28), [`agent-output.test.ts:34`](libs/assembly-lines/src/agent-output.test.ts#L34), [`agent-output.test.ts:41`](libs/assembly-lines/src/agent-output.test.ts#L41), [`agent-output.test.ts:53`](libs/assembly-lines/src/agent-output.test.ts#L53), [`agent-output.test.ts:61`](libs/assembly-lines/src/agent-output.test.ts#L61), [`agent-output.test.ts:67`](libs/assembly-lines/src/agent-output.test.ts#L67), [`agent-output.test.ts:73`](libs/assembly-lines/src/agent-output.test.ts#L73), [`agent-output.test.ts:79`](libs/assembly-lines/src/agent-output.test.ts#L79), [`agent-output.test.ts:89`](libs/assembly-lines/src/agent-output.test.ts#L89), [`agent-output.test.ts:93`](libs/assembly-lines/src/agent-output.test.ts#L93), [`agent-output.test.ts:99`](libs/assembly-lines/src/agent-output.test.ts#L99), [`agent-output.test.ts:111`](libs/assembly-lines/src/agent-output.test.ts#L111), [`agent-output.test.ts:133`](libs/assembly-lines/src/agent-output.test.ts#L133), [`node-terminal.test.ts:171`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L171), [`node-terminal.test.ts:184`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L184), [`node-terminal.test.ts:190`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L190), [`node-event-handler.test.ts:322`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L322); implemented by [`agent-output.ts:119`](libs/assembly-lines/src/agent-output.ts#L119), [`node-terminal.ts:66`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L66))
  - *(added 2026-07-30)* Because the post precedes the transition, a redelivered terminal event or an event-vs-reaper race re-executes it — #870: the same review posted twice on one PR. The post MUST be idempotent per node visit: every posted review, inline body and fallback comment alike, carries an invisible `<!-- lore-review-run: <line>/<node>/<iteration> -->` marker (iteration-keyed, so a legitimate revisit still posts), and the poster probes the PR's reviews and issue comments for that exact marker after the parse (a run that would post nothing spends no reads) and immediately before posting — found means skip, outcome `already_posted`, audited as `review_post_deduped`. The probe fails open: a poster without the read surface, an unknown iteration (guessing `1` would let a first post suppress a revisit's real review), or a probe error posts anyway, since a rare duplicate beats a dropped review; racers landing inside the probe-to-post window (seconds, not the redelivery gap that produced #870) remain the accepted residual. ([validated by `node-terminal.test.ts:518`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L570), [`node-terminal.test.ts:591`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L591), [`node-terminal.test.ts:605`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L605), [`node-terminal.test.ts:623`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L623), [`node-terminal.test.ts:639`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L639), [`node-terminal.test.ts:662`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L662), [`post-review.test.ts:281`](apps/floor/src/jobs/review/post-review.test.ts#L281), [`post-review.test.ts:285`](apps/floor/src/jobs/review/post-review.test.ts#L285), [`post-review.test.ts:296`](apps/floor/src/jobs/review/post-review.test.ts#L296), [`post-review.test.ts:307`](apps/floor/src/jobs/review/post-review.test.ts#L307), [`post-review.test.ts:338`](apps/floor/src/jobs/review/post-review.test.ts#L338), [`post-review.test.ts:344`](apps/floor/src/jobs/review/post-review.test.ts#L344), [`post-review.test.ts:350`](apps/floor/src/jobs/review/post-review.test.ts#L350), [`post-review.test.ts:357`](apps/floor/src/jobs/review/post-review.test.ts#L357), [`post-review.test.ts:361`](apps/floor/src/jobs/review/post-review.test.ts#L361), [`post-review.test.ts:381`](apps/floor/src/jobs/review/post-review.test.ts#L381), [`post-review.test.ts:407`](apps/floor/src/jobs/review/post-review.test.ts#L407), [`post-review.test.ts:434`](apps/floor/src/jobs/review/post-review.test.ts#L434), [`node-terminal.test.ts:672`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L672); implemented by [`post-review.ts:54`](apps/floor/src/jobs/review/post-review.ts#L54), [`post-review.ts:226`](apps/floor/src/jobs/review/post-review.ts#L226), [`node-terminal.ts:167`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L167))
  - *(added 2026-07-31)* The reply post owes the same idempotency — #1004: `postReplyFromNode` also runs before the node-outcome CAS, so a redelivered terminal event or an event-vs-reaper race posts the refine node's REVIEW_REPLY twice. Every posted reply, in-thread and plain-comment delivery alike, carries an invisible `<!-- lore-reply-run: <line>/<node>/<iteration> -->` marker leading the agent-authored body, and the poster probes the PR's review-comment threads and issue comments for that exact marker immediately before posting — found means skip, outcome `already_posted`, audited as `review_reply_post_deduped`. The probe fails open exactly like the review post's: a poster without the read surface, an unknown iteration, or a probe error posts anyway. The marker leads rather than trails because the body is the agent's — there is no preamble of ours to pin, unlike the review fallback's `FALLBACK_NOTE` — so a trailing marker would let a reply opening with a prefix the platform-github `listIssueComments` adapter drops (`PR created:` / `Agent ` / `Task `) hide the posted comment from the plain-comment probe; leading, the comment always opens with the marker and the filter can never eat it. ([validated by `node-terminal.test.ts:676`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L728), [`node-terminal.test.ts:750`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L750), [`node-terminal.test.ts:764`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L764), [`node-terminal.test.ts:787`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L787), [`node-terminal.test.ts:803`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L803), [`node-terminal.test.ts:824`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L824), [`node-terminal.test.ts:840`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L840), [`node-terminal.test.ts:858`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L858); implemented by [`node-terminal.ts:243`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L243), [`node-terminal.ts:299`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L299), [`node-terminal.ts:346`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L346))
- **FR6.12** *(added 2026-07-17)* Every assembly-line closure with a failure outcome MUST notify the user through one generic seam — no definition-specific notification code. `finishLine` (the single closure path for the walk and the reaper) fires the notifier for any outcome outside the benign set (`completed`, `lease_held`, `pr_created`, `changes_requested`, `pr_closed`) and stays silent for benign closures, including the `lease_held` overlap defer. ([validated by `notify-failure.test.ts:36`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L38), [`notify-failure.test.ts:42`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L44), [`advance.test.ts:877`](apps/floor/src/jobs/assembly-run/advance.test.ts#L877), [`advance.test.ts:877`](apps/floor/src/jobs/assembly-run/advance.test.ts#L877); implemented by [`notify-failure.ts:26`](apps/floor/src/jobs/assembly-run/notify-failure.ts#L26), [`advance.ts:266`](apps/floor/src/jobs/assembly-run/advance.ts#L290), [`advance.test.ts:919`](apps/floor/src/jobs/assembly-run/advance.test.ts#L919), [`advance.test.ts:963`](apps/floor/src/jobs/assembly-run/advance.test.ts#L963), [`advance.test.ts:977`](apps/floor/src/jobs/assembly-run/advance.test.ts#L977))
  - Notification MUST fire exactly once per line — only the winner of the first-writer-wins `finish` CAS notifies (the port reports the win as its return value, in the Pg adapter and the in-memory double alike), and a notifier throw never fails the line transition or re-drives the event retry. ([validated by `advance.test.ts:950`](apps/floor/src/jobs/assembly-run/advance.test.ts#L950), [`advance.test.ts:1014`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1014), [`assembly-line-reads.test.ts:97`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L112), [`assembly-line-reads.test.ts:538`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L546); implemented by [`assembly-lines-pg.ts:159`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L159))
  - Channels: an `escalation`-level message through the repo's `dark_factory.notify` routing carrying the definition, repo, outcome, reason, and run link; plus, when the line is PR-linked, a PR comment with the run link (and the `@lore review` re-run hint on `code-review` lines — omitted for other definitions; lines without a `pr_number` send no comment). Each send is best-effort: a failed send is audited as `failure_notify_failed` with its channel, and the other channel still fires. ([validated by `notify-failure.test.ts:52`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L54), [`notify-failure.test.ts:69`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L71), [`notify-failure.test.ts:84`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L86), [`notify-failure.test.ts:115`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L115), [`notify-failure.test.ts:163`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L163), [`notify-failure.test.ts:179`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L179), [`notify-failure.test.ts:193`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L193), [`notify-failure.test.ts:208`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L208); implemented by [`notify-failure.ts:71`](apps/floor/src/jobs/assembly-run/notify-failure.ts#L71), [`notify-slack.test.ts:38`](libs/shared/src/project/notify/notify-slack.test.ts#L38), [`notify-slack.test.ts:72`](libs/shared/src/project/notify/notify-slack.test.ts#L72))
  - The one closure that bypasses `finishLine` — the start handler's task-less unknown-definition config error — notifies through the same contract, winner-gated so a redelivered start event never re-notifies. ([validated by `start-event-handler.test.ts:197`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L197); implemented by [`start-event-handler.ts:85`](apps/floor/src/jobs/assembly-run/start-event-handler.ts#L80))
- **FR6.13** *(added 2026-07-17)* A line's terminal state MUST be honest about node failures. A walk that reaches exit with a `failed` node visit closes the row with outcome `failed` and a `node "<id>" failed` reason instead of `completed` — every definition routes `failed` edges toward exit, so "completed" would render a green check over a failed review. *(strengthened 2026-08-20 by FR6.55: naming the node was honest about WHETHER it failed and silent about WHY, so the reason now carries the node's classified cause when it has one and falls back to this wording when it does not.)* *(strengthened 2026-09-02, run 52c3fdd5: a failure the walk RECOVERED from — a later visit of the same node closed non-failed — no longer decides the line. The verdict is the LAST unrecovered failed visit, so the reason names the failure that actually routed the walk out, and a walk whose every failure was retried to success closes `completed`.)* ([validated by `advance.test.ts:994`](apps/floor/src/jobs/assembly-run/advance.test.ts#L994), [`advance.test.ts:1804`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1804), [`advance.test.ts:1830`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1830), [`advance.test.ts:1840`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1840); implemented by [`advance.ts:55`](apps/floor/src/jobs/assembly-run/advance.ts#L55), [`advance.test.ts:789`](apps/floor/src/jobs/assembly-run/advance.test.ts#L789))
  - The PR check maps any non-benign line outcome (`failed`, `error`, `iteration_max`, ...) to a failure conclusion carrying the reason (plus the `@lore review` re-run hint on every line of the REVIEW FAMILY, from one shared constant — it was gated on `code-review` alone and worded three different ways, so a failed recheck published a red check under the `lore/code-review` name that said nothing about how to re-run it) — keying on row status alone would miss it, since only `error` flips the status column. ([validated by `pr-check.test.ts:129`](apps/floor/src/jobs/assembly-run/pr-check.test.ts#L129), [`pr-check.test.ts:146`](apps/floor/src/jobs/assembly-run/pr-check.test.ts#L146), [`pr-check.test.ts:177`](apps/floor/src/jobs/assembly-run/pr-check.test.ts#L177); implemented by [`pr-check.ts:58`](apps/floor/src/jobs/assembly-run/pr-check.ts#L58), [`notify-failure.test.ts:97`](apps/floor/src/jobs/assembly-run/notify-failure.test.ts#L97))
  - The review post reports how it went (`posted` / `already_posted` / `no_findings` / `post_failed` / `not_review`), and a review that published NOTHING records the node as `failed` rather than `success`, so the generic FR6.12 notification catches it. That covers both the original outage shape — no findings and no verdict, e.g. an agent that could not read the diff yet exited 0 — and the case where a verdict of `changes_requested` was reached but its findings block would not parse (#1401: a `body` carrying unescaped quotes killed `JSON.parse`, four findings including a blocking one were lost, and the check went green). Only a `success` verdict without findings stays untouched, and that combination does not arise in practice because a bare approval is posted. Posted findings and non-review nodes stay untouched too. ([validated by `node-terminal.test.ts:473`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L525), [`node-terminal.test.ts:352`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L352), [`node-terminal.test.ts:478`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L478), [`node-terminal.test.ts:494`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L494), [`node-terminal.test.ts:504`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L504), [`node-terminal.test.ts:535`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L535); implemented by [`node-terminal.ts:86`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L86), [`node-terminal.test.ts:1114`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L1114))
  - *(added 2026-09-03)* A `failed` review or recheck visit routes BACK to its own node with a one-retry budget rather than forward to `done`: the usual failure is a findings block that did not parse, which a fresh pass normally publishes, and a second identical failure fails the RUN via `iteration_max` — so the line can never complete as if a review had happened (PR #1714 merged minutes after its only review visit failed into a cleanly completed run). ([validated by [`builtin-assembly-lines.test.ts:34`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L34), [`builtin-assembly-lines.test.ts:50`](libs/assembly-lines/src/builtin-assembly-lines.test.ts#L50))
  - *(added 2026-09-03)* One failure class is exempt from both the retry and the red check: a review visit failed as `anthropic-credit` (the LLM budget is exhausted) posts an APPROVE stating loudly that no review happened — "Approved without review (no LLM budget)", naming the reviewer that would have run — deduped by the same per-visit marker as a real review, audited as `review_budget_skip`, and recorded as `success` so the line completes: an empty account is an operator problem, and blocking every PR on it reviews nothing. A non-review node or a run without a PR is untouched. ([validated by [`node-terminal.test.ts:1133`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L1133), [`node-terminal.test.ts:1164`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L1164), [`node-terminal.test.ts:1177`](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L1177), [`review-summary.test.ts:72`](libs/shared/src/review/review-summary.test.ts#L72), [`review-summary.test.ts:82`](libs/shared/src/review/review-summary.test.ts#L82))
  - *(added 2026-09-03)* Every posted review body names the model that judged the diff. The disclosure reads the models that actually BILLED against the visit (`llm_calls` via `UsagePort.modelsUsed`, distinct and sorted), because the dispatch spec snapshots the yaml default while the agent-definition row overrides it at run time; a visit with no billed call falls back to the node's declared model, and a body with no known model omits the line rather than guessing. ([validated by [`review-summary.test.ts:55`](libs/shared/src/review/review-summary.test.ts#L55), [`review-summary.test.ts:64`](libs/shared/src/review/review-summary.test.ts#L64), [`usage-memory.test.ts:130`](libs/shared/src/project/usage/usage-memory.test.ts#L130), [`usage-memory.test.ts:152`](libs/shared/src/project/usage/usage-memory.test.ts#L152))

- **FR6.14** *(added 2026-07-17)* A node CR that fails because the Anthropic account ran dry (`Credit balance is too low` / `insufficient credit`) MUST raise ONE throttled operator alert, distinct from the per-line FR6.12 failure notice. Such a failure downs every LLM node — review, refine, triage, implementation — at once, so a per-run notice would flood the channel while a code fix would do nothing; the fix is topping up the account. The classifier reads the agent's terminal error text (`terminalErrorText` — the last `is_error` result line, peeled from the same NDJSON/attribution envelope as FR6.11, since the CR's own `failureReason` is only the Job-level `BackoffLimitExceeded`) *(restated 2026-08-20: for its first months this requirement did not hold in production. The alert ran downstream of `normalizeAgentStatus`, which had already unwrapped the envelope, so `terminalErrorText` found no result line to read, the fallback was the Job-level `BackoffLimitExceeded`, and no alert ever fired through an account-wide outage. The lift now happens inside the unwrap (FR6.52) and the classification is the shared `classifyError`, not a local matcher; a test drives the real alert through the handler, which is the composition neither side's tests covered — #1455)*; a global time-window throttle (account-wide, so one alert per window across all repos, not per drowned run) gates the `escalation`-level Slack send; and the whole path is best-effort — a notify throw is logged, never propagated, so it cannot fail the node-event handler or re-drive the event. Non-billing failures neither alert nor consume the throttle. ([validated by `billing-alert.test.ts:14`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L14), [`billing-alert.test.ts:33`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L33), [`billing-alert.test.ts:41`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L41), [`billing-alert.test.ts:60`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L60), [`billing-alert.test.ts:75`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L75), [`billing-alert.test.ts:94`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L94), [`billing-alert.test.ts:118`](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L118), [`node-event-handler.test.ts:163`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L163), [`node-event-handler.test.ts:293`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L293), [`node-outcome.test.ts:135`](libs/assembly-lines/src/node-outcome.test.ts#L135), [`node-outcome.test.ts:135`](libs/assembly-lines/src/node-outcome.test.ts#L135), [`agent-output.test.ts:144`](libs/assembly-lines/src/agent-output.test.ts#L144), [`agent-output.test.ts:154`](libs/assembly-lines/src/agent-output.test.ts#L154), [`agent-output.test.ts:164`](libs/assembly-lines/src/agent-output.test.ts#L164), [`agent-output.test.ts:172`](libs/assembly-lines/src/agent-output.test.ts#L172); implemented by [`billing-alert.ts:29`](apps/floor/src/jobs/assembly-run/billing-alert.ts#L29), [`node-event-handler.ts:76`](apps/floor/src/jobs/assembly-run/node-event-handler.ts#L78))

- **FR6.15** *(added 2026-08-10)* A terminal line MUST settle its backing `pipeline.tasks` row. `finishLine` closed the run row and reclaimed the token but wrote nothing to the task, and the agent-watcher's post-completion path returns early for labeled node CRs — so a line-backed task sat at `running` with a NULL `failure_reason` forever, and every consumer downstream (the task list, the feature-planning wizard) read a dead run as still working. The settle is winner-gated like the FR6.12 notification, decided purely from the line outcome plus the task's current status, and written compare-and-set against the status that was read, so a task already past settling (`pr-created`, `completed`, `cancelled`) is never clobbered and a losing racer is a no-op. A `completed` line completes the task; any other outcome fails it carrying the line's reason (or `assembly line <outcome>` when the line recorded none); a `lease_held` defer settles nothing, because the run that holds the branch still owns the task. A task-less line settles nothing. A settle failure is logged, never propagated — it must not poison the line closure. ([validated by `settle-task.test.ts:37`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L37), [`settle-task.test.ts:43`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L43), [`settle-task.test.ts:53`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L53), [`settle-task.test.ts:59`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L59), [`settle-task.test.ts:65`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L65), [`settle-task.test.ts:73`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L73), [`settle-task.test.ts:138`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L138), [`settle-task.test.ts:179`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L179), [`settle-task.test.ts:193`](apps/floor/src/jobs/assembly-run/settle-task.test.ts#L193), [`advance.test.ts:963`](apps/floor/src/jobs/assembly-run/advance.test.ts#L963); implemented by [`settle-task.ts:48`](apps/floor/src/jobs/assembly-run/settle-task.ts#L48), [`advance.ts:270`](apps/floor/src/jobs/assembly-run/advance.ts#L294), [`assembly-run-reaper.test.ts:1111`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L1111), [`advance.test.ts:1759`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1759))

- **FR6.60** *(added 2026-08-29)* The 6-hour stale-task safety net MUST consult the LINE before escalating, exactly as the boot-time crash-recovery sweep does. Age alone stopped being sufficient once a line could legitimately stay open for days: a line parked on a human node waits as long as the person does, and the task owning it stays `running` that whole time. Escalating it was not merely a false alarm but an IRREVERSIBLE one — `decideTaskSettlement` settles a task only while it is still in {pending, queued, running}, so when the person finally answered and the line completed the task stayed at `needs-human-help` forever and its issue was never picked up again. The clock only decides among tasks nothing is working on. ([validated by escalates a 9.2h task whose line is closed](apps/floor/src/jobs/task/stale-task-check.test.ts#L31), [`stale-task-check.test.ts:38`](apps/floor/src/jobs/task/stale-task-check.test.ts#L38), [`stale-task-check.test.ts:47`](apps/floor/src/jobs/task/stale-task-check.test.ts#L47), [`stale-task-check.test.ts:55`](apps/floor/src/jobs/task/stale-task-check.test.ts#L55))

- **FR6.21** *(added 2026-08-12)* The Floor MUST exit on a termination signal. Registering ANY signal handler overrides Node's default terminate, and the Floor had two — one stopping the HTTP server, one flushing telemetry — so it did both halves and neither exited, while the drain loop, scheduler and K8s watch kept the event loop busy. The result was a process that stopped serving and lived on: locally a Floor answering nothing while its supervisor reported it healthy, and in a rollout a pod failing its liveness probe for the whole termination grace period while looking like a slow shutdown. Process lifecycle now has ONE owner, which stops serving, flushes, then exits. Every step is best-effort — a shutdown that cannot complete must still terminate, or the zombie returns — and the sequence runs once however many signals arrive. `startHealthServer` registers no handler and returns its stop function instead, and is awaited, because fire-and-forgetting it left a late failure with nowhere to surface. ([validated by stops serving, flushes telemetry, then EXITS](apps/floor/src/shutdown.test.ts#L60), [`shutdown.test.ts:73`](apps/floor/src/shutdown.test.ts#L73), [`shutdown.test.ts:86`](apps/floor/src/shutdown.test.ts#L86), [`shutdown.test.ts:98`](apps/floor/src/shutdown.test.ts#L98); implemented by [`shutdown.ts:29`](apps/floor/src/shutdown.ts#L29))

- **FR6.19** *(added 2026-08-12; the `signal` mechanism superseded by FR6.40)* A node's worker may be a HUMAN. A human station is one whose work happens outside the pod system — a person in the planning wizard, or a spec PR merging — and it reports the same station contract (an outcome plus extras) over HTTP that a pod reports over stdout. Naming the surface that will report it is REQUIRED, because a station nobody can complete parks a run forever; that naming was `signal` and is now the node's own type plus its `route` (FR6.40). Three consequences, each load-bearing: the walk records the node row but DISPATCHES NOTHING, since the row is what parks the walk and lets the graph show whose move it is; the reaper never times it out, because a node parked on a person is not stuck and "how long may someone take to answer" has no defensible number — a large budget would merely kill the feature next month instead of this hour; and a back-edge OUT OF a wait node needs no `iteration_max`, because the runaway that rule guards against needs two agents, and a human decides each pass. That exemption is keyed strictly on the source node's type, so it cannot become a way to write an unbounded agent loop. ([validated by accepts a wait node and an unbounded back-edge out of it](libs/assembly-lines/src/loader.test.ts#L204), [`loader.test.ts:247`](libs/assembly-lines/src/loader.test.ts#L247), [`loader.test.ts:287`](libs/assembly-lines/src/loader.test.ts#L287), [`advance.test.ts:394`](apps/floor/src/jobs/assembly-run/advance.test.ts#L394), [`assembly-run-reaper.test.ts:217`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L217), [`assembly-run-reaper.test.ts:226`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L226), [validated by the planning line parks on the author with nothing dispatched](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L43); implemented by [`loader.ts:96`](libs/assembly-lines/src/loader.ts#L96), [`advance.ts:255`](apps/floor/src/jobs/assembly-run/advance.ts#L279), [`assembly-run-reaper.ts:53`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.ts#L53))

- **FR6.20** *(added 2026-08-12)* A `wait` node's worker reports through `assembly_run.resume`, and that event takes EXACTLY the path a terminal CR takes: record the node's outcome, advance the walk. The convergence is the design — if the resume handler ever grows its own advance logic, the human node has stopped being a station and become a special case. The event names the node it completes, because a line may be parked on more than one wait and guessing would resume the wrong one; it carries an outcome the station contract defines, because a typo'd one would route down an edge nobody wrote and `selectEdge` does not fall through; and whatever the worker produced is merged into the line's args BEFORE the walk advances, so the next node reads it as its brief through the same channel a produced artifact uses (FR6.17) rather than a second mechanism invented for human input. ([validated by records the outcome and advances, the same path a pod's outcome takes](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L41), [`resume-event-handler.test.ts:60`](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L60), [`resume-event-handler.test.ts:83`](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L83), [`resume-event-handler.test.ts:91`](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L91), [`resume-event-handler.test.ts:100`](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L100), [`resume-event-handler.test.ts:108`](apps/floor/src/jobs/assembly-run/resume-event-handler.test.ts#L108), [validated by the author's refine feedback rides args into round 2](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L54); implemented by [`resume-event-handler.ts:35`](apps/floor/src/jobs/assembly-run/resume-event-handler.ts#L35))

- **FR6.21** *(added 2026-08-12)* A feature's planning is ONE assembly line whose rounds are revisits, not one line per round. `analyze` hands to the `author` wait node instead of ending the line, and the author's `changes_requested` sends the walk back for another round — so the moments a person acts stop being invisible gaps between runs and become steps the graph can draw. The loop is unbounded because it is human-gated: a number here would silently kill a feature that needed one more round. A revisit MUST advance the iteration, because a node's storage identity is (nodeId, iteration) and the Agent CR name derives from it — so the walk bumps on any edge returning to an ALREADY-VISITED node rather than only on a budgeted one. Those two rules picked out the same edges while every back-edge carried `iteration_max`, and stopped agreeing the moment an unbounded one existed. ([validated by parks on the author after a round is produced](libs/assembly-lines/src/planning-author-loop.test.ts#L22), [`planning-author-loop.test.ts:28`](libs/assembly-lines/src/planning-author-loop.test.ts#L28), [`planning-author-loop.test.ts:37`](libs/assembly-lines/src/planning-author-loop.test.ts#L37), [`planning-author-loop.test.ts:46`](libs/assembly-lines/src/planning-author-loop.test.ts#L46), [`planning-author-loop.test.ts:62`](libs/assembly-lines/src/planning-author-loop.test.ts#L62), [`planning-author-loop.test.ts:71`](libs/assembly-lines/src/planning-author-loop.test.ts#L71), [`planning-author-loop.test.ts:80`](libs/assembly-lines/src/planning-author-loop.test.ts#L80), [`planning-author-loop.test.ts:90`](libs/assembly-lines/src/planning-author-loop.test.ts#L90); implemented by [`transition.ts:66`](libs/assembly-lines/src/transition.ts#L66), [`feature-planning.yaml:1`](libs/assembly-lines/src/assembly-lines/feature-planning.yaml#L1))

- **FR6.22** *(added 2026-08-12)* The planning wizard REPORTS to the node its feature's line is parked on instead of minting a line per round: the author's verdict becomes an `assembly_run.resume` for that node, carrying the round's brief in `args`. The line is found by the feature's SUBJECT KEY (`feature:<id>`, the string the Floor stamps at launch), newest planning line first — *(amended 2026-08-21)* it was found through the FIRST round's task on the premise that it owns the line for its whole life, but a failed round 1 makes round 2 legally mint a task and line of its own, after which the first-task resolver sees only the finished line and takes the legacy path forever (#1462) — and the parked node is the `author` row that has no outcome yet — a row that already has one has been reported, and completing it twice would advance a walk that has moved on. *(amended 2026-08-22)* There is no longer a second way in: the parked node is the ONLY route for a round or an accept, so a feature with none — planning that predates the merged line, or a line already past the author — is REFUSED with a 409 naming the run that is working it. The refusal is the point. The old fallback minted a line per press, which is the shape that produced four runs and three duplicate spec PRs from one feature (#1366); a feature that predates the merged line now strands visibly instead of quietly starting runs nothing joins. The resumed round mints NO task: a second `feature-planning` task would start a second line for the same feature, so the round's handle is the iteration row the wizard already polls. Unlike the ingest triggers, this event insert is never swallowed — a lost event is a lost round, and a 202 would tell the author it started. ([validated by resumes the author node the line is parked on](libs/shared/src/feature-planning/round-dispatch.test.ts#L36), [`round-dispatch.test.ts:44`](libs/shared/src/feature-planning/round-dispatch.test.ts#L44), [`round-dispatch.test.ts:54`](libs/shared/src/feature-planning/round-dispatch.test.ts#L54), [`round-dispatch.test.ts:67`](libs/shared/src/feature-planning/round-dispatch.test.ts#L67), [`round-dispatch.test.ts:73`](libs/shared/src/feature-planning/round-dispatch.test.ts#L73), [`round-dispatch.test.ts:83`](libs/shared/src/feature-planning/round-dispatch.test.ts#L83), [`round-dispatch.test.ts:93`](libs/shared/src/feature-planning/round-dispatch.test.ts#L93), [`features.test.ts:165`](apps/lore-api/src/api/routes/features/features.test.ts#L165), [`features.test.ts:831`](apps/lore-api/src/api/routes/features/features.test.ts#L831); implemented by [`round-dispatch.ts:34`](libs/shared/src/feature-planning/round-dispatch.ts#L34), [`features.ts:resolveDispatch`](apps/lore-api/src/api/routes/features/features.ts#L102))

- **FR6.23** *(added 2026-08-12)* A parked `wait` node MUST read as waiting, and MUST say whose move it is. Its walk row seeds `running` like any other open node, so before this the graph showed a spinner and the label "Running" on a node that was in fact waiting for the person reading the screen — promising work that was not happening. The badge instead reads **Waiting for you** (`author_feedback`) or **Waiting for the spec PR** (`pr_merged`), in a distinct tone that is never the running one, and the detail panel explains the pause rather than reporting progress. A wait node the walk has NOT reached stays Pending: announcing "waiting for you" there would ask for input the line cannot yet accept. ([validated by reads as waiting for you, not Running, while the author holds the round](apps/web-ui/src/lib/run-node-status.test.ts#L82), [`run-node-status.test.ts:93`](apps/web-ui/src/lib/run-node-status.test.ts#L93), [`run-node-status.test.ts:100`](apps/web-ui/src/lib/run-node-status.test.ts#L100), [`run-node-status.test.ts:109`](apps/web-ui/src/lib/run-node-status.test.ts#L109), [`run-node-detail-presenter.test.ts:285`](apps/web-ui/src/lib/run-node-detail-presenter.test.ts#L286), [`run-node-detail-presenter.test.ts:303`](apps/web-ui/src/lib/run-node-detail-presenter.test.ts#L304); implemented by [`run-node-status.ts:64`](apps/web-ui/src/lib/run-node-status.ts#L64), [`run-node-detail-presenter.ts:134`](apps/web-ui/src/lib/run-node-detail-presenter.ts#L134))

- **FR6.46** *(added 2026-08-18)* A node badge MUST fit inside the box that draws it. FR6.23 fixes the words a parked node shows, and the longest of them — **Waiting for the spec PR** — was wider than the 176px box, so a parked `pr_review` node rendered as a line of text running out past its own border and over the graph behind it. SVG text neither wraps nor clips itself, so fitting is the box's job: the width is sized to the longest badge the platform ships (the layout takes the same constant as its column pitch, so the gaps widen with it), and a character budget derived from that width is the backstop that keeps an unforeseen label — a longer verdict, a larger type scale — inside the border rather than through it. A clipped label keeps its full text in a hover title; an unclipped one gets no title, because a tooltip repeating what is already on screen is noise the accessibility tree reads twice. ([validated by keeps 'Waiting for the spec PR' whole — the longest badge the spec ships](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L9), [`run-graph-geometry.test.ts:15`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L15), [`run-graph-geometry.test.ts:19`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L19), [`run-graph-geometry.test.ts:26`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L26), [`run-graph-geometry.test.ts:30`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L30), [`run-graph-geometry.test.ts:36`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.test.ts#L36); implemented by [`run-graph-geometry.ts:43`](apps/web-ui/src/app/assembly-runs/[id]/run-graph/run-graph-geometry.ts#L43))

- **FR6.47** *(added 2026-08-18)* A node's `prompt_ref` names the recipe it runs, and a ref that names nothing MUST fail the node — never silently run a different recipe. The resolver fell back to `general` for an unknown ref, which made the declaration unfalsifiable: `push-only` (named by the pushing node of five lines) and `address-feedback` (the implementation line's review-response node) had no task type at all, so both ran "complete the following task" against the raw description. The push nodes duly edited files, committed, exited 0 and reported success without ever pushing, for weeks, while no line opened a PR and every affected run parked on a wait node for a PR that could not exist. Both recipes now exist — the push one's only job is delivery, and it says so — and an unresolvable ref raises rather than substituting. The rule is guarded twice: at dispatch, and at build time by a test that resolves every agent node of every shipped line through the same `prompt_ref ?? type` rule the walk uses. ([validated by every agent node resolves to a task type that exists](libs/assembly-lines/src/prompt-refs.test.ts#L33), [`prompt-refs.test.ts:42`](libs/assembly-lines/src/prompt-refs.test.ts#L42), [`prompt-refs.test.ts:53`](libs/assembly-lines/src/prompt-refs.test.ts#L53), [`config.test.ts:37`](apps/floor/src/kernel/config.test.ts#L37), [`config.test.ts:43`](apps/floor/src/kernel/config.test.ts#L43), [`config.test.ts:49`](apps/floor/src/kernel/config.test.ts#L49), [`config.test.ts:55`](apps/floor/src/kernel/config.test.ts#L55), [`config.test.ts:69`](apps/floor/src/kernel/config.test.ts#L69); implemented by [`config.ts:123`](apps/floor/src/kernel/config.ts#L123))

- **FR6.48** *(added 2026-08-18)* A push that delivered NOTHING must fail its line, not park it. GitHub refusing the PR with "No commits between" is the branch stating plainly that the node reported success and pushed nothing, and the node after a push is a human station — so swallowing that refusal left the walk waiting on a PR that could not exist, with the diagnosis in a Floor log nobody reads and the wizard showing "Waiting for the spec PR" indefinitely (#1330). The empty-branch refusal now closes the run with a reason naming the node and the branch, which the task settle puts in front of the author; the walk never reaches the wait node, because a failed row is not one `advanceLine` walks. Every OTHER stamp failure stays transient — a 5xx, a token blip, a duplicate-PR 422 say nothing about the branch, and failing a run on a network hiccup would discard work that is fine. Matched on GitHub's validation text rather than its status code, since 422 is also what those other refusals return. ([validated by fails the line instead of parking it on a PR that cannot exist](apps/floor/src/jobs/assembly-run/advance.test.ts#L1158), [`advance.test.ts:1183`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1183), [`advance.test.ts:1183`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1183), [`spec-pr.test.ts:546`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L546), [`spec-pr.test.ts:546`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L546), [`spec-pr.test.ts:554`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L554), [`spec-pr.test.ts:560`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L560), [`spec-pr.test.ts:574`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L574), [`spec-pr.test.ts:574`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L574); implemented by [`spec-pr.ts:51`](apps/floor/src/jobs/assembly-run/spec-pr.ts#L51))

- **FR6.24** *(added 2026-08-12)* A conversation belongs to a NODE EXECUTION, not to an assembly line, and a RETRY is told from a next round by WHY the node is being revisited. Both rules were expressed in terms that were only accidentally equivalent while every round was its own line: "never resume this LINE's conversation" and "never continue at iteration > 1". On a line whose rounds are revisits (FR6.21) the first excludes every earlier round of the run's own line and the second refuses every round after the first — so the merged line silently lost conversation continuity, and nothing reported it, because a missing prior conversation just re-briefs the agent with the whole draft again and still succeeds. Identity is therefore `(line, iteration)`, a retry is a revisit whose own last visit FAILED, and rewind names a round by its iteration because a resumed round mints no task to name it by. A row with no recorded iteration is the only execution on its line, so pre-existing rows stay both excludable and addressable. ([validated by continues the previous round even though it ran on this same line](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L241), [`resolve-conversation.test.ts:288`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L288), [`resolve-conversation.test.ts:308`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L308), [`resolve-conversation.test.ts:325`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L325), [`resolve-conversation.test.ts:351`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L351), [`resolve-conversation.test.ts:138`](apps/floor/src/jobs/assembly-run/resolve-conversation.test.ts#L138), [`conversation-thread.test.ts:67`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L67), [`conversation-thread.test.ts:71`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L71), [`conversation-thread.test.ts:75`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L75), [`conversation-thread.test.ts:79`](apps/floor/src/jobs/assembly-run/conversation-thread.test.ts#L79), [`conversations.test.ts:171`](libs/shared/src/project/conversations/conversations.test.ts#L171), [`conversations.test.ts:188`](libs/shared/src/project/conversations/conversations.test.ts#L188), [`conversations.test.ts:204`](libs/shared/src/project/conversations/conversations.test.ts#L204), [`features.test.ts:479`](apps/lore-api/src/api/routes/features/features.test.ts#L479), [`features.test.ts:485`](apps/lore-api/src/api/routes/features/features.test.ts#L485), [`conversations.test.ts:251`](libs/shared/src/project/conversations/conversations.test.ts#L251), [`conversations.test.ts:263`](libs/shared/src/project/conversations/conversations.test.ts#L263), [`conversations.test.ts:222`](libs/shared/src/project/conversations/conversations.test.ts#L222); implemented by [`conversation-thread.ts:70`](apps/floor/src/jobs/assembly-run/conversation-thread.ts#L70), [`resolve-conversation.ts:36`](apps/floor/src/jobs/assembly-run/resolve-conversation.ts#L36), [`conversations-memory.ts:13`](libs/shared/src/project/conversations/conversations-memory.ts#L13))

- **FR6.25** *(added 2026-08-12)* Exactly ONE Floor may drain `pipeline.events`. The loop claims rows with `FOR UPDATE SKIP LOCKED`, so a second instance does not corrupt a row — it SPLITS the stream, which is worse: each Floor handles some events with whatever code it loaded, so a stale process silently applies old rules while the log of the instance being watched stays clean. That failed a real assembly line with a replay-divergence error minutes after the new rule was confirmed present in the loaded artifact, and the `:8080` health-port guard does not cover it — a Floor that binds no health server drains events perfectly happily. The lock is a Postgres SESSION-level advisory lock on a DEDICATED connection, so the server releases it the moment a holder dies (no heartbeat, no reaper, and a killed Floor never locks out the next one); a pooled client would hand the lock to whichever job borrowed it next. A contending Floor WAITS rather than exits, because during a rolling update the outgoing pod still holds the lock and exiting would crash-loop the new pod against its own predecessor — and it announces the wait ONCE, since that line is the only explanation for an otherwise silent startup. The wait MUST come after the health server and the signal handlers: a Floor waiting its turn is healthy, and one that served nothing while waiting would fail its liveness probe and be killed — reproducing the very crash-loop the wait exists to avoid, and reporting a healthy successor as down. ([validated by starts immediately and says nothing when no other Floor holds the lock](apps/floor/src/kernel/single-instance.test.ts#L30), [`single-instance.test.ts:42`](apps/floor/src/kernel/single-instance.test.ts#L42), [`single-instance.test.ts:51`](apps/floor/src/kernel/single-instance.test.ts#L51), [`single-instance.test.ts:59`](apps/floor/src/kernel/single-instance.test.ts#L59); [`single-instance.test.ts:69`](apps/floor/src/kernel/single-instance.test.ts#L69); the lock's connection is WATCHED: it is checked out of the pool and never released, so the pool's own handler — which covers idle clients — never sees it, and a Postgres restart (57P01) raised an unhandled `error` that killed the Floor seven times until systemd's start limit left it dead. Exiting is right, since the lock died with the connection; doing so DELIBERATELY is what lets the supervisor bring it back. implemented by [`single-instance.ts:38`](apps/floor/src/kernel/single-instance.ts#L38), [`index.ts:74`](apps/floor/src/index.ts#L74))

- **FR6.26** *(added 2026-08-12)* Accepting the plan does not start a NEW run: the author station reports `success` and the spec work — analyse-specs → write → push — continues on the SAME line, so one line id spans a feature from its first round to its pushed spec PR. Two consequences. The spec analysis asking a question becomes an EDGE back to the author instead of the line ending, because that analysis has no upstream node — its input is a plan a PERSON accepted, so only that person can answer — which retires the workaround where `settle-task` faked a task failure to surface the objection. And the back-edge exemption widens from "out of a `wait` node" to "either END is a `wait` node": leaving one, the human has just decided; entering one, the human decides before anything else runs. A cycle between two agents stays bounded (write ⇄ analyse-specs keeps `iteration_max: 1`), so the exemption cannot become a way to write an unbounded agent loop. *(amended 2026-08-21)* The accept resume also CARRIES the accepted plan: the resume merge is a shallow jsonb `||`, so without fresh args the line keeps the LAST REFINE's brief — the round-before-accepted draft plus the author's objections — as the `args.description` that `analyse-specs` and `write` render as "the accepted plan" (#1470). The accept therefore re-renders `description` via `composePlanningPrompt` from the latest READY round, and nulls `round_feedback` and `resume_from_iteration` outright, because a key the merge omits SURVIVES and would leave the last refine steering later rounds. *(amended 2026-08-22)* That render CARRIES the answers the author typed before accepting: they fill the form and press accept in one motion, and sending an empty body dropped the last thing they said about the plan — `analyse-specs` and `write` saw the draft alone. ([validated by analyses which specs change before anything writes one](libs/assembly-lines/src/planning-author-loop.test.ts#L105), [`planning-author-loop.test.ts:113`](libs/assembly-lines/src/planning-author-loop.test.ts#L113), [`planning-author-loop.test.ts:122`](libs/assembly-lines/src/planning-author-loop.test.ts#L122), [`planning-author-loop.test.ts:131`](libs/assembly-lines/src/planning-author-loop.test.ts#L131), [`planning-author-loop.test.ts:141`](libs/assembly-lines/src/planning-author-loop.test.ts#L141), [`planning-author-loop.test.ts:153`](libs/assembly-lines/src/planning-author-loop.test.ts#L153), [`planning-author-loop.test.ts:163`](libs/assembly-lines/src/planning-author-loop.test.ts#L163), [`features.test.ts:515`](apps/lore-api/src/api/routes/features/features.test.ts#L515), [`features.test.ts:570`](apps/lore-api/src/api/routes/features/features.test.ts#L570), [validated by accept moves the same line to analyse-specs](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L71), [validated by write's rejection re-runs analyse-specs once](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L106), [validated by a spec PR review objection returns to the author](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L132); [`planning-author-loop.test.ts:179`](libs/assembly-lines/src/planning-author-loop.test.ts#L179); an agent node MUST name its own `station_ref` when its recipe differs from the line's task-type default — the Station carries the prompt template AND `output.watch`, so a node that inherits the line's Station runs the wrong prompt and emits the wrong artifact while reporting success. implemented by [`feature-planning.yaml:1`](libs/assembly-lines/src/assembly-lines/feature-planning.yaml#L1), [`loader.ts:417`](libs/assembly-lines/src/loader.ts#L417), [`features.ts:finalize accept resume`](apps/lore-api/src/api/routes/features/features.ts#L545), [`features.test.ts:958`](apps/lore-api/src/api/routes/features/features.test.ts#L958), [`features.test.ts:980`](apps/lore-api/src/api/routes/features/features.test.ts#L980), [`PlanningWizard.acceptance.test.tsx:187`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L187), [`features.test.ts:1004`](apps/lore-api/src/api/routes/features/features.test.ts#L1004), [`features.test.ts:1029`](apps/lore-api/src/api/routes/features/features.test.ts#L1029))

- **FR6.27** *(added 2026-08-12)* An open assembly line means its task is NOT stale. Crash recovery infers stuckness from age — anything `running` past 30 minutes goes back to `pending` — which was sufficient only while every line finished in minutes. A merged planning line parks on its author for as long as the person takes, and the task that owns it stays `running` for the feature's whole life, so the sweep read a perfectly healthy feature as a crash and re-dispatched it on EVERY Floor boot: a fresh planning agent, and a bill, per restart. The line is the authority on whether work is in flight; the clock is only a hint. A task with no queued-or-running line behind it is still recovered, because that is exactly what crash recovery is for. ([validated by leaves a task alone while its assembly line is still open](apps/floor/src/jobs/task/worker.test.ts#L229), [`worker.test.ts:249`](apps/floor/src/jobs/task/worker.test.ts#L249); implemented by [`worker.ts:89`](apps/floor/src/jobs/task/worker.ts#L89))

- **FR6.28** *(added 2026-08-12)* The round a result belongs to comes from the LINE, not from the task. A resumed round mints no task (FR6.22), so every round after the first reports against the feature's ORIGINATING task — whose `context_bundle` still says iteration 1. Reading the round number there wrote round N's result onto round 1 and left round N with nothing, which the terminal settlement then correctly reported as a round that produced no result: the author saw "round N failed" beside a plan that had visibly updated, and round 1's draft was overwritten unrecoverably. The task's own value stays as the fallback for the legacy shape, where each round had a line AND a task of its own. ([validated by writes the result to the round the LINE says is running](apps/floor/src/jobs/agent/planning-result.test.ts#L215), [`planning-result.test.ts:232`](apps/floor/src/jobs/agent/planning-result.test.ts#L232); implemented by [`planning-result.ts:60`](apps/floor/src/jobs/agent/planning-result.ts#L60))

- **FR6.29** *(added 2026-08-12)* The spec PR is linked back to its feature by the task CARRYING a feature, not by the task's type. The merged line runs a feature's whole life under one `feature-planning` task (FR6.26), so a check for `feature-finalize` silently stopped linking: `push` opened the spec PR and nothing flipped the feature to `pr-open`, leaving the wizard on "Creating the spec PR…" forever while the work had in fact completed — the accept looked like it did nothing. A planning ROUND never reaches this path, since only a run that produced a PR does, so keying on the feature cannot over-fire. The slug is optional: the merged line's task predates the spec, so the path is omitted rather than invented. ([validated by links the PR for the merged line, whose task type is feature-planning](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L113), [`agent-watcher-logic.test.ts:120`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L120), [`agent-watcher-logic.test.ts:120`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L120), [`agent-watcher-logic.test.ts:124`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L124), [`agent-watcher-logic.test.ts:128`](apps/floor/src/jobs/watcher/agent-watcher-logic.test.ts#L128); implemented by [`agent-watcher-logic.ts:1`](apps/floor/src/jobs/watcher/agent-watcher-logic.ts#L1))

- **FR6.30** *(added 2026-08-12)* The spec phase gets the SAME running card as a planning round. Both run on the same line (FR6.26) and the author has no decision to make while either is working, so showing the decision row with everything disabled — a greyed "Refine again" beside a primary relabelled "Creating the spec PR…" — offered two dead controls and, worse, hid the run graph, which rendered only while a ROUND was running. The card names which half is working rather than reporting a round number the spec phase does not have. ([validated by announces the spec phase instead of a planning round](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L119), [`RunningCard.test.tsx:135`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.test.tsx#L135); [`feature-phase.test.ts:53`](apps/web-ui/src/lib/feature-phase.test.ts#L80), [`feature-phase.test.ts:96`](apps/web-ui/src/lib/feature-phase.test.ts#L123), [`feature-phase.test.ts:27`](apps/web-ui/src/lib/feature-phase.test.ts#L54), [`feature-phase.test.ts:123`](apps/web-ui/src/lib/feature-phase.test.ts#L148); whether the spec phase is running is read from the LINE, never from whether the author recently pressed the button — a local flag could only clear when the feature left the planning phase, so a line that finished without producing a PR left the progress card up indefinitely, timed from the last ROUND's creation. [`PlanningWizard.acceptance.test.tsx:168`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L168), [`PlanningWizard.acceptance.test.tsx:177`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L177), [`PlanningWizard.acceptance.test.tsx:206`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L206), [`PlanningWizard.acceptance.test.tsx:226`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L226), [`PlanningWizard.acceptance.test.tsx:248`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L248); [`PlanningWizard.acceptance.test.tsx:284`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L284), [`PlanningWizard.acceptance.test.tsx:295`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlanningWizard.acceptance.test.tsx#L295); the wizard POLLS for as long as it is mounted, not only while a planning round runs — the spec phase runs no round, so the old guard stopped polling exactly when the line was working, and a RELOAD mid-phase offered the accept again and never learned otherwise, which mints a second line per press. the accept walk is covered as a SEQUENCE — parked, press, spec phase, line finishing — because each component was individually correct while the walk between them was not. implemented by [`RunningCard.tsx:1`](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/RunningCard.tsx#L1))

- **FR6.31** *(added 2026-08-13)* An assembly line is READABLE over the API. Until now nothing outside the web UI could see one: the UI reads `pipeline.assembly_runs` straight from Postgres and lore-api exposed nothing, so "which node is this line on, which Station is that, which recipe does it run" was answerable only by something holding a database connection. These live on the FLOOR, not lore-api: resolving a Station needs the assembly-line definitions, which are baked into the Floor's image and deliberately absent from lore-api's lean one — putting them there made that container fail to build, since the dependency resolved through workspace hoisting locally and did not exist in Docker. `GET /api/assembly-lines/{id}` returns the line, its node rows, and per node the resolved Station; `GET /api/assembly-line-definitions` returns the same for the whole catalog. The Station is the load-bearing field — it carries the prompt template AND the `output.watch` that decides what artifact a run can produce — and `stationInherited` is what makes an INHERITED one visible: an agent node that declares no `station_ref` runs the recipe named after its LINE, which is how every node on the merged planning line ran the planning prompt and reported success. The first live call to this endpoint found a third such node (`push`). The reads are bearer-authenticated like every other Floor route. A line whose definition is unknown still serves its rows, because the rows are the record of what actually ran and hiding them hides the run being debugged. ([validated by returns the line with its nodes](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L72), [`assembly-line-reads.test.ts:224`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L224), [`assembly-line-reads.test.ts:85`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L85), [`assembly-line-reads.test.ts:100`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L100), [`assembly-line-reads.test.ts:192`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L192), [`assembly-line-reads.test.ts:198`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L198), [`assembly-line-reads.test.ts:211`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L211), [`assembly-line-reads.test.ts:233`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L233), [`node-station.test.ts:8`](libs/assembly-lines/src/node-station.test.ts#L8), [`node-station.test.ts:17`](libs/assembly-lines/src/node-station.test.ts#L17), [`node-station.test.ts:24`](libs/assembly-lines/src/node-station.test.ts#L24), [`node-station.test.ts:30`](libs/assembly-lines/src/node-station.test.ts#L30), [`node-station.test.ts:36`](libs/assembly-lines/src/node-station.test.ts#L36); implemented by [`assembly-line-reads.ts:70`](apps/floor/src/delivery/http/routes/assembly-line-reads.ts#L70), [`node-station.ts:36`](libs/assembly-lines/src/node-station.ts#L36))

- **FR6.33** *(added 2026-08-13)* The PR a `push` node produced MUST be recorded on its assembly line. Nothing did: the push recipe ends "commit it, and stop" and deferred to the watcher, which returns early for every CR labelled `assembly-line-id` so that per-CR dedupe cannot route each node of a line into PR creation — so on the merged planning line no spec PR was opened at all and `lore.features.spec_pr_url` stayed null. Recording it on the LINE is the load-bearing half, because `findOpenByPr` matches `args->>'pr_number'`: a line whose PR was never stamped cannot be found when that PR merges, which is exactly what the `merged` wait node needs to be resumable. The node is identified by its `prompt_ref`, not its id, so a line naming it something other than "push" still works; a line already carrying a `pr_number` is skipped, so a push re-run after a correction updates the existing PR instead of forking the review across two. The stamp is committed BEFORE the feature transition and survives it throwing — a stale feature status a later run can correct is a smaller failure than a line stranded at `merged` forever. ([validated by stamps when a push node succeeds on a line with no PR yet](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L137), [`spec-pr.test.ts:141`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L141), [`spec-pr.test.ts:147`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L147), [`spec-pr.test.ts:151`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L151), [`spec-pr.test.ts:155`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L155), [`spec-pr.test.ts:159`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L159), [`spec-pr.test.ts:166`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L166), [`spec-pr.test.ts:185`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L185), [`spec-pr.test.ts:200`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L200), [`spec-pr.test.ts:212`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L212), [`spec-pr.test.ts:225`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L225), [`spec-pr.test.ts:237`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L237), [`spec-pr.test.ts:258`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L258), [`spec-pr.test.ts:268`](apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L268); implemented by [`spec-pr.ts:1`](apps/floor/src/jobs/assembly-run/spec-pr.ts#L1), [`advance.ts:350`](apps/floor/src/jobs/assembly-run/advance.ts#L374), [`spec-pr.test.ts:564`](../../apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L564), [`spec-pr.test.ts:580`](../../apps/floor/src/jobs/assembly-run/spec-pr.test.ts#L580))
- **FR6.32** *(added 2026-08-13)* A feature's WHOLE life is one assembly line, decomposition included. The line no longer ends at `push`: it parks on a `merged` wait node carrying the `pr_merged` signal, and the human who merges the spec PR reports that node's outcome exactly as the author reports theirs. Merging runs `decompose` → `issues` on the SAME line. This replaces a second line started by minting a `feature-decompose` task when a `feature-finalize` PR merged — a task-type predicate that stopped matching the moment finalize became a resume of the planning line, so nothing decomposed and nothing said so. A reviewer requesting changes on the spec PR returns to the `author`, for the same reason the spec analysis's question does: it objects to a plan a PERSON accepted, so only that person can answer. That back-edge needs no `iteration_max` because BOTH ends are wait nodes, while `issues` ⇄ `decompose` keeps its bound — two agents without a referee will argue all day, two humans will not. `decompose` MUST name its own `station_ref`, or it inherits the LINE's task type and runs the planning recipe. ([validated by parks on the merged node once the branch is pushed](libs/assembly-lines/src/planning-author-loop.test.ts#L205), [`planning-author-loop.test.ts:213`](libs/assembly-lines/src/planning-author-loop.test.ts#L213), [`planning-author-loop.test.ts:222`](libs/assembly-lines/src/planning-author-loop.test.ts#L222), [`planning-author-loop.test.ts:231`](libs/assembly-lines/src/planning-author-loop.test.ts#L231), [`planning-author-loop.test.ts:240`](libs/assembly-lines/src/planning-author-loop.test.ts#L240), [`planning-author-loop.test.ts:249`](libs/assembly-lines/src/planning-author-loop.test.ts#L249), [`planning-author-loop.test.ts:259`](libs/assembly-lines/src/planning-author-loop.test.ts#L259), [`planning-author-loop.test.ts:269`](libs/assembly-lines/src/planning-author-loop.test.ts#L269), [`planning-author-loop.test.ts:280`](libs/assembly-lines/src/planning-author-loop.test.ts#L280), [`planning-author-loop.test.ts:293`](libs/assembly-lines/src/planning-author-loop.test.ts#L293), [`planning-author-loop.test.ts:304`](libs/assembly-lines/src/planning-author-loop.test.ts#L304); implemented by [`feature-planning.yaml:1`](libs/assembly-lines/src/assembly-lines/feature-planning.yaml#L1), [`loader.ts:429`](libs/assembly-lines/src/loader.ts#L429))
- **FR6.36** *(added 2026-08-14)* The merge is read off the CLOSED-PR EVENT, which is the only place it reliably arrives. FR6.32 built the resume and hung it off `handleMergedTask` — reached only for tasks the mergeable sweep returns (`status IN ('pr-created','review') AND pr_number IS NOT NULL`). A feature-planning task is `running` with a null `pr_number`, because the push node stamps the LINE's args (FR6.33 — which is what `findOpenByPr` matches on) and nothing copies it onto the task row. So the resume was unreachable for the only PRs it existed to serve: a merged spec PR decomposed on NO deployment, by webhook or by the cron that is meant to be the webhook's safety net — the same shape of dead join FR6.32 was written to remove, one layer further out. The handler rides as an EXTRA on `github.pull_request.closed`, so a throw in it cannot stop the spec-task sync and vice versa; an unmerged close settles the line rather than advancing it; and the target is still a NODE, so a code-review line sharing the PR is passed over rather than pushed through a step it never waited for. ([validated by finds the line by the PR number stamped on its args, not by a task row](apps/floor/src/jobs/spec-pr-resume.test.ts#L47), [`spec-pr-resume.test.ts:53`](apps/floor/src/jobs/spec-pr-resume.test.ts#L53), [`spec-pr-resume.test.ts:68`](apps/floor/src/jobs/spec-pr-resume.test.ts#L68), [`spec-pr-resume.test.ts:74`](apps/floor/src/jobs/spec-pr-resume.test.ts#L74), [`spec-pr-resume.test.ts:84`](apps/floor/src/jobs/spec-pr-resume.test.ts#L84), [`spec-pr-resume.test.ts:94`](apps/floor/src/jobs/spec-pr-resume.test.ts#L94); the decision itself [`decompose-resume.test.ts:226`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L226), [`decompose-resume.test.ts:236`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L236), [`decompose-resume.test.ts:242`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L242), [`decompose-resume.test.ts:248`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L248), [`decompose-resume.test.ts:254`](libs/shared/src/project/assembly-runs/decompose-resume.test.ts#L254) ; implemented by [`decompose-resume.ts:54`](libs/shared/src/project/assembly-runs/decompose-resume.ts#L54))
- **FR6.37** *(added 2026-08-14)* Closing a PR ends only the lines whose choreography OWNS that PR's review. `finishOpenByPr` closed EVERY open line carrying the number, which was safe only while the port's stated invariant held — "only code-review lines carry `pr_number` in args" — and that ended when the push node began stamping the spec PR on the feature-planning line. The result was worse than the missing resume of FR6.36 and hid it: on the SAME merge event the planning line was resumed off its `merged` node and then immediately finished `pr_closed`, one step before decompose, by a handler that had no idea the line existed. Observed on line `7505be4d`. The filter is an explicit definition allowlist passed by the caller rather than a guess inside the adapter, because which lines a PR's close may end is the CALLER's knowledge; an absent filter still closes everything, so no other caller changes behaviour. ([validated by finishOpenByPr leaves a line outside the named definitions alone](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L734), [`assembly-lines.test.ts:753`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L761), [`code-review.test.ts:440`](apps/floor/src/jobs/review/code-review.test.ts#L440); implemented by [`assembly-lines-pg.ts:322`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L322), [`code-review.test.ts:486`](apps/floor/src/jobs/review/code-review.test.ts#L486), [`code-review.test.ts:499`](apps/floor/src/jobs/review/code-review.test.ts#L499))
- **FR6.35** *(added 2026-08-13)* The planning view derives WHAT A FEATURE IS DOING from the assembly line, as one value. It used to branch on `feature.status` plus five booleans rebuilt from the round's task — fragile because `feature.status` had no live writer for `pr-open`, so the column the view branched on was stale by design. The line's open (`outcome === null`) rows say which node is working, and the LAST open row wins because a revisit mints a new one. Elapsed time counts from that NODE, not from the round that contains it. A feature whose planning predates the merged line resolves no line at all, so the round's own rows still decide for it — the fallback is the old behaviour, kept rather than dropped. ([validated by reports planning while the analyze node works](apps/web-ui/src/lib/feature-phase.test.ts#L54), [`feature-phase.test.ts:41`](apps/web-ui/src/lib/feature-phase.test.ts#L68), [`feature-phase.test.ts:53`](apps/web-ui/src/lib/feature-phase.test.ts#L80), [`feature-phase.test.ts:64`](apps/web-ui/src/lib/feature-phase.test.ts#L91), [`feature-phase.test.ts:78`](apps/web-ui/src/lib/feature-phase.test.ts#L105), [`feature-phase.test.ts:87`](apps/web-ui/src/lib/feature-phase.test.ts#L114), [`feature-phase.test.ts:96`](apps/web-ui/src/lib/feature-phase.test.ts#L123), [`feature-phase.test.ts:108`](apps/web-ui/src/lib/feature-phase.test.ts#L135), [`feature-phase.test.ts:161`](apps/web-ui/src/lib/feature-phase.test.ts#L188), [`feature-phase.test.ts:172`](apps/web-ui/src/lib/feature-phase.test.ts#L199), [`feature-phase.test.ts:183`](apps/web-ui/src/lib/feature-phase.test.ts#L210), [`feature-phase.test.ts:194`](apps/web-ui/src/lib/feature-phase.test.ts#L221), [`feature-phase.test.ts:207`](apps/web-ui/src/lib/feature-phase.test.ts#L234), [`feature-phase.test.ts:215`](apps/web-ui/src/lib/feature-phase.test.ts#L242); implemented by [`feature-phase.ts:1`](apps/web-ui/src/lib/feature-phase.ts#L1))

- **FR6.34** *(added 2026-08-13)* Reporting an outcome to a parked wait node is ONE mechanism, owned by the assembly line rather than by whoever happens to be waiting. The author accepting a plan and a reviewer merging a spec PR are the same act — a person completing a `wait` node — so both resolve the target the same way and both insert the same `assembly_run.resume` event. Parked means a row for that node with no outcome yet, on a line that is still `running` or `queued`; the NEWEST such row wins, because a revisit mints a new (nodeId, iteration) row and resuming an older open one would report into a walk that has already passed it. A node that already reported is never a target, so a re-delivered webhook cannot advance the walk twice. ([validated by finds the waiting row for the node it was asked about](libs/shared/src/project/assembly-runs/parked-node.test.ts#L27), [`parked-node.test.ts:37`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L37), [`parked-node.test.ts:45`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L45), [`parked-node.test.ts:53`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L53), [`parked-node.test.ts:63`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L63), [`parked-node.test.ts:75`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L75), [`parked-node.test.ts:83`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L83), [`parked-node.test.ts:92`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L92), [`parked-node.test.ts:106`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L106), [`parked-node.test.ts:115`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L115), [`parked-node.test.ts:126`](libs/shared/src/project/assembly-runs/parked-node.test.ts#L126); implemented by [`parked-node.ts:1`](libs/shared/src/project/assembly-runs/parked-node.ts#L1))

- **FR6.18** *(added 2026-08-12)* A step MUST be able to send work back to the step that fed it. `changes_requested` is that verdict, and the loader is what makes the discipline real: it is a PRODUCIBLE outcome of every agent node and of the `issues` station — the first station that judges its input rather than merely acting on it — so a definition using either MUST route it or fail to load, since `selectEdge` does not fall through. Rework is bounded by `iteration_max` on the back-edge, which the loader also requires: one correction, then the budget is exhausted and the line fails, because two agents disagreeing without a referee will do so indefinitely. A step whose input came from a HUMAN routes `changes_requested` to the exit instead of looping — there is no upstream node to correct, and the settlement returns the work to the person. ([validated by accepts an issues station node](libs/assembly-lines/src/loader.test.ts#L355), [`loader.test.ts:395`](libs/assembly-lines/src/loader.test.ts#L395), [`loader.test.ts:424`](libs/assembly-lines/src/loader.test.ts#L424), [`loader.test.ts:724`](libs/assembly-lines/src/loader.test.ts#L724), [`loader.test.ts:748`](libs/assembly-lines/src/loader.test.ts#L748), [`transition.test.ts:344`](libs/assembly-lines/src/transition.test.ts#L344), [`planning-author-loop.test.ts:141`](libs/assembly-lines/src/planning-author-loop.test.ts#L141), [`planning-author-loop.test.ts:113`](libs/assembly-lines/src/planning-author-loop.test.ts#L113); implemented by [`loader.ts:88`](libs/assembly-lines/src/loader.ts#L88))

- **FR6.17** *(added 2026-08-12)* A node's produced artifact MUST become the next node's INPUT. A step whose deliverable is a file — a spec-change plan, a decomposition — declares it under `output.watch`, and the Floor merges its content into the line's `args`, which is already the channel a node reads its input from (`args.description`, `args.round_feedback`). Routing is the event's own NAME (`spec.plan` → `args.spec_plan`), so a new artifact needs a recipe declaration and a prompt rather than a branch in the Floor. The same path carries objections BACKWARDS: a node that finds its input unusable records `changes_requested` and writes what was wrong, and the back-edge re-runs the upstream node against it. `mergeArgs` is ADDITIVE by key and merged in SQL, never read-modify-write — two nodes can produce artifacts in one tick, and a JS-side merge would let the second read stale args and silently drop the first one's output; a key the patch names IS replaced, because an upstream node re-running after an objection must supersede the output that was rejected. Delivery is best-effort: the run that produced the file already succeeded, so a lost handoff must not retroactively fail it — the consuming node reports its missing input itself. `planning.result` keeps its own handler (the features API is a different destination). *(amended 2026-08-22)* Delivery MUST ride the ADVANCING EVENT, not only the telemetry sink. The sink is a separate HTTP post racing the Kubernetes event the walk advances on, so nothing ordered the merge before the next node's dispatch — the next station could read an arg its predecessor had already produced and simply not find it, which reads as "my predecessor decided nothing" rather than as the delivery failure it is. Both terminal doors (the node event and the reaper's resolve, since a dropped event makes the reaper the only reader that output will ever have) therefore parse the artifacts out of the CR's RAW `status.output` and merge them BEFORE recording the outcome; the sink stays the fast path, and merging identical content twice is a no-op. A declared artifact the agent never produced FAILS the node carrying its name and reason, rather than advancing with a hole in the bag. *(amended 2026-08-25)* A station running in the POOLED SERVICE has no CR output to parse artifacts from, so its `NodeResult` names them directly: an optional `args` map, forwarded as the resume's own args payload and merged into the line before the walk advances — the same channel, entered at the door that form actually reports through. Extras deliberately do not ride it: they are routing and telemetry, not the next node's brief. ([validated by AssemblyLinesPort mergeArgs adds a produced artifact to the line](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1698), [`assembly-line-reads.test.ts:1685`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1717), [`assembly-line-reads.test.ts:1703`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1735), [`assembly-line-reads.test.ts:1719`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1751), [`artifact-args.test.ts:31`](apps/floor/src/jobs/agent/artifact-args.test.ts#L35), [`artifact-args.test.ts:35`](apps/floor/src/jobs/agent/artifact-args.test.ts#L39), [`artifact-args.test.ts:43`](apps/floor/src/jobs/agent/artifact-args.test.ts#L47), [`artifact-args.test.ts:56`](apps/floor/src/jobs/agent/artifact-args.test.ts#L60), [`artifact-args.test.ts:72`](apps/floor/src/jobs/agent/artifact-args.test.ts#L72), [`artifact-args.test.ts:83`](apps/floor/src/jobs/agent/artifact-args.test.ts#L83), [`artifact-args.test.ts:96`](apps/floor/src/jobs/agent/artifact-args.test.ts#L96), [`artifact-args.test.ts:117`](apps/floor/src/jobs/agent/artifact-args.test.ts#L117), [`artifact-args.test.ts:128`](apps/floor/src/jobs/agent/artifact-args.test.ts#L128), [`artifact-args.test.ts:139`](apps/floor/src/jobs/agent/artifact-args.test.ts#L139), [`artifact-args.test.ts:147`](apps/floor/src/jobs/agent/artifact-args.test.ts#L147), [`node-event-handler.test.ts:442`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L442), [`node-event-handler.test.ts:453`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L453), [`node-event-handler.test.ts:465`](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L465), [`assembly-run-reaper.test.ts:808`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L808), [`node-result-schema.test.ts:86`](libs/assembly-lines/src/node-result-schema.test.ts#L86), [`node-result-schema.test.ts:95`](libs/assembly-lines/src/node-result-schema.test.ts#L95), [`run-node.test.ts:118`](apps/stations/src/kernel/run-node.test.ts#L118), [`run-node.test.ts:135`](apps/stations/src/kernel/run-node.test.ts#L135), [validated by spec.plan lands in args before write launches](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L96), [validated by the issues node reports through the service door and closes the run](apps/floor/src/jobs/assembly-run/feature-planning-acceptance.test.ts#L144); implemented by [`artifact-args.ts:45`](apps/floor/src/jobs/agent/artifact-args.ts#L45), [`assembly-lines-pg.ts:161`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L161))

- **FR6.16** *(added 2026-08-10)* A task branch MUST exist before an Agent CR is told to check it out. The per-task recipe pins `ref: <branch>` and the subsystem's init runs a bare `git checkout <ref>`, which exits 1 on a branch that is not there — and the branch name embeds the task id, so for a fresh task it never is. The three in-process handlers (onboard / feature-request / feature-finalize) already create their branch before writing to it; the Agent-CR path did not, so every task type dispatched onto a fresh branch died in its init container with `pathspec '…' did not match any file(s) known to git` before the agent ever ran. Creation MUST be conditional on the remote being known to lack the branch: `createBranch` force-resets an existing one (delete + recreate at base), so an unconditional call would discard the commits of a revision task resuming its own branch. An adapter that cannot report existence, or a probe that fails, leaves the branch alone — guessing here destroys work. A creation failure propagates, because a task that fails with a legible reason beats a pod that dies in init. ([validated by `ensure-task-branch.test.ts:21`](apps/floor/src/jobs/task/ensure-task-branch.test.ts#L21), [`ensure-task-branch.test.ts:31`](apps/floor/src/jobs/task/ensure-task-branch.test.ts#L31), [`ensure-task-branch.test.ts:39`](apps/floor/src/jobs/task/ensure-task-branch.test.ts#L39), [`ensure-task-branch.test.ts:47`](apps/floor/src/jobs/task/ensure-task-branch.test.ts#L47), [`ensure-task-branch.test.ts:63`](apps/floor/src/jobs/task/ensure-task-branch.test.ts#L63); implemented by [`ensure-task-branch.ts:30`](apps/floor/src/jobs/task/ensure-task-branch.ts#L30), [`handle-claude-code-task.ts:55`](apps/floor/src/jobs/task/handle-claude-code-task.ts#L55), [`platform-github.ts:346`](libs/shared/src/project/lib/platform-github.ts#L346))

- **FR6.38** *(added 2026-08-14)* A run CLONES its blueprint. `project.assemblyRuns.start()` snapshots the resolved definition graph onto the run row, and every later step — the walk, the reads, the UI — reads THAT, never the YAML by name. Before this a run referenced its definition by name only and `advanceLine` re-resolved the file off the Floor's image on every step, so editing a YAML changed the graph under a walk already in flight, and a renamed or deleted definition made its own history undrawable: the read API served nulls for node type and Station, and the UI synthesized a chain of `always` edges nobody had declared and drew it as if it were the design. The snapshot is written ONCE, for the same reason the definition hash was: it names the graph the run's node rows were produced by, so a redelivered start loading a since-edited definition must not rewrite what the rows actually came from. Stations are resolved AT CLONE TIME rather than on each read, which is what stops `station_ref` inheritance from being a runtime hazard — an inherited Station becomes a recorded fact instead of a derivation repeated at every call site. Rows predating the snapshot carry no graph and fall back to the name, because a run that already exists must stay inspectable. ([validated by stampBlueprint stores the hash and the cloned graph together](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L307), [`assembly-runs.test.ts:334`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L342), [`assembly-runs.test.ts:355`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L363), [`start-event-handler.test.ts:255`](apps/floor/src/jobs/assembly-run/start-event-handler.test.ts#L255), [`advance.test.ts:264`](apps/floor/src/jobs/assembly-run/advance.test.ts#L264), [`advance.test.ts:291`](apps/floor/src/jobs/assembly-run/advance.test.ts#L291), [`snapshot-graph.test.ts:47`](libs/assembly-lines/src/snapshot-graph.test.ts#L47), [`snapshot-graph.test.ts:55`](libs/assembly-lines/src/snapshot-graph.test.ts#L55), [`snapshot-graph.test.ts:64`](libs/assembly-lines/src/snapshot-graph.test.ts#L64), [`snapshot-graph.test.ts:72`](libs/assembly-lines/src/snapshot-graph.test.ts#L72), [`snapshot-graph.test.ts:80`](libs/assembly-lines/src/snapshot-graph.test.ts#L80), [`snapshot-graph.test.ts:93`](libs/assembly-lines/src/snapshot-graph.test.ts#L93), [`snapshot-graph.test.ts:97`](libs/assembly-lines/src/snapshot-graph.test.ts#L97), [`snapshot-graph.test.ts:111`](libs/assembly-lines/src/snapshot-graph.test.ts#L111), [`resolve-run-graph.test.ts:32`](libs/assembly-lines/src/resolve-run-graph.test.ts#L32), [`resolve-run-graph.test.ts:45`](libs/assembly-lines/src/resolve-run-graph.test.ts#L45), [`resolve-run-graph.test.ts:61`](libs/assembly-lines/src/resolve-run-graph.test.ts#L61), [`assembly-line-reads.test.ts:117`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L117), [`assembly-line-reads.test.ts:160`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L160), [`assembly-lines.test.ts:250`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L250); implemented by [`snapshot-graph.ts:1`](libs/assembly-lines/src/snapshot-graph.ts#L1), [`advance.ts:160`](apps/floor/src/jobs/assembly-run/advance.ts#L160), [`start-event-handler.ts:102`](apps/floor/src/jobs/assembly-run/start-event-handler.ts#L102))

- The run views draw THAT graph too: the API serves it and web-ui stopped keeping a hand-transcribed copy of the builtin YAMLs, which could only ever describe the CURRENT blueprint and went stale the moment one changed. ([validated by draws the graph the run RECORDED, not a transcription of today's yaml](apps/web-ui/src/lib/run-graph-definition.test.ts#L41), [`run-graph-definition.test.ts:58`](apps/web-ui/src/lib/run-graph-definition.test.ts#L58), [`feature-run.test.ts:103`](apps/web-ui/src/lib/feature-run.test.ts#L99); implemented by [`run-graph-definition.ts:60`](apps/web-ui/src/lib/run-graph-definition.ts#L60))

- **FR6.39** *(added 2026-08-14)* A station run has an IDENTITY, not a naming convention. Each `(run, node, iteration)` row carries a `station_run_id` UUID, and the per-tool-call telemetry (`agent_run_events`, `agent_run_turns`) and cost rows key on it. They previously correlated by matching `agent_cr_name` — a string built from a 12-hex prefix of the run id plus the node id — through a `LEFT JOIN LATERAL` that broke ties with "newest node row wins", so two runs colliding on their id prefix silently attributed one's telemetry to the other, and the correlation could not be expressed as a foreign key even in principle. The id rides the CR's labels, so the Floor resolves it from what it already named; the pod is not required to echo it back, which keeps the change inside this repo. The existing BIGINT row id stays as the VISIT ORDER the replay depends on — a v4 UUID does not sort, and `listStationRuns` returning rows out of visit order would reorder the walk's own history. A converged duplicate returns the id already minted, because the id names the VISIT and a second one would give one pod two names; a revisit is a different visit and gets its own. The id rides the Agent CR's LABELS so a pod found in the cluster maps back to its visit without re-deriving anything from the CR name, and the telemetry writers resolve it ONCE at write time — `agent_run_events`, `agent_run_turns` and the cost rows all key on it thereafter. The pod is deliberately not asked to echo it back: the Floor named the CR, so the correlation improves with no change in the external ai-agent-subsystem repo. ([validated by ensureStationRun returns a station run uuid, unchanged by a converged duplicate](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L485), [`assembly-runs.test.ts:504`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L512), [`advance.test.ts:238`](apps/floor/src/jobs/assembly-run/advance.test.ts#L238), [`agent-run-events.test.ts:443`](libs/shared/src/project/agent-run-events/agent-run-events.test.ts#L443), [`assembly-lines.test.ts:458`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L458), [`floor-assembly-run.test.ts:213`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L213), [`floor-assembly-run.test.ts:235`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L235), ; implemented by [`assembly-runs-pg.ts:192`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L192), [`agent-run-events-pg.ts:86`](libs/shared/src/project/agent-run-events/agent-run-events-pg.ts#L86), [`advance.ts:283`](apps/floor/src/jobs/assembly-run/advance.ts#L307))

- **FR6.40** *(added 2026-08-14, supersedes the `signal` half of FR6.19)* A human station names the PAGE its worker acts on. `type: wait` + `signal` said only THAT a person acts and left WHERE to be reconstructed elsewhere — so the web UI carried a hardcoded map of node ids to interface phases, transcribing the blueprint's knowledge into a view exactly as the hand-copied definition catalog did, and a node added to a definition was invisible to the interface until someone remembered to extend the map. The node instead declares a TYPE naming its form contract (`feature_review` — the planning wizard's per-section feedback; `pr_review` — the GitHub PR view) and a `route` template naming where that form lives: relative for a page this platform serves, absolute for a surface it does not own. `signal` is deleted rather than kept alongside, because it existed only to distinguish which human surface reports and the type now says that outright. The template resolves against the run's args AT READ TIME, never at clone time — `args.pr_url` is stamped only once the push node has opened the PR (FR6.33), so a route resolved at start would name a page that does not exist yet. Placeholders may reference `args` only, which is what keeps the engine domain-free: it still never learns what a feature is. Every consequence FR6.19 established holds unchanged — the walk records the row and dispatches nothing, the reaper never times it out, and a back-edge touching one needs no `iteration_max` — and all three now key on ONE predicate rather than on the string `wait` repeated at each site, so registering a human station is a single entry rather than five edits nobody can find. ([validated by accepts a human station and an unbounded back-edge out of it](libs/assembly-lines/src/loader.test.ts#L204), [`loader.test.ts:287`](libs/assembly-lines/src/loader.test.ts#L287), [`loader.test.ts:308`](libs/assembly-lines/src/loader.test.ts#L308), [`loader.test.ts:330`](libs/assembly-lines/src/loader.test.ts#L330), [`node-station.test.ts:36`](libs/assembly-lines/src/node-station.test.ts#L36), [`assembly-line-reads.test.ts:100`](apps/floor/src/delivery/http/routes/assembly-line-reads.test.ts#L100), [`assembly-run-reaper.test.ts:217`](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L217), [`run-graph.test.ts:9`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L9), [`run-graph.test.ts:15`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L15), [`run-graph.test.ts:22`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L22), [`run-graph.test.ts:35`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L35), [`run-graph.test.ts:44`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L44), [`run-graph.test.ts:53`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L53), [`run-graph.test.ts:59`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L59), [`run-graph.test.ts:63`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L63), [`run-graph.test.ts:67`](libs/shared/src/project/assembly-runs/run-graph.test.ts#L67), [`human-station.test.ts:5`](apps/web-ui/src/lib/human-station.test.ts#L5), [`human-station.test.ts:13`](apps/web-ui/src/lib/human-station.test.ts#L13), [`human-station.test.ts:21`](apps/web-ui/src/lib/human-station.test.ts#L21), [`human-station.test.ts:26`](apps/web-ui/src/lib/human-station.test.ts#L26), [`human-station.test.ts:31`](apps/web-ui/src/lib/human-station.test.ts#L31); implemented by [`human-station.ts:1`](libs/assembly-lines/src/human-station.ts#L1), [`loader.ts:311`](libs/assembly-lines/src/loader.ts#L311), [`feature-planning.yaml:29`](libs/assembly-lines/src/assembly-lines/feature-planning.yaml#L29), [`run-graph.ts:66`](libs/shared/src/project/assembly-runs/run-graph.ts#L66))

- The planning view reads a human station's phase off that type, so a station added or renamed in the blueprint reports itself without anyone editing a list of node ids in the view. ([validated by reads a human station's phase from its declared type, not from a node-id list](apps/web-ui/src/lib/feature-phase.test.ts#L27); implemented by [`feature-phase.ts:39`](apps/web-ui/src/lib/feature-phase.ts#L39))

- **FR6.40a** *(added 2026-08-19)* The enriched run read is served by **lore-api**,
  not the Floor. It answers with the run, one row per station run in visit order,
  and per node the Station it dispatches to — `type`, `promptRef`, `station`,
  `stationInherited`, and the resolved `route` a human station's worker acts on.
  A run whose blueprint is gone and which predates clones reports
  `definitionKnown: false` and still serves its rows, because the rows are the
  record of what actually ran. The old comment on the Floor's copy claimed this
  had to live there because the definitions were "deliberately absent from
  lore-api's lean image"; that was a packaging fact, not a boundary, and
  lore-api's Dockerfile now builds `libs/assembly-lines` as the Floor's does. ([validated by `run-read.test.ts:27`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L27), [`run-read.test.ts:34`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L34), [`run-read.test.ts:42`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L42), [`run-read.test.ts:72`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L72), [`run-read.test.ts:105`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L105), [`run-read.test.ts:137`](apps/lore-api/src/api/routes/assembly-lines/run-read.test.ts#L137))

- **FR6.41** *(added 2026-08-14)* The runtime model is NAMED for what it is, and the rename ships READERS-FIRST. `pipeline.assembly_runs` held runs, not assembly lines, so the word meant both the authored blueprint and one execution of it. The blueprint keeps the name `AssemblyLine`; one execution is an `AssemblyRun` and one node visit is a `StationRun`. But a rename across separately-deployed images is expand/contract, and the order is not optional: every consumer must ACCEPT both spellings in one release BEFORE any producer emits the new one. This release is the accept half only — the registry answers to `assembly_run.*` as well as `assembly_run.*`, both CR-label spellings are read, the trailer parser reads both keys, and every route is served at both paths — while every WRITER still emits the old name. Flipping a writer in the same release breaks two directions at once: a peer that has not rolled yet, and a rollback, whose failures are a dead-lettered event (a person's plan-accept vanishing behind a 202) and an in-flight node CR routed into the single-agent PR path. For the same reason only the two tables this model OWNS are renamed, behind compat views: the telemetry tables that merely POINT at a run keep `assembly_line_id`, because a renamed column on a same-named table has no view to hide behind and a rolled-back Floor's batch insert would fail whole. The producers flip in a follow-up, once every deployed reader accepts both (FR6.44). ([validated by still reads the pre-rename Lore-Assembly-Run key](libs/shared/src/commit-trailers.test.ts#L102), [`commit-trailers.test.ts:44`](libs/shared/src/commit-trailers.test.ts#L44), [`registry.test.ts:28`](apps/floor/src/main-loop/registry.test.ts#L28), [`assembly-lines.test.ts:363`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L363), [`assembly-lines.test.ts:379`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L379), [`assembly-lines.test.ts:269`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L269), [`k8s-map.test.ts:28`](libs/shared/src/project/events/k8s-map.test.ts#L28), [`k8s-map.test.ts:34`](libs/shared/src/project/events/k8s-map.test.ts#L34), [`assembly-lines.test.ts:67`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L67); implemented by [`registry.ts:82`](apps/floor/src/main-loop/registry.ts#L82), [`commit-trailers.ts:13`](libs/shared/src/commit-trailers.ts#L13), [`k8s-map.ts:15`](libs/shared/src/project/events/k8s-map.ts#L15))

- **FR6.42** *(added 2026-08-14)* Runs are QUERYABLE — by repo, by blueprint, and by both. The port offered lookups by task, by PR number, and by openness, so the ordinary operational question ("every code-review run on this repo", "what has this blueprint been doing") had no answer short of hand-written SQL, and the runs list could not be filtered at all. One `list(query)` read answers all of them; `listForTask` / `listOpen` / `findOpenByPr` remain as named callers of it, because each states an intent the walk depends on, but the SQL exists once. Filters are ANDed and every one is optional, so an empty query is "the newest runs". Ordering is newest-first with the id as a tiebreak, which buys STABILITY rather than chronology — run ids are random uuids, so two runs created in the same millisecond come back in a fixed but arbitrary order in Postgres and in the double alike. The blueprint filter accepts several names at once because the PR-lifecycle choreography asks by family, and a repo ID is resolved to the canonical `owner/repo` by the caller rather than denormalized onto the row, where a GitHub rename would silently rot it. ([validated by list returns only the runs of the named blueprint, newest first](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L373), [`assembly-runs.test.ts:393`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L401), [`assembly-runs.test.ts:410`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L418), [`assembly-runs.test.ts:436`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L444), [`assembly-runs.test.ts:464`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L472), [`assembly-lines.test.ts:233`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L233); implemented by [`assembly-runs-pg.ts:303`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L303), [`assembly-runs-memory.ts:239`](libs/shared/src/project/assembly-runs/assembly-runs-memory.ts#L239))

- The HTTP surface carries the same filter, so the question is answerable without SQL. ([validated by narrows to one blueprint on its own, which nothing could ask for before](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L131); implemented by [`assembly-lines.ts:29`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.ts#L29))

- **FR6.43** *(added 2026-08-15)* Every implementation of `AssemblyRunsPort` MUST satisfy ONE behavioural contract, executed against all of them — a port whose double is verified by behaviour and whose adapter is verified by SQL text lets the two disagree while both suites pass. The contract asserts behaviour only; SQL text belongs in the adapter's own file. The Postgres run requires a migrated database and SKIPS without one, but the skip is a named test rather than a swallowed warning and `LORE_REQUIRE_PG_CONTRACT=1` makes it a failure, so an environment meant to exercise the SQL cannot quietly stop. ([validated by the Postgres implementation is actually exercised](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L43), [`assembly-runs.contract.test.ts:135`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L135), [`assembly-runs.contract.test.ts:149`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L149), [`assembly-runs.contract.test.ts:161`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L161), [`assembly-runs.contract.test.ts:174`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L174), [`assembly-runs.contract.test.ts:183`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L183), [`assembly-runs.contract.test.ts:273`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L273), [`assembly-runs.contract.test.ts:290`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L290), [`assembly-runs.contract.test.ts:342`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L342), [`assembly-runs.contract.test.ts:359`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L359), [`assembly-runs.contract.test.ts:409`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L409), [`assembly-runs.contract.test.ts:422`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L422), [`assembly-runs.contract.test.ts:440`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L440), [`assembly-runs.contract.test.ts:454`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L454), [`assembly-runs.contract.test.ts:463`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L463), [`assembly-runs.contract.test.ts:490`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L490), [`assembly-runs.contract.test.ts:500`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L500), [`assembly-runs.contract.test.ts:517`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L517), [`assembly-runs.contract.test.ts:545`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L545), [`assembly-runs.contract.test.ts:560`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L560), [`assembly-runs.contract.test.ts:581`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L581), [`assembly-runs.contract.test.ts:609`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L609))

- **FR6.44** *(added 2026-08-17)* The producers flip only once the accept half is DEPLOYED, and what they flip is bounded by who else has to move. FR6.41's accept release shipped in `8cc5609c` and reached the cluster on 2026-08-17 (`e499668e`, an ancestor-verified descendant, deploy and smoke green), so `assembly_run.start` / `.resume`, the `station-run-id`-era CR label and the `Lore-Assembly-Run:` trailer are now what the writers emit. Three of FR6.41's four dual-spellings deliberately do NOT flip with them: the HTTP route aliases stay because the deployed web-ui still calls `/api/assembly-lines/*` and a UI is a separately released image, the telemetry pointer columns stay because renaming a column no view can hide needs a dual-column write before a read moves, and every legacy READER stays forever — a trailer parser reads git history, and a registry reads rows queued by the image it replaced. Those legacy readers are spelled as LITERALS, never as the writer's constant: the registry once expressed its pre-rename entries with `RUN_START_EVENT`, so flipping that constant silently deleted the legacy handler and registered the new name twice, leaving the Floor deaf to exactly the spelling still sitting in the events table. A frozen wire value has to be written out precisely because the constant beside it is the thing that moves. *(Amended 2026-08-18, #1272: the registry's legacy entries and the start handler's `definitionName` fallback were deleted one events-retention window after the flip — the literal-spelling rule still binds any future frozen wire value.)* ([validated by RUN_START_EVENT is assembly_run.start](libs/shared/src/project/assembly-runs/run-events.test.ts#L5), [`run-events.test.ts:9`](libs/shared/src/project/assembly-runs/run-events.test.ts#L9), [`registry.test.ts:34`](apps/floor/src/main-loop/registry.test.ts#L34), [`commit-trailers.test.ts:44`](libs/shared/src/commit-trailers.test.ts#L44), [`commit-trailers.test.ts:102`](libs/shared/src/commit-trailers.test.ts#L102); implemented by [`run-events.ts:15`](libs/shared/src/project/assembly-runs/run-events.ts#L15), [`registry.ts:92`](apps/floor/src/main-loop/registry.ts#L92), [`floor-assembly-run.ts:49`](apps/floor/src/jobs/assembly-run/floor-assembly-run.ts#L49))

- **FR6.45** *(added 2026-08-18)* The event-payload key follows the same readers-first path as the event names: every `assembly_run.*` / `kubernetes.agent_node.*` handler MUST accept `params.assemblyRunId` (new key winning) with `params.assemblyLineId` as the fallback for rows queued by the pre-flip image, BEFORE any writer emits the new key — the writers (`assembly-runs-pg` start/resume CTEs, `parked-node`, the k8s listener) flip only once this accept half is deployed. ([validated by `event-params-compat.test.ts:30`](apps/floor/src/jobs/assembly-run/event-params-compat.test.ts#L30), [`event-params-compat.test.ts:61`](apps/floor/src/jobs/assembly-run/event-params-compat.test.ts#L61), [`event-params-compat.test.ts:89`](apps/floor/src/jobs/assembly-run/event-params-compat.test.ts#L89))

- **FR6.49** *(added 2026-08-19)* A run MAY declare the SUBJECT it works on, and at most one OPEN run may hold a given `(repo, subject_key)`. The key names the subject — `feature:<uuid>`, `detect:<blueprint>:<repo>`, `ingest:<kind>:<ref>[:<chunk>]`, `review:pr-<n>` — never the action, because `feature:<id>:finalize` would guard repeat finalizes while still letting a planning line and a finalize line work one feature at once, which is the thing that actually went wrong. It is OPT-IN: a run with no key is unconstrained, so lines that are MEANT to overlap (comment-triage and code-review-reply carry distinct human comments on one PR branch) declare nothing, which replaces the old opt-out-by-blueprint-name list. Enforcement is a partial unique index, not a check-then-act read, so two callers racing cannot both pass. Every producer and reader builds its key from ONE module: `subject_key` is a wire format between images (the Floor stamps it, lore-api queries it), so two independent spellings would not fail to compile — they would simply never match, which reads exactly like "nothing is in flight", the very condition the key exists to detect. ([validated by start with a subject key already open returns the run already in flight](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L622), [`assembly-runs.contract.test.ts:638`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L638), [`assembly-runs.contract.test.ts:670`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L670), [`assembly-runs.contract.test.ts:681`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L681), [`spec-trace-dispatch.test.ts:186`](apps/floor/src/jobs/spec-trace/spec-trace-dispatch.test.ts#L186); implemented by [`0041_assembly_runs_subject_key.sql`](infra/terraform/modules/gke-mcp/lore-platform/charts/ui-helm/migrations/0041_assembly_runs_subject_key.sql), [`assembly-runs.test.ts:57`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L57), [`assembly-runs.test.ts:70`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L70), [`assembly-runs.test.ts:1584`](libs/shared/src/project/assembly-runs/assembly-runs.test.ts#L1616), [`spec-trace-dispatch.test.ts:208`](apps/floor/src/jobs/spec-trace/spec-trace-dispatch.test.ts#L208), [`subject-keys.test.ts:12`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L12), [`subject-keys.test.ts:18`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L18), [`subject-keys.test.ts:24`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L24), [`subject-keys.test.ts:28`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L28), [`subject-keys.test.ts:32`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L32), [`subject-keys.test.ts:38`](libs/shared/src/project/assembly-runs/subject-keys.test.ts#L38); implemented by [`subject-keys.ts`](libs/shared/src/project/assembly-runs/subject-keys.ts))

- **FR6.50** *(added 2026-08-19)* `start` is start-or-JOIN: a subject already in flight yields the id of the run doing the work rather than minting a second one or throwing, so every caller ends up holding a usable run id and a duplicate request can answer with the work already happening. A settled run frees its subject, and a FORK inherits its source's subject — a fork re-runs the same work, so it must hold the guard its source held and answer the same subject query; that is safe because forking is legal only from a terminal run. A caller whose start JOINED owns no CR and will never complete on its own, so it is told (`joinedRun`) and MUST settle its task rather than leave it running. ([validated by a settled run frees its subject key for the next start](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L651), [`assembly-runs.contract.test.ts:774`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L774), [`assembly-run-station-backend.test.ts:140`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L140), [`assembly-run-station-backend.test.ts:131`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L131), [`assembly-run-station-backend.test.ts:155`](apps/floor/src/jobs/assembly-run/assembly-run-station-backend.test.ts#L155))

- **FR6.51** *(added 2026-08-19)* The subject is also the QUERY key: `GET /api/assembly-runs?subject_key=…` answers "what has worked on this thing", newest first, whatever blueprint each run was. This is what a reader uses instead of resolving a run through a task id and a blueprint name — a lookup that could only ever find runs some task started for one round, and so hid a feature's finalize run from its own page (FR-12.5 of specs/7-feature-planning). A producer that would START a run asks first and refuses a duplicate with `409` naming the run in flight; a producer that would create a job_run asks BEFORE minting it, since a job_run for work already running has no owner to close it. That pre-check closes the common case but not the race: two ticks can both read "nothing in flight" before either starts. The loser's start() JOINS, and because the run carries the job_run_id of whichever tick actually started it, a mismatch IS the join — so the loser closes its own job_run rather than leaving an orphan nothing reaps. ([validated by list by subject returns that subject's runs whatever blueprint they ran](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L751), [`assembly-runs.contract.test.ts:697`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L697), [`assembly-runs.contract.test.ts:713`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L713), [`features.test.ts:741`](apps/lore-api/src/api/routes/features/features.test.ts#L741), [`features.test.ts:807`](apps/lore-api/src/api/routes/features/features.test.ts#L807), [`fan-out.test.ts:190`](apps/floor/src/jobs/detect/fan-out.test.ts#L190), [`assembly-runs.contract.test.ts:745`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L745), [`fan-out.test.ts:128`](apps/floor/src/jobs/detect/fan-out.test.ts#L128), [`fan-out.test.ts:151`](apps/floor/src/jobs/detect/fan-out.test.ts#L151))

- **FR6.54** *(added 2026-08-20)* A node that failed MUST be classified from the AGENT'S OWN last words, not from the Job-level reason. The Kubernetes `failureReason` is `BackoffLimitExceeded` however the agent died, so reading it first classified every death identically; the text that says what actually happened rides the terminal `is_error` result line of the NDJSON stream, which only exists BEFORE the output is unwrapped. `normalizeAgentStatus` therefore lifts it into `errorText` at the moment it unwraps, and both doors into the walk — the node-event handler and the reaper — read it from there. *(amended 2026-08-30)* There are TWO shapes of those last words, and the lift reads both in order of richness: claude's terminal `is_error` result line, and — when the engine dies at BOOT and never reaches one — the runner's relayed stderr, the `[agent] …` lines it echoes between lifecycle envelopes. With only the first, a boot crash fell through to the Job-level reason after all, which is the very thing this requirement forbids: run `129235d4` (2026-08-28) printed `Error: Settings file not found: /agent/.claude/settings.json`, was classified `infra` off `BackoffLimitExceeded`, and spent a 25-minute `implement` retry on a permanent misconfiguration. Two gates keep ordinary output from being read as a cause: the line must carry the runner's own prefix, and the run must carry a lifecycle envelope reporting the agent phase FAILED. The class itself comes from the shared `classifyError`, now the only failure matcher in the platform: the second one it replaces (`isBillingError`) disagreed with it about what "out of credits" meant. **This is also the fix for the defect that made FR6.14 inert** — the alert ran downstream of the unwrap, so the classifier read a string that could never be a billing error and the account-dry alarm never fired once in production (#1455). An EMPTY `errorText` is silence, not speech: `terminalErrorText` answers `parsed.result` for any line carrying `is_error`, so an agent that errors with an empty result string must fall through to the Job-level reason rather than win the precedence with nothing to say. ([validated by the agent's own terminal text is classified, not the Job reason](libs/assembly-lines/src/node-outcome.test.ts#L135), [the Job reason is used only when the agent printed nothing](libs/assembly-lines/src/node-outcome.test.ts#L153), [`node-outcome.test.ts` unknown-class case](libs/assembly-lines/src/node-outcome.test.ts#L188), [a success carries no class](libs/assembly-lines/src/node-outcome.test.ts#L197), [the error text is lifted off the raw stream before unwrapping](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L196), [a clean stream carries no error text](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L210), [re-normalizing keeps what the first pass lifted](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L222), [the REAL alert path fires end to end for a pod that died out of credits](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L195), [the dropped-event door alerts too](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L365), [the alert carries the remediation hint](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L27), [a rate limit is not an empty account](apps/floor/src/jobs/assembly-run/billing-alert.test.ts#L50), [`error-classify.test.ts` insufficient-credit phrasing](libs/shared/src/error-classify.test.ts#L44), [the agent's bare terminal text classifies](libs/shared/src/error-classify.test.ts#L52), [a Job-level backoff classifies as infra](libs/shared/src/error-classify.test.ts#L58), [a deadline and an eviction classify as infra](libs/shared/src/error-classify.test.ts#L66), [`node-outcome.test.ts:170`](libs/assembly-lines/src/node-outcome.test.ts#L170); implemented by [`node-outcome.ts:113`](libs/assembly-lines/src/node-outcome.ts#L113), [`node-terminal.ts:42`](apps/floor/src/jobs/assembly-run/node-terminal.ts#L42), [`error-classify.ts:33`](libs/shared/src/error-classify.ts#L33), [returns the relayed stderr line of a run the lifecycle says failed](libs/assembly-lines/src/agent-output.test.ts#L395), [returns the LAST relayed line, which is the one that killed it](libs/assembly-lines/src/agent-output.test.ts#L401), [says nothing about a run the lifecycle never reported as failed](libs/assembly-lines/src/agent-output.test.ts#L414), [ignores model prose and tool output, reading only the runner's own prefix](libs/assembly-lines/src/agent-output.test.ts#L424), [returns null for empty, absent, or unstructured output](libs/assembly-lines/src/agent-output.test.ts#L468), [caps the text at 300 chars, like the result-line reader](libs/assembly-lines/src/agent-output.test.ts#L474), [says nothing when the phase that failed was not the agent's](libs/assembly-lines/src/agent-output.test.ts#L435), [still reads a failed envelope that names no phase at all](libs/assembly-lines/src/agent-output.test.ts#L445), [reads the line that preceded the failure, not one logged after it](libs/assembly-lines/src/agent-output.test.ts#L456), [lifts the relayed stderr when the engine died before any result line](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L128), [still prefers a real result line over the relayed stderr](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L134))

- **FR6.55** *(added 2026-08-20)* The classification MUST be PERSISTED on the station run — `failure_class` and `failure_detail`, migration `0042`. Before this the only record of why a node died lived in the Agent CR, pruned about an hour after it goes terminal, and in the pod, which is garbage-collected: an hour later a failed run was undiagnosable from Lore's own data and answering "why" meant reaching for `kubectl`. Both columns are nullable and a NULL means "not classified", never "classified as fine" — every visit recorded before the migration has a cause that was thrown away, and no backfill can honestly invent one. A FORK inherits the visits but not their verdict: `failure_class`/`failure_detail` classify the attempt that is over, and copying them into a fresh run makes the replay refuse a retry on inherited history — so a fork taken to rerun a credit failure would die of the failure it exists to get past, on its first advance. ([validated by the class and detail survive a round trip through the port](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L304), [a success leaves both columns null](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L325), [the walk records them on the run it just finished](apps/floor/src/jobs/assembly-run/advance.test.ts#L877), [`assembly-runs.contract.test.ts:811`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L811); implemented by [`0042_station_runs_failure.sql`](infra/terraform/modules/gke-mcp/lore-platform/charts/ui-helm/migrations/0042_station_runs_failure.sql), [`station-run.ts:18`](libs/shared/src/models/station-run.ts#L18), [`advance.ts:333`](apps/floor/src/jobs/assembly-run/advance.ts#L357))

- **FR6.56** *(added 2026-08-20)* A back-edge MUST NOT spend its `iteration_max` budget on a failure no retry can clear. `anthropic-credit`, `auth` and the two GitHub permission classes need a balance, a credential or a grant to change first, so a retry buys a second identical failure several minutes later and then reports the EDGE BUDGET as the cause — which is exactly what a planning round told its author on 2026-08-20. A rate limit is deliberately NOT permanent, and neither is `infra` or `unknown`: a crashed pod is the case the budget exists for. `unclaimed` — the class a queue-timeout carries since 2026-08-30 — IS permanent, and is the one member of the set that is not about a credential: no pod ever started, so re-running the node BEFORE it cannot change whether a cluster exists to take the next one. It was `infra`, which is retryable, and five consecutive implementation-loop runs therefore re-ran a 25-minute `implement` against a paused `central`, waited out a second 30-minute queue-timeout, and reported the exhausted edge budget as the cause (#1648). The zod schema that carries a class across the station boundary is DERIVED from this list rather than copied, because zod drops what it does not declare — a hand-mirrored enum would erase the new class instead of failing on it. Only the RETRY is suppressed, never the ROUTE — a `failed` edge that goes forward still goes forward, or a permanent failure would silently skip the retrospective every line runs on its way out. The category predicate answers for DECLARED categories only — `failure_class` is a plain text column read straight back into a visit, and a prototype key reaching `HINTS[...]` would put a function where the remediation line goes. The infra matcher is anchored to the Kubernetes phrasings for the same reason: a bare `timed out` also matches the agent's own request timeout, and the infra hint would then tell an operator the pod died when it did not. ([validated by a node the account cannot pay for is not retried](libs/assembly-lines/src/transition.test.ts#L128), [the failure names the cause, not the budget it declined to spend](libs/assembly-lines/src/transition.test.ts#L142), [a rate limit is still retried](libs/assembly-lines/src/transition.test.ts#L161), [an unclassified failure is still retried](libs/assembly-lines/src/transition.test.ts#L175), [a permanent failure still routes FORWARD](libs/assembly-lines/src/transition.test.ts#L185), [a dry account is permanent](libs/assembly-lines/src/failure-reason.test.ts#L52), [a rate limit and an unclassified failure are not](libs/assembly-lines/src/failure-reason.test.ts#L61), [an unrecognised class is not permanent](libs/assembly-lines/src/failure-reason.test.ts#L71), [the permanent set](libs/shared/src/error-classify.test.ts#L146), [`error-classify.test.ts` rate-limit case](libs/shared/src/error-classify.test.ts#L154), [infra and unknown are worth a retry](libs/shared/src/error-classify.test.ts#L158), [`error-classify.test.ts:117`](libs/shared/src/error-classify.test.ts#L117), [`error-classify.test.ts:127`](libs/shared/src/error-classify.test.ts#L127), [`error-classify.test.ts:135`](libs/shared/src/error-classify.test.ts#L135), [`error-classify.test.ts:139`](libs/shared/src/error-classify.test.ts#L139), [returns true for unclaimed — nothing ran, so a retry buys a second wait](libs/shared/src/error-classify.test.ts#L163), [carries a remediation hint for unclaimed that points at the registry](libs/shared/src/error-classify.test.ts#L167), [fails the run instead of re-launching implement](libs/assembly-lines/src/transition.test.ts#L500), [reports the paused cluster as the cause, not the spent retry budget](libs/assembly-lines/src/transition.test.ts#L527), [still routes a genuine validate failure back to implement](libs/assembly-lines/src/transition.test.ts#L527), [fails the run once and never re-dispatches implement](apps/floor/src/jobs/assembly-run/assembly-run-reaper.test.ts#L1184), [accepts %s](libs/assembly-lines/src/node-result-schema.test.ts#L103); implemented by [`transition.ts:129`](libs/assembly-lines/src/transition.ts#L129), [`failure-reason.ts:52`](libs/assembly-lines/src/failure-reason.ts#L52))

- **FR6.57** *(added 2026-08-20)* A failed line's terminal reason MUST name the CAUSE. The reason travels to the run row, to the backing task's `failure_reason` (FR6.15) and to the FR6.12 notification, and it is the text a person actually reads — so `AssemblyLine feature-planning: edge analyze->analyze exceeded iteration_max 1`, a true statement about the walk and a silent one about the failure, is not good enough. It now reads `node "analyze" failed: Credit balance is too low — Top up the Anthropic account behind the agent's ANTHROPIC_API_KEY (Plans & Billing)`: what died, what it said, and what to do. One composer answers this for both sides of the fence, since the pure replay and the Floor must not tell different stories about one run, and it degrades to the previous `node "<id>" failed` wording for a visit that carries no classification. ([validated by the reason names node, cause and remedy](libs/assembly-lines/src/failure-reason.test.ts#L5), [an unclassified failure keeps the old wording](libs/assembly-lines/src/failure-reason.test.ts#L18), [a detail with no class renders without a hint](libs/assembly-lines/src/failure-reason.test.ts#L24), [an unrecognised class renders without a hint](libs/assembly-lines/src/failure-reason.test.ts#L30), [the detail survives a hint-less class](libs/assembly-lines/src/failure-reason.test.ts#L40), [the line closes with the cause the node recorded](apps/floor/src/jobs/assembly-run/advance.test.ts#L892); implemented by [`failure-reason.ts:35`](libs/assembly-lines/src/failure-reason.ts#L35), [`advance.ts:95`](apps/floor/src/jobs/assembly-run/advance.ts#L95))

- **FR6.58** *(added 2026-08-20)* An ACCOUNT-WIDE failure MUST stop dispatch rather than be re-discovered once per run. On 2026-08-20 the Floor dispatched into a dry Anthropic account for two hours: twelve pods, twelve `npm ci` bootstraps, twelve user-visible failures somebody then had to retry by hand, every one of them decided before its pod started. A classified `anthropic-credit` failure now trips a dispatch gate, and a gated agent node is PARKED, never failed — `advanceLine` returns before it mints a station-run row, so the run stays `running` with no open node and the reaper's existing "running, no open node → advanceLine" arm re-drives it every 60s for free. Minting the row instead would leave the reaper reading it as "relaunch me" and re-dispatching the pod every minute of the outage; failing the run instead would tell an author their work died when it has not. Only agent nodes are gated (a validate or gate station has no model call to fail), the reaper's own relaunch arm is gated too, and a `*/5` cron running the existing `anthropicCreditsExhausted` probe is what CLEARS the gate — nothing else can, because once dispatch stops there are no more pods left to report. The probe is fail-open by construction, so a Floor with no `ANTHROPIC_API_KEY` behaves exactly as before. The outage is surfaced where the work is read, derived on demand from recent `failure_class` rows rather than mirrored out of the Floor's memory, which the web UI cannot see. A parked run SAYS SO: `advanceLine` answers void, so a caller cannot tell parked from advanced, and an operator would get one gate-trip warning and then silence while runs sit `running` with no open node. The park names the run and the node. The banner's own read degrades rather than 500s on a database that predates the failure columns — it is polled by the banner, and a 500 there is the outage-reporting machinery reporting itself — and it quotes the detail belonging to the failure it dates, which two independent `min()` aggregates did not. ([validated by dispatch is allowed until something trips the gate](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L7), [a dry account trips it](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L11), [it reports the cause and when it started](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L20), [it reports no cause while healthy](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L32), [the first trip's timestamp survives later reports](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L40), [a one-run failure does not trip it](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L53), [a rate limit does not trip it](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L62), [it trips with no detail text](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L69), [it clears once the account answers](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L78), [the default clock stamps the trip](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L87), [clearing an untripped gate reports nothing was blocked](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.test.ts#L98), [a blocked node parks with NO row and no CR](apps/floor/src/jobs/assembly-run/advance.test.ts#L828), [the parked node dispatches once the account is healthy](apps/floor/src/jobs/assembly-run/advance.test.ts#L857), [a dry-account failure trips the gate](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L256), [a one-run failure leaves it alone](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L277), [healthy when nothing recent failed on the account](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L9), [the outage, its cause, and the runs it ate](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L19), [a one-run class does not degrade the platform](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L38), [the account-wide class wins over louder local ones](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L51), [a missing detail is survivable](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L70), [the banner renders nothing while the platform is healthy](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L24), [it names the outage as the platform's, not the feature's](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L30), [it quotes the account's own error](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L38), [it says retrying will not help](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L44), [it counts one run in the singular](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L52), [it omits a zero count](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L58), [it renders without a detail line](apps/web-ui/src/app/repos/[owner]/[repo]/features/[id]/PlatformOutageBanner.test.tsx#L64), [`advance.test.ts:843`](apps/floor/src/jobs/assembly-run/advance.test.ts#L843), [`llm-status.test.ts:98`](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L98), [`llm-status.test.ts:113`](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L113), [`llm-status.test.ts:125`](apps/lore-api/src/api/routes/platform/llm-status.test.ts#L125), [`llm-status.test.ts:76`](apps/lore-api/src/integration-tests/llm-status.test.ts#L76); implemented by [`llm-dispatch-gate.ts:32`](apps/floor/src/jobs/assembly-run/llm-dispatch-gate.ts#L32), [`advance.ts:204`](apps/floor/src/jobs/assembly-run/advance.ts#L204), [`llm-status.ts:60`](apps/lore-api/src/api/routes/platform/llm-status.ts#L60))
- **FR6.59** *(added 2026-08-22)* A visit records WHAT IT WAS GIVEN, not only how it went. `ensureStationRun` persists a bounded `input` snapshot — round description, the agent node's resolved prompt (null for a station) or the station node's params (null for an agent), and the `repo`/`ref` the pod clones — because the Agent CR that held them is pruned after the run, so an hour later nothing could answer "what was this node asked to do", and a node handed a stale plan was indistinguishable from one handed the right plan and reasoning badly. The first writer wins: a converged duplicate (the relaunch door re-dispatching the same visit) keeps what the row already says rather than rewriting history, and a resumed fork inherits each copied visit's input, which is history rather than the attempt residue beside it. The assembled context is deliberately excluded — an order of magnitude larger, assembled after the row is minted, and reproducible. A visit recorded before migration 0046 reads null, meaning "not captured", never "no input". ([validated by ensureStationRun persists the input it is given and listStationRuns returns it](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L203), [`assembly-runs.contract.test.ts:229`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L229), [`assembly-runs.contract.test.ts:260`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L260), [`advance.test.ts:1203`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1203), [`advance.test.ts:1278`](apps/floor/src/jobs/assembly-run/advance.test.ts#L1278), [`floor-assembly-run.test.ts:270`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L270), [`floor-assembly-run.test.ts:287`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L287), [`floor-assembly-run.test.ts:299`](apps/floor/src/jobs/assembly-run/floor-assembly-run.test.ts#L299), [`assembly-lines.test.ts:186`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L186))

- **FR6.60** *(added 2026-08-28)* A CR that fails because an AgentDefinition's `skills_source` is unreachable MUST raise the same one-throttled-alert treatment FR6.14 gives a dry Anthropic account — it is the same shape of outage on a different axis (every Claude-agent node the affected CLUSTER claims, not the account). The failure never reaches a structured result line at all: claude crashes before printing one (`Error: Settings file not found: /agent/.claude/settings.json`), so `terminalErrorText` finds nothing and the only structured signal is the Job-level `BackoffLimitExceeded` — which FR6.54 already established is not the agent's own words. Classifying from that Job reason alone would read a permanent misconfiguration as ordinary infra flakiness worth retrying, which is exactly backwards: every attempt fails identically until `skills_source` is fixed (2026-08-27, PR #1603's minikube satellite — two attempts, two identical crashes, discovered only by reading pod logs by hand). The classifier therefore adds `agent-settings-missing` to the shared `classifyError`, matched on the bare stderr line ahead of the generic infra fallback, and marks it `isPermanentFailure` alongside the other config-and-not-flakiness classes (FR6.56). The alert itself reads raw `output`, not the lifted `errorText` FR6.54 built for billing — this failure produces no result line for that lift to find, so the text lives only in the untouched stream. *(amended 2026-08-30)* That asymmetry was the defect, not the design: the ALERT read the raw stream and fired, while the CLASS driving the retry decision read `errorText`, found nothing, and fell through to `BackoffLimitExceeded` — so the alarm was correct and the run was retried anyway. FR6.54's lift now finds this failure's text too, so the class is `agent-settings-missing`, `isPermanentFailure` refuses the back-edge, and the run costs one attempt instead of two. A separate throttle instance (same `BillingAlertThrottle` shape, a different window) keeps the two alerts from suppressing each other. ([validated by `error-classify.test.ts:82`](libs/shared/src/error-classify.test.ts#L83), [`error-classify.test.ts:90`](libs/shared/src/error-classify.test.ts#L91), [`agent-config-alert.test.ts:20`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L20), [`agent-config-alert.test.ts:33`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L33), [`agent-config-alert.test.ts:43`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L43), [`agent-config-alert.test.ts:52`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L52), [`agent-config-alert.test.ts:62`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L62), [`agent-config-alert.test.ts:93`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L93), [`agent-config-alert.test.ts:122`](apps/floor/src/jobs/assembly-run/agent-config-alert.test.ts#L122), [the wiring fires the hook on a failed node](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L178), [the real alert path fires end to end](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L224), [the mirrored zod schema accepts the category too](libs/assembly-lines/src/node-result-schema.test.ts#L34); implemented by [`error-classify.ts:39`](libs/shared/src/error-classify.ts#L39), [`agent-config-alert.ts:31`](apps/floor/src/jobs/assembly-run/agent-config-alert.ts#L31), [`node-event-handler.ts:91`](apps/floor/src/jobs/assembly-run/node-event-handler.ts#L91), [classifies the boot crash as permanent config, not as retryable infra](apps/floor/src/jobs/assembly-run/node-terminal.test.ts#L150), [fails the run on the first attempt and names the misconfiguration](apps/floor/src/jobs/assembly-run/implementation-loop-acceptance.test.ts#L176))
- The features API is the one producer that flips by EXPANDING rather than switching: it emits the run id under both spellings at once, and the UI reads whichever it gets. lore-api and web-ui are separate images in a single umbrella release, so their rollouts never land together — emitting only the new key blanks the running wizard's run graph for that window, and emitting only the old one leaves the rename unfinishable. Both keys come from ONE function rather than a spread at each of the three response sites, because a pair assembled by hand is a pair that eventually disagrees, and a poll reporting one run while a dispatch reports another is a graph pointing at the wrong run. On the reading side the new key WINS, so dropping the deprecated one later is a no-op rather than a regression, and an EMPTY id falls through as if absent rather than being handed on as a run that does not exist. ([validated by carries the run id under both spellings while the rename is in flight](apps/lore-api/src/api/routes/features/features.test.ts#L632), [`run-id.test.ts:5`](apps/web-ui/src/lib/api/run-id.test.ts#L5), [`run-id.test.ts:9`](apps/web-ui/src/lib/api/run-id.test.ts#L9), [`run-id.test.ts:13`](apps/web-ui/src/lib/api/run-id.test.ts#L13), [`run-id.test.ts:19`](apps/web-ui/src/lib/api/run-id.test.ts#L19), [`run-id.test.ts:26`](apps/web-ui/src/lib/api/run-id.test.ts#L26), [`run-id.test.ts:30`](apps/web-ui/src/lib/api/run-id.test.ts#L30), [`feature-poll.test.ts:139`](apps/web-ui/src/lib/feature-poll.test.ts#L139); implemented by [`features-schema.ts:200`](apps/lore-api/src/api/routes/features/features-schema.ts#L200), [`run-id.ts:19`](apps/web-ui/src/lib/api/run-id.ts#L19))

- **FR6.52** *(added 2026-08-20)* The run READS go through the port, not around it. `lore-api`'s browse and by-id routes ran their own SQL over `pipeline.assembly_runs` — the same predicates, the same `ORDER BY created_at DESC, id DESC` tiebreak and the same limit the port's `list` already owned — so "every code-review run" meant two different things depending on which endpoint was asked, and neither could drift into the other's test. WHICH runs a read answers with is now the port's decision; the route joins only the two OTHER tables (`pipeline.tasks` for the PR, `pipeline.llm_calls` for the summed cost) onto the runs it was handed. The cross-table read model stays in the route, because it is a contract over three tables rather than a projection of one. `listSummaries` serves the browse list: the same selection without the blueprint clone, stated as a narrower TYPE rather than a `graph: null`, since a null meaning "not read" is indistinguishable from a null meaning "this run predates clones" and the second is a state a reader must handle. ([validated by `assembly-runs.contract.test.ts:439`](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L475), [`assembly-lines.test.ts:106`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L106), [`assembly-lines.test.ts:233`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L233), [`assembly-lines.test.ts:430`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L430); implemented by [`assembly-runs-pg.ts:424`](libs/shared/src/project/assembly-runs/assembly-runs-pg.ts#L424), [`assembly-lines.ts:29`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.ts#L29))

- **FR6.53** *(added 2026-08-20)* The ENRICHMENT the run reads add is held to a run against the real tables. It is the only SQL those routes write rather than move — the `unnest` join onto the ids the port selected, the LATERAL that falls back to the TASK's calls for spend recorded before per-line attribution existed, and the author COALESCE — and a route test that answers the pool with no rows leaves every enriched field null whatever the query says, which is a green test over an unexecuted string. So the mapping is proven from a non-empty enrichment row and the query itself from a migrated Postgres. `args_pr_number` is lifted the way the SQL it replaced lifted it: `Number(null)` and `Number("")` are a finite ZERO, so a bare coercion answers PR #0 — a link to a pull request that does not exist — where `(args->>'pr_number')::int` answered NULL. ([validated by `assembly-lines.test.ts:282`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L282), [`assembly-lines.test.ts:317`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L317), [`assembly-lines.test.ts:337`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L337), [`assembly-run-enrichment.test.ts:107`](apps/lore-api/src/integration-tests/assembly-run-enrichment.test.ts#L107), [`assembly-run-enrichment.test.ts:116`](apps/lore-api/src/integration-tests/assembly-run-enrichment.test.ts#L116), [`assembly-run-enrichment.test.ts:120`](apps/lore-api/src/integration-tests/assembly-run-enrichment.test.ts#L120); implemented by [`assembly-lines.ts:84`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.ts#L84))
- **FR6.63** *(added 2026-08-30)* A node whose job is to CHANGE THE BRANCH — one of the delivering recipes (`implementation-tdd`, `implementation`, `address-feedback`) — MUST NOT be recorded `success` when its branch has no changes against the default branch, whatever it printed. The Floor decides this right after the node, where it already decides the declared-artifact case, rather than two nodes later at `push`: the next node is another pod with a fresh clone, so `validate` would otherwise diff an empty branch and lint the whole tree for nothing. The failure is retryable (`unknown`), because an agent that forgot to push is exactly what the self-retry edge exists for, and a composition that provides no change count trusts the node's own word. Both terminal doors share the decision. ([validated by [records the node failed, naming the empty branch, when zero commits landed](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L559), [keeps the success when commits landed, and moves on to validate](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L576), [changes nothing when the composition provides no commit count](apps/floor/src/jobs/assembly-run/node-event-handler.test.ts#L586); implemented by [`node-event-handler.ts`](apps/floor/src/jobs/assembly-run/node-event-handler.ts))

- The web UI serves the run at `/assembly-runs/<id>`, and the pre-rename path REDIRECTS there rather than 404ing. This is the one part of the rename whose readers are outside the system entirely: GitHub Issues, PR bodies and `lore/code-review` check-run summaries already posted embed `/assembly-lines/<uuid>`, and no release can rewrite them. `/pipeline` — the rename before this one — points straight at the current path rather than chaining through the intermediate hop, since a chain costs a round trip and breaks the day the middle hop is deleted. Every entry is non-permanent on purpose: a 301 is cached by browsers indefinitely, so it could not be taken back if a path had to move again. The internal `/api/assembly-lines/*` proxy paths are deliberately NOT part of this — retiring those is a separate, deploy-gated step. ([validated by sends /assembly-lines/<id> to the same id under /assembly-runs](apps/web-ui/src/lib/legacy-route-redirects.test.ts#L22), [`legacy-route-redirects.test.ts:28`](apps/web-ui/src/lib/legacy-route-redirects.test.ts#L28), [`legacy-route-redirects.test.ts:34`](apps/web-ui/src/lib/legacy-route-redirects.test.ts#L34), [`legacy-route-redirects.test.ts:44`](apps/web-ui/src/lib/legacy-route-redirects.test.ts#L44), [`references.test.ts:32`](libs/shared/src/references.test.ts#L32); implemented by [`next.config.ts`](apps/web-ui/next.config.ts), [`references.ts:48`](libs/shared/src/references.ts#L48))

- The four tables that merely POINT at a run keep `assembly_line_id`, and that is a decision rather than an omission. `pipeline.agent_run_events`, `agent_run_turns`, `llm_calls` and `agent_conversations` are not themselves renamed, so no compat view can cover a renamed column on them — a view needs a name and the table already owns it. Telemetry ingest is a deliberately FK-free, skip-not-fail BATCH insert, so a rolled-back Floor writing the column it knows fails the whole batch: the run visualization would die rather than degrade. Renaming safely means add-backfill-dual-write-flip-drop — four migrations and two deploys — to buy a word, and the word is not even the load-bearing one: `station_run_id` is the correlation key these tables now use, and `assembly_line_id` is a secondary pointer. So the columns stay, and the guard is that every query naming them is pinned as SQL TEXT: a mocked pool and an in-memory double both answer to any column name, so the only thing that catches a future sweep is an assertion on the statement itself, and the failure it prevents is a runtime 42703 — which is exactly how the attempt during the rename was found, as a permanent 500 from spend analytics. ([validated by attributes run-scoped spend through llm_calls.assembly_line_id](apps/lore-api/src/api/routes/analytics/spend-window.test.ts#L508), [`assembly-lines.test.ts:269`](apps/lore-api/src/api/routes/assembly-lines/assembly-lines.test.ts#L269), [`agent-run-events.test.ts:521`](libs/shared/src/project/agent-run-events/agent-run-events.test.ts#L521), [`agent-run-turns.test.ts:419`](libs/shared/src/project/agent-run-turns/agent-run-turns.test.ts#L419), [`conversations-pg.test.ts:12`](libs/shared/src/project/conversations/conversations-pg.test.ts#L12), [`conversations-pg.test.ts:25`](libs/shared/src/project/conversations/conversations-pg.test.ts#L25); implemented by [`spend-window.ts:397`](apps/lore-api/src/api/routes/analytics/spend-window.ts#L397), [`conversations-pg.ts:42`](libs/shared/src/project/conversations/conversations-pg.ts#L42))

## Requirement Traceability (Background)

Each functional requirement maps to the user scenario(s) that exercise it and the
success criteria it advances. Per-statement test and implementation links live
inline on each FR above (`validated by` / `implemented by`).

| Requirement | Scenario(s) | Success criteria |
|---|---|---|
| FR1.1 Trailers on every commit | 1, 6 | SC5 |
| FR1.2 Next node from persisted node rows (restated) | 2 | SC2 |
| FR1.3 No-op phase checkpoints as a node row (DB-as-state) | 1 | SC5 |
| FR1.4 Recorded node history is immutable (DB-as-state) | 2 | SC2, SC5 |
| FR1.5 `Lore-Task:` in PR body | 7 | SC5 |
| FR1.6 Structural concurrency + branch overlap guard (re-scoped) | 2 | SC2 |
| FR2.1 Workflow YAML (no alt formats) | 1, 3 | SC1 |
| FR2.2 Node/edge model | 1, 3 | SC1 |
| FR2.3 Same definition local + GKE | 1, 2, 3 | SC1 |
| FR2.4 Migrate existing flows | 6 | SC1 |
| FR2.5 New flow = new graph only | — | SC1 |
| FR3.1 `enabled` gate | 6 | SC4 |
| FR3.2 `create_issue` | 1, 4 | SC4 |
| FR3.3 `auto_merge` allowlist + trust | 1, 3 | SC3, SC6, SC7 |
| FR3.4 `review` gate | 3 | SC6 |
| FR3.5 `notify` channels | 5 | SC4 |
| FR3.6 Per-task overrides | 4 | — |
| FR3.7 Auto-merge audit record | 1, 3 | SC5, SC7 |
| FR3.8 Escalation Issue content | 5 | SC4 |
| FR3.9 Two-key settings authz | (settings change) | SC7 |
| FR4.1–4.4 Migration & compatibility | 6 | SC8 |
| FR5.1/5.1a Observability (auto-merge span + NDJSON→llm_calls) | 2 | SC2 |
| FR5.2 Repo dashboard | 7 | — |
| FR5.3 Timeline + `Lore-Task:` resolver | 7 | SC5 |

## Success Criteria (Background)

These are org-level outcome metrics measured operationally after enablement, not
unit-testable assertions — the underlying behaviours are validated via the FRs
above (e.g. SC7 ← FR3.3/3.7 path-allowlist, SC2 ← FR1.2/1.6 replay).

**SC1 — Handover reduction**
For implementation tasks, **the old 4+ CR / Issue / comment handoff chain per task collapses** — the event-driven walk dispatches one Agent/station CR per node with no cross-pod handover ledger — measured over a 7-day window after dark mode is enabled on a representative repo.

**SC2 — Pod-death survivability**
*(restated 2026-07)* **A Floor pod dying at ANY point leaves no assembly line stranded**: there is no walker process — transitions are event-driven over persisted node rows (FR6.9), so already-recorded nodes are never re-executed, in-flight Agent CRs keep running and their terminal events (or the FR6.10 reaper) advance the line on the next Floor instance. Measured by `kubectl rollout restart` during canary runs across all node types.

**SC3 — Stale-PR elimination**
On dark-mode-enabled repos, **no PR auto-generated by Lore stays open with green CI + bot-approved status for more than 24 hours**, measured over a rolling 30-day window.

**SC4 — Human notification reduction**
For a representative onboarded repo, **the number of GitHub Issues created by Lore drops by at least 80%** when comparing the 30-day post-enable window against the 30-day pre-enable baseline (captured in `pipeline.dark_factory_baseline` per T011b), while task throughput stays equal or higher. The baseline is captured by the settings write that flips `dark_factory.enabled` on — the only moment that can guarantee the window is pre-enablement — and only on the off→on transition, since re-capturing while enabled would overwrite the comparison with a post-enable window. Built in #307 and wired in #1353; before that nothing wrote the table and this criterion was unmeasurable. ([validated by `baseline-capture.test.ts:9`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L9), [`baseline-capture.test.ts:13`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L13), [`baseline-capture.test.ts:19`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L19), [`baseline-capture.test.ts:28`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L28), [`baseline-capture.test.ts:34`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L34), [`baseline-capture.test.ts:59`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L59), [`baseline-capture.test.ts:69`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L69), [`baseline-capture.test.ts:80`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L80), [`baseline-capture.test.ts:91`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L91), [`baseline-capture.test.ts:99`](apps/lore-api/src/features/dark-factory/baseline-capture.test.ts#L99))

**SC5 — Audit completeness preserved**
**100% of Lore-authored merged PRs are resolvable to their originating task via the `Lore-Task:` trailer**, with the branch's commit log reconstructing the full phase sequence and outcomes.

**SC6 — Human review focus**
For dark-mode-enabled repos, **the share of bot-authored PRs that require human review drops to ≤ 30%** of the dark-mode total, with the remainder auto-merging on policy.

**SC7 — Trust-based gating works**
**Zero auto-merges occur outside the configured path allowlist** during the first 90 days post-launch. Any violation is treated as a P1 incident.

**SC8 — Adoption gate**
At least **three repos representing distinct trust tiers (`docs`, `tests`, `implementation`)** must be running dark mode for ≥ 14 days each before the feature can be declared general-availability.

## Key Entities (Background)

**Assembly Line Graph** — A declarative description of a flow as nodes and edges. Stored in `assembly-lines/*.yaml` and loaded by the shared kernel that both the local runner and the Floor walk. ([implemented by `loader.ts:128`](libs/assembly-lines/src/loader.ts#L128))

**Stage Commit** — A Lore-authored git commit carrying `Lore-Stage:`/`Lore-Iteration:`/`Lore-Task:` trailers — the audit substrate (FR1.1). Under the event-driven walk (ADR-031) the durable handover unit between phases is the `pipeline.station_runs` row, not a pushed commit; stage commits are local audit detail, never the resume source. ([implemented by `commit-trailers.ts:30`](libs/shared/src/commit-trailers.ts#L30))

**Dark-Factory Policy** — The merged result of the per-repo `settings.dark_factory.*` block plus per-task overrides. Determines: whether to create an Issue, whether to auto-merge on success, who to notify, and how strict the review gate is. ([implemented by `dark-factory-settings.ts:63`](libs/shared/src/dark-factory-settings.ts#L63))

**Auto-Merge Decision Record** — An audit-log entry capturing every auto-merge: the PR, the task, the policy that applied, the trust level at decision time, the CI status, and the bot-review verdict. Required for forensic reconstruction. ([implemented by `auto-merge.ts:60`](apps/floor/src/jobs/merge/auto-merge.ts#L64), [`audit.ts:9`](apps/floor/src/jobs/lib/audit.ts#L9))

**Escalation Issue** — A GitHub Issue created on the fly when a task hits `needs-human-help`. Differs from today's per-task Issue in that it carries diagnostic context and links to partial work; humans can act on it without re-deriving state. ([implemented by `escalation.ts:65`](libs/shared/src/escalation/escalation-body.ts#L76))

## Constitutional Impact (Background)

This feature **partially supersedes** the row "Task tracking | Pipeline tasks via Lore MCP + GH Issues" in Principle 7 of the constitution. Per the amendment procedure, this requires:

- An ADR superseding the task-tracking decision row, documenting the alternatives rejected (keep Issues mandatory; remove Issues entirely; the chosen middle path of opt-in-per-repo).
- A constitution patch (MINOR version bump) updating the row to "Task tracking | Pipeline tasks via Lore MCP; GH Issues for exception surfaces (opt-out)".

All other principles remain intact. Specifically preserved:
- **P4 Three-Command Developer Interface** — no new commands.
- **P5 Single Interface (Lore MCP)** — no change to MCP surface.
- **P9 Intelligent Agents Over Mechanical Scripts** — strengthened (supervisor agents now own end-to-end flows).
- **P11 Intelligent Memory Lifecycle** — unchanged.

## Assumptions (Background)

- The progressive-trust system (`docs → tests → implementation → full`) accurately reflects per-repo readiness and is the right input to auto-merge gating.
- GitHub does not impose API rate limits that make on-the-fly escalation Issue creation unreliable. (Mitigation: Issue creation already used elsewhere; rate limits are familiar.)
- Web-ui pipeline page is acceptable as the canonical "where's my task" surface for non-engineer stakeholders. PMs already have access.
- _(Superseded 2026-07)_ The original branch-as-state assumptions — that in-flight branches are never human-rebased, and that supervisor pods can cheaply read git history at startup — no longer bind: under the event-driven walk (ADR-031) resume derives from `pipeline.station_runs` rows, not git history, so there is no supervisor and no git-log resume to protect.

## Dependencies (Background)

- The progressive-trust system (`settings.trust.level`) — already shipped.
- The audit_log infrastructure — already shipped.
- The web-ui pipeline page and task detail view — already shipped; this feature adds rendering for stage timelines and the `Lore-Task:` resolver.
- The OpenTelemetry instrumentation — already shipped; this feature adds new span types.
- GitHub App permissions for auto-merge — currently the App can comment and create PRs; merge permissions need verification at plan time.

## Branch-guard exemptions and coordination-skip display (added 2026-07)

- **FR1.6a** *(retired 2026-08-19 — the branch-overlap guard was replaced by the subject key, FR6.49)* `comment-triage` and `code-review-reply` used to be listed as exempt from the branch-overlap guard, because every PR comment rides the PR's head branch carrying a DISTINCT payload and guarding them dropped a burst as `lease_held`. Exemption is no longer a list to keep: the subject guard is opt-IN, so a line that is meant to overlap simply declares no subject and nothing has to remember its name. ([validated by runs carrying no subject key start independently of each other](libs/shared/src/project/assembly-runs/assembly-runs.contract.test.ts#L670))
- **FR1.6b** The assembly-lines runs list folds `lease_held` coordination skips away by default behind a toggle that reveals them in place, and renders a placeholder row when every run in view is a coordination skip. ([validated by `AssemblyRunsTable.test.tsx:96`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L96), [`AssemblyRunsTable.test.tsx:128`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L128), [`AssemblyRunsTable.test.tsx:156`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L156), [`AssemblyRunsTable.test.tsx:174`](apps/web-ui/src/app/assembly-runs/AssemblyRunsTable.test.tsx#L174))
